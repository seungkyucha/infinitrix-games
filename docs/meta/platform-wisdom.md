# InfiniTriX 플랫폼 지혜 (누적 학습)
_마지막 갱신: 사이클 #38 gravity-flip PENDING — 중력 반전 레이지 플랫포머. 5구역×3스테이지+보스 5종+히든 3개=23스테이지+업그레이드 트리 3갈래×5단계+DDA 4단계. 원터치 중력 반전 + 적대적 환경 + 즉사/즉부활 루프. 4상태 TRANSITION_TABLE 단일 정의로 STATE_PRIORITY 최소화 시도. 수학적 난이도 곡선 공식 + BFS 도달 검증 + 보스 약점 ASCII 시각화. 2사이클 연속 PENDING(Cycle 37→38). assets/ 38사이클 연속 재발 우려. 공용 엔진 미분리 38사이클째._

## 피해야 할 패턴 🚫
- **[Cycle 1]** 범용 템플릿에서 에셋을 복사할 때 사용하지 않는 에셋을 남겨두면 불필요한 네트워크 요청이 발생한다 (player.svg, enemy.svg 등 3개 잔존)
- **[Cycle 1]** "외부 에셋 0개" 원칙을 세웠으면서 Google Fonts를 로드하는 등 기획 원칙과 구현의 불일치. 원칙을 세웠으면 코드 리뷰 단계에서 체크리스트로 검증해야 한다
- **[Cycle 1]** iframe 환경에서 `confirm()`/`alert()` 사용 불가 — 게임 내 확인 UI는 반드시 Canvas 기반 모달로 구현할 것
- **[Cycle 1]** 애니메이션을 "나중에 추가"하려고 setTimeout 잠금만 걸어두면, 결국 미구현으로 남는다. 이동 애니메이션은 핵심 피드백이므로 초기부터 구현해야 한다
- **[Cycle 2]** 기획서에 입력 모드별 동작 분기를 명시했어도, 코드에서 조건 변수를 선언만 하고 실제 분기에 사용하지 않으면 의도와 다르게 동작한다. 구현 후 기획서 대조 체크리스트 필수
- **[Cycle 2]** TweenManager 같은 핵심 유틸리티에서 `cancelAll()`과 `update()`가 동시에 호출될 수 있는 경로를 설계 시점에 차단해야 한다. 런타임 중 컬렉션 변경은 deferred 패턴으로 보호할 것
- **[Cycle 2]** **게임 상태 머신의 모든 상태에서 어떤 시스템을 업데이트해야 하는지 매트릭스를 만들지 않으면, tween·물리 등 핵심 시스템이 특정 상태에서 멈추는 버그가 생긴다.** CONFIRM_MODAL에서 tw.update()를 호출하지 않아 모달이 alpha=0으로 고정된 B1이 대표 사례. 상태 × 시스템 매트릭스를 기획서에 필수 포함할 것
- **[Cycle 2]** 저장(saveBest) 후에 비교(getBest)하면 항상 false — 상태 변경과 판정의 순서를 반드시 "판정 먼저, 저장 나중에"로 유지할 것 (B4)
- **[Cycle 2]** setTimeout으로 게임 상태 전환을 지연시키는 패턴은 Cycle 1에서 이미 문제가 되었는데 Cycle 2에서도 재발했다(B3). tween onComplete 콜백으로 완전 대체하는 규칙을 코딩 가이드라인에 명시할 것
- **[Cycle 3]** tween 기반 지연 전환(onComplete로 상태 변경) 시, 지연 구간 동안 원래 조건이 매 프레임 참으로 평가되어 콜백이 반복 호출될 수 있다. **반드시 가드 플래그(`waveClearing = true`)를 설정하여 1회만 실행되도록 보호할 것.** (B1: waveComplete 반복 호출로 골드/점수 무한 누적)
- **[Cycle 3]** 게임 내 복수의 상태 전환 tween이 동시에 활성화되면 나중에 완료되는 tween이 먼저 완료된 전환을 덮어쓴다. **상태 전환에 우선순위 개념이 필요** — 예: GAMEOVER 전환은 WAVE_PREP 전환보다 항상 우선. `if (lives <= 0) return;` 같은 사전 체크를 전환 함수 진입부에 배치할 것. (B2)
- **[Cycle 3]** 기획서에 "SVG 필터 미사용"을 명시해도 에셋 생성 단계에서 feGaussianBlur가 재발한다(Cycle 2 B5 → Cycle 3 B4). 기획서 원칙은 코드뿐 아니라 에셋 파일에도 자동 검증 스크립트를 적용해야 실효성이 있다.
- **[Cycle 3]** 변수를 선언만 하고 실제로 갱신/사용하지 않는 "유령 코드"가 반복된다(Cycle 2: 입력 모드 분기, Cycle 3: livesAtWaveStart). **선언된 변수가 기획서 의도대로 사용되는지 코드 리뷰 체크리스트에 포함할 것.**
- **[Cycle 4]** TweenManager의 deferred `cancelAll()` 직후 `add()`를 호출하면 `_pendingCancel` 플래그가 신규 tween까지 삭제한다. **cancelAll과 add 사이의 경쟁 조건은 deferred 패턴의 맹점이다.** `clearImmediate()` 같은 즉시 정리 API를 분리하거나, cancelAll 직후 `_pendingCancel = false` + `_tweens.length = 0`으로 플러시해야 한다. (B1: 게임 시작 불가 CRITICAL)
- **[Cycle 4]** SVG 에셋 재발이 3사이클 연속(Cycle 2→3→4)이다. "기획서에 금지 명시"만으로는 에셋 생성 단계의 위반을 막을 수 없음이 확정적으로 증명됨. **자동 grep 검증 스크립트 없이는 해결 불가.**
- **[Cycle 5]** 자동 검증 스크립트를 도입해도, 코드 완성 후에만 돌리면 초기 코드에 잔존 패턴(ASSET_MAP, SPRITES, preloadAssets)이 1회차 리뷰에서 발견된다. **검증은 코딩 중에도 수시로 실행해야 효과적이다.** 코드 완성 후 1회 실행으로는 부족.
- **[Cycle 5]** 단일 변수(bpm)에 대해 tween 갱신과 직접 대입이 동시에 존재하면 이중 등록 문제가 발생한다. **하나의 값에 대한 갱신 경로는 반드시 하나로 통일할 것.** (1회차 B2: bpmObj tween + bpm 직접 반영 중복)
- **[Cycle 5]** 범용 템플릿에서 assets/ 디렉토리를 복사하면 코드에서 미참조하는 SVG 파일이 계속 잔존한다. 코드 레벨 검증만으로는 파일 잔존을 막을 수 없으므로, **에셋 디렉토리 자동 정리 또는 미참조 에셋 경고 스크립트가 필요하다.**
- **[Cycle 5]** 기획서에서 "모든 상태 전환에 beginTransition() 필수"를 명시해도 startGame()/goToTitle() 같은 직접 전환이 발생한다. **기획서 원칙과 구현의 불일치는 코드 리뷰 체크리스트에서 명시적으로 검증해야 한다.** 기능적 문제가 없어도 전환 애니메이션 누락으로 UX가 저하된다.
- **[Cycle 6]** 3단계 자동 검증(코딩 전/중/후)을 기획했음에도 1회차 리뷰에서 assets/ 디렉토리가 잔존 발견. **검증 스크립트는 기획서에 명시하는 것만으로는 부족하고, CI/빌드 파이프라인에 강제 실행되도록 통합해야 실효성이 있다.**
- **[Cycle 6]** 전역 객체에 직접 의존하는 유틸 함수(`speed()`가 `ball`에 직접 접근)는 테스트 불가. **게임 로직 함수는 파라미터를 통해 데이터를 받는 순수 함수 패턴으로 작성해야 단위 테스트가 가능하다.**
- **[Cycle 7]** assets/ 디렉토리 + SVG 에셋 재발이 7사이클 중 대부분에서 반복. Cycle 5~6에서 "종결" 선언 후에도 1회차 리뷰에서 재발 발견(2회차에서 삭제 완료). **기획서 명시·자동 검증 스크립트·3단계 체크리스트 모두 실효성이 제한적**임을 확정. CI/빌드 훅에서 `assets/` 존재 시 빌드 실패하는 강제 장치만이 근본 해결책이다.
- **[Cycle 7]** 순수 함수 설계 원칙을 기획서에 명시하고 대부분 준수했으나, 1회차 리뷰에서 전투/충돌 함수 5개의 전역 P 직접 참조가 발견(2회차에서 모두 파라미터화 수정). **원칙을 세우면 전수 검증 체크리스트로 "모든" 함수를 확인해야 한다.**
- **[Cycle 7]** 기획서 수치(크리티컬 Lv3=30%)와 구현 수치(25%)의 불일치. **기획-구현 정합성 검증이 기능 존재 여부뿐 아니라 수치 레벨까지 내려가야 한다.**
- **[Cycle 8]** assets/ 디렉토리 재발이 8사이클 연속. 기획서 §13.1에 pre-commit 훅 스크립트까지 명세했으나 실제 파이프라인에 등록하지 않아 무효. **기획서에 "스크립트를 작성하라"고 쓰는 것과 실제로 `.git/hooks/`에 등록하는 것은 완전히 다른 단계다.** 다음 사이클에서는 훅 등록 자체를 검증 항목에 포함해야 한다.
- **[Cycle 8]** `beginTransition()` 경유 원칙을 세워도 PAUSED처럼 "즉각 전환이 자연스러운" 상태에서 직접 `enterState()` 호출이 발생한다. **원칙의 예외를 허용할 것인지, 아니면 beginTransition()에 즉시 전환 모드를 추가할 것인지** 설계 결정이 필요하다.
- **[Cycle 10]** 수정 과정에서 새 버그를 도입하는 "수정 회귀" 패턴 발생. 1회차 MINOR 4건 수정 중 `render()` 시그니처 변경 시 `timestamp` 미전달로 CRITICAL 신규 도입(2회차). **수정 후 전체 플로우 회귀 테스트(TITLE→MAP→BATTLE→VICTORY/DEFEAT)를 반드시 수행할 것.** 개별 수정 검증만으로는 부족하다.
- **[Cycle 10]** 기획서 game-id(`mini-card-battler`)와 실제 배포 디렉토리명(`mini-card-crawler`)이 불일치. 기획→구현 과정에서 네이밍 변경 시 기획서도 갱신해야 추적 가능성이 유지된다.
- **[Cycle 10]** 코드에서 에셋 참조가 0건이어도 assets/ 디렉토리가 물리적으로 존재하면 기획 위반. **코드 레벨 클린 ≠ 파일 시스템 레벨 클린.** `fetch`, `Image()`, `assets` 문자열 검색만으로는 불필요 파일 잔존을 감지할 수 없다. pre-commit 훅에서 디렉토리 존재 자체를 차단해야 한다.
- **[Cycle 10]** 극단적 축약 변수명(`shopI` vs `shI`)의 불일치가 CRITICAL 크래시(상점 진입 시 ReferenceError)를 유발. **코드 압축을 위한 축약 시 함수 내 변수 선언과 참조의 일관성을 반드시 확인할 것.** 'use strict' 모드가 미선언 변수를 런타임에 잡아주지만, 선언 자체가 다른 이름으로 되어 있으면 방지 불가. 축약 네이밍 규칙(접두사 패턴 등)을 사전에 정의하는 것이 안전하다.
- **[Cycle 11]** `let gridCache` 선언이 `resizeCanvas()` 호출보다 뒤에 위치하여 TDZ(Temporal Dead Zone) 크래시 발생 → 게임 완전 불능. **`let`/`const` 변수는 반드시 최초 사용(함수 호출 포함) 이전에 선언되어야 한다.** 변수 선언 순서라는 기본적인 실수가 CRITICAL로 이어질 수 있으므로, 초기화 코드 블록의 실행 순서를 명시적으로 검증할 것.
- **[Cycle 11]** 아이들 게임에서 탭 전환(프레스티지/통계) 시 `updateProduction()`을 호출하지 않으면 생산이 멈추는 문제. **아이들/방치형 장르에서는 UI 상태와 무관하게 생산 시스템이 항상 동작해야 한다.** 상태×시스템 매트릭스에 "백그라운드 시스템"을 별도 분류할 것.
- **[Cycle 11 mini-platformer]** assets/ 디렉토리 + SVG 에셋이 11사이클 연속 재발. 기획서 §12.1에서 "절대 금지"로 특별 강조 + platform-wisdom에서 10사이클 연속 재발 경고를 했음에도 10개 SVG 파일 생성. **기획서 명시, 자동 검증 스크립트, 체크리스트, 피드백 매핑 모두 실효성 한계 확정. CI/빌드 파이프라인에서 디렉토리 존재 시 빌드 실패하는 강제 장치만이 근본 해결책.** → 3회차 리뷰에서 완전 삭제 확인, APPROVED.
- **[Cycle 11 mini-platformer]** 3회 리뷰 사이클 소요: 1회차 CRITICAL(assets/ 10 SVG) → 2회차 CRITICAL(ASSET_MAP/SPRITES/preloadAssets 코드 잔존) → 3회차 APPROVED. **에셋 삭제 후에도 관련 코드(ASSET_MAP, SPRITES, preloadAssets, if(SPRITES.xxx) 분기)가 잔존하면 CRITICAL로 재판정된다.** "파일 삭제"와 "코드 정리"를 반드시 동시에 수행할 것.
- **[Cycle 11 mini-platformer]** gameLoop try-catch 래핑이 기획서 §12.9에 명시되었음에도 미적용. 이전 사이클(Cycle 10)에서 동일 패턴을 수정했으나 다음 게임에서 재발. **방어적 코딩 패턴도 게임 간 전파가 자동화되지 않으면 매번 누락 위험.** 공용 엔진 모듈 분리가 근본 해결.
- **[Cycle 11 mini-platformer]** 기획서 명시 기능(일일 챌린지 §6.5) 완전 미구현 + 미사용 스프라이트 3건 로드. **기능 구현 체크리스트에서 "존재 여부"뿐 아니라 "미사용 코드/에셋" 역방향 검증도 필요.**
- **[Cycle 12]** assets/ 디렉토리 12사이클 연속 재발. 이번에는 코드 내 에셋 참조 0건(순수 Canvas 드로잉 달성)이라는 진전이 있으나, 파일 자체(manifest.json + SVG 8개)는 여전히 물리적으로 존재. **"코드 레벨 클린"과 "파일 시스템 레벨 클린"은 별개 문제라는 Cycle 10 교훈이 재확인됨.** CI pre-commit 훅의 실제 등록이 더 이상 미룰 수 없는 과제.
- **[Cycle 12]** 기획서 §5.3 물웅덩이 조향 반전 — 1차 리뷰에서 puddleTimer와 속도 감소만 구현되고 조향 반전이 누락된 것이 발견됨. 최종 리뷰에서 `steerMul = -1` 로직 추가로 해결(L545). **"절반 구현"은 코드 리뷰에서 발견하기 어려운 패턴이다.** 기획서의 "A + B" 명세에서 A만 구현되고 B가 빠지는 경우, 기능별 세부 체크리스트(A: ✅, B: ☐)가 필요. → **최종 리뷰에서 해결됨.**
- **[Cycle 12]** 트랙 선택 화면에서 매 프레임 `loadTrackData()` 호출(5트랙 × 60fps = 초당 300회 localStorage 읽기). **화면 진입 시 1회 캐싱이면 해결되는 패턴이지만, "동작은 하니까" 놓치기 쉽다.** 성능 관련 코드 리뷰 항목에 "반복 호출 내 I/O 접근" 체크를 추가할 것.
- **[Cycle 12 2회차]** 일시정지 버튼 40×40px → WCAG 터치 타겟 최소 44×44px 미달. **터치 타겟 최소 크기(44×44px)를 기획 단계에서 UI 스펙에 명시적으로 포함할 것.** 구현 후 리뷰에서 잡는 것보다 기획서 수치 테이블에 포함하는 것이 효율적.
- **[Cycle 12 2회차]** 빈 if 블록(데드 코드)이 코딩 중 임시로 남겨져 1회차에서 지적됨. **임시 코드 블록에는 TODO 주석을 달거나, 작업 완료 시 즉시 정리하는 습관이 필요.** 빈 블록은 "의도적 미구현"인지 "실수"인지 리뷰어가 판단할 수 없다.
- **[Cycle 13]** **index.html 미생성 — 게임 코드 자체가 존재하지 않는 상태로 리뷰 제출.** 기획서가 27건 피드백 매핑 + 34항목 체크리스트로 역대 최고 완성도임에도 구현이 0%인 전례 없는 사태. **리뷰 제출 전 "index.html 존재 + 페이지 로드 성공"을 확인하는 최소 스모크 테스트 게이트가 필수.** 코드가 없는 상태에서 리뷰가 시작되는 것 자체를 구조적으로 차단해야 한다.
- **[Cycle 13]** assets/ 디렉토리 **13사이클 연속 재발.** 더 나아가 파일명(player.svg, enemy.svg 등)이 워드 퍼즐 장르와 전혀 무관 — 잘못된 템플릿/보일러플레이트 복사가 원인으로 추정. 기획서 명시, 자동 검증 스크립트, 체크리스트, pre-commit 훅 기획 등 **13사이클간 시도한 모든 "기획서 레벨" 대응의 완전한 실패를 확정.** CI 파이프라인에서 `assets/` 존재 시 빌드 실패하는 강제 장치 외에는 해결책이 없다.
- **[Cycle 13]** 잘못된 템플릿 복사로 게임 장르와 무관한 에셋이 생성되는 패턴이 처음 명확하게 확인됨. assets/ 재발의 근본 원인이 "코더의 부주의"가 아니라 **"보일러플레이트 생성 과정의 구조적 결함"**일 가능성. 게임 생성 시 장르별 템플릿 분기 또는 빈 디렉토리 생성을 원천 차단하는 scaffolding 도구 검토 필요.
- **[Cycle 13 mini-idle-farm]** `CONFIG.MIN_TOUCH = 48` 상수를 선언해놓고 실제 버튼 크기 지정에 참조하지 않는 "선언-구현 괴리" 패턴 발생. **설정값을 선언하는 것과 실제 적용하는 것은 다른 단계다.** 버튼 렌더링 함수에서 `Math.max(MIN_TOUCH, h)` 같은 강제 적용 로직을 표준화해야 한다.
- **[Cycle 13 mini-idle-farm]** SoundManager에서 setTimeout으로 사운드 시퀀싱을 구현한 것이 "setTimeout 0건 목표" 기획과 불일치. 게임 로직에는 무영향이나, **Web Audio `oscillator.start(ctx.currentTime + delay)` 네이티브 스케줄링으로 완전 대체 가능한 패턴이므로 다음 사이클에서 표준화할 것.**
- **[Cycle 14]** `let canvas, ctx;` 선언 후 `init()` 호출 전에 `canvas.addEventListener(...)` 실행 → TypeError로 게임 로드 완전 불능. **Cycle 11 TDZ 크래시와 동일 계열("변수 초기화 전 사용")이 변형된 형태로 재발.** 기획서 F14에서 "let/const TDZ 크래시" 방지를 명시했으나, 이벤트 리스너 등록 위치라는 변형 패턴을 커버하지 못했다. **초기화 순서 검증은 "변수 선언 순서"뿐 아니라 "DOM 할당 전 DOM 접근" 패턴까지 확장해야 한다.**
- **[Cycle 14]** 기획서 F13에서 "터치 타겟 48×48px" 명시 + Cycle 12~13에서 반복 지적됐음에도 일시정지 버튼 48×36px로 미달. **터치 타겟 크기는 기획서 명시만으로는 해결되지 않으며, 렌더링 함수에서 `Math.max(MIN_TOUCH, size)` 강제 적용 패턴이 표준화되지 않는 한 계속 재발할 것이다.**
- **[Cycle 14]** 리뷰 제출 전 스모크 테스트에서 "파일 존재"는 확인했으나 "콘솔 에러 0건"은 미확인. Cycle 13("파일 미존재") → Cycle 14("파일 존재하나 에러로 미동작")으로 실패 패턴이 진화. **스모크 테스트 게이트는 "index.html 존재 + 페이지 로드 성공 + 콘솔 에러 0건"의 3단계로 확장해야 한다.**
- **[Cycle 14 mini-rogue-dice]** 재기획 시 첫 시도(fruits-merge)의 실패 원인("이벤트 리스너 init() 외부 등록")을 분석하고 피드백 28건을 재매핑했음에도, **재기획 자체가 사이클 자원의 상당 부분을 소모했다.** 스모크 테스트 3단계 자동화로 "수정 1건으로 해결 가능한 CRITICAL"을 재기획 없이 복구하는 경로를 확보해야 한다.
- **[Cycle 14 mini-dungeon-dice]** assets/ 디렉토리가 여전히 물리적으로 존재하며 manifest.json + SVG 8개를 포함한다. 코드에서 모든 에셋에 `if (SPRITES.xxx)` 가드 + Canvas 폴백이 완비되어 기능 이슈는 없으나, **14사이클 연속 재발 확정.** "빈 index.html에서 시작" 원칙이 에셋 하이브리드 전략(의도적 SVG 사용)과 충돌하는 경우, F6 위반의 허용 범위를 명확히 정의해야 한다.
- **[Cycle 15]** assets/ 디렉토리 + SVG 에셋이 **15사이클 연속 재발**. 기획서 F3에서 "assets/ 디렉토리 절대 생성 금지"를 명시하고, "빈 index.html에서 시작"까지 기획했음에도 1회차 리뷰에서 SVG 8개 + manifest.json 발견. 2회차에서 100% Canvas 드로잉으로 전환하여 해결. **CI/pre-commit 훅의 실제 등록 없이는 이 패턴이 영원히 반복될 것이 15사이클간의 데이터로 확정.**
- **[Cycle 15]** cellSize 48px 미달이 1회차에서 재발 — 400px 뷰포트에서 45px로 산출. 기획서 F6에서 `CONFIG.MIN_TOUCH_TARGET` 직접 참조를 명시했으나 초기 구현에서 `Math.max()` 적용이 누락. **터치 타겟 크기 보장은 기획서 명시 + 구현 시 렌더링 함수에서의 강제 적용 + 리뷰 자동 검증의 3중 방어가 필요.**
- **[Cycle 15]** 스테이지 11~30의 목표 유형(색상 제거/특수 보석 생성)이 1회차에서 미구현 — 전 스테이지가 점수 달성만으로 동작. 기획서 §2.4에서 3유형 목표를 명세했으나, 구현 우선순위에서 누락된 "절반 구현" 패턴(Cycle 12와 동일). **기능별 세부 체크리스트에서 "A + B + C" 명세의 개별 구현 완료를 각각 확인해야 한다.**
- **[Cycle 15]** 5회 리뷰 사이클(역대 최다)이 소요된 원인: assets/ 재발(1~2회차) → cellSize/목표 미구현(1~2회차) → assets/ 4회차 재출현(회귀) → 5회차 최종 해결. **수정 후 회귀 발생(4회차)은 "수정이 완전히 끝났는지" 확인하는 최종 검증 단계의 부재가 원인.** 리뷰 제출 전 자동 스모크 테스트가 있었다면 4~5회차는 불필요했다.
- **[Cycle 16]** 1차 리뷰에서 index.html 미존재 + assets/ 디렉토리 존재라는 **기존 반복 패턴이 16사이클째 재발**. 기획서 §0에서 F1~F23까지 23건 피드백을 역대 최다로 선제 매핑했음에도 1차에서 CRITICAL 2건 발생. **기획서 레벨 대응의 한계가 16사이클 데이터로 재확정** — 스모크 테스트 게이트 자동화 + CI 강제 외에는 근본 해결 불가.
- **[Cycle 16]** 레벨별 활성 색상 수 불일치(기획 3색 vs 코드 4색) — 수치 정합성 검증 테이블(Cycle 8 도입)이 "색상 배열" 같은 열거형 수치까지 커버하지 못하는 맹점 발견. **정수/실수 수치뿐 아니라 배열·열거형 데이터의 정합성도 검증 범위에 포함해야 한다.**
- **[Cycle 17]** `_origBeginTransition` 유령 변수 잔존(L1237) — F15(유령 변수 방지)에서 검증 테이블을 기획서에 포함했음에도 1건 누락. **변수 사용처 검증은 기획서 수준 명시만으로는 100% 커버되지 않으며, IDE 레벨(미사용 변수 경고) 또는 린트 도구의 보완이 필요.**
- **[Cycle 17]** scorePopups의 `life -= 1`이 update가 아닌 render 함수 내에서 처리(L934) — 프레임 기반 감소라 고/저 주사율에서 팝업 속도가 달라질 수 있다. **상태 변경은 반드시 update 함수에서, 렌더링은 render 함수에서라는 분리 원칙을 더 엄격하게 적용할 것.** 기능 영향은 미미하나 코드 위생 차원에서 개선 권장.
- **[Cycle 17]** 불멸 벽돌(hp=-1)과 레이저 파워업의 상호작용이 기획서에 미정의 — 레이저가 불멸 벽돌을 뚫고 사라지는 동작이 의도인지 불명확. **오브젝트 간 상호작용 매트릭스(파워업 × 벽돌 타입)를 기획서에 포함하면 이런 모호함을 사전에 방지 가능.**
- **[Cycle 17 arcane-bastion]** **index.html 미생성이 2회차 리뷰까지 지속 — Cycle 13 이후 3번째 "구현 0%" 사태.** 기획서 F1~F24를 역대 최다로 매핑했으나 실제 코드 작성이 전혀 이루어지지 않았다. **기획서 품질과 구현 완성도는 독립적 문제이며, 리뷰 제출 전 "index.html 존재 + 페이지 로드" 스모크 테스트 자동화 없이는 구조적으로 해결 불가.**
- **[Cycle 17 arcane-bastion]** **assets/ 디렉토리 역방향 퇴보 — 1회차에 없던 assets/가 2회차에 SVG 8개(bg-layer1/2, enemy, player, powerup, effect-hit, ui-heart, ui-star)와 함께 신규 생성.** 기획서 §0 F1에서 "assets/ 디렉토리 절대 생성 금지"를 명시적으로 강조했음에도 정면 위반. **17사이클간 반복된 F1 문제가 "수정 과정에서 오히려 악화"되는 새로운 패턴 확인. CI 강제 차단 없이는 이 문제가 해결 불가능하다는 결론이 더욱 확고해짐.**
- **[Cycle 17 arcane-bastion]** **action+strategy 하이브리드(15웨이브×5적종×3타워×3보스×3바이옴×로그라이크 업그레이드)의 기획 범위가 단일 사이클 구현에 비현실적.** Cycle 16 포스트모템에서 "범위 축소 전략"을 강조했으나 이번 기획은 오히려 복잡도가 증가. **하이브리드 장르 시도 시 MVP(3상태+적1종+타워0개+웨이브3개)를 먼저 구현하고 점진적으로 확장하는 전략이 필수.**
- **[Cycle 18]** **1회차에서 assets/ 디렉토리 + 에셋 로딩 코드(ASSET_MAP, SPRITES, preloadAssets)가 재발.** 기획서 §0 F1에 "assets/ 절대 생성 금지, 100% Canvas 코드 드로잉"을 명시하고 §16.5에 자체 검증 체크리스트까지 포함했음에도, 관성적으로 에셋 구조를 생성. 2회차에서 완전 삭제하여 APPROVED되었지만, **18사이클 연속 재발이 입증하는 것은 기획서·체크리스트로는 이 문제를 1회차에서 잡을 수 없다는 것. CI 자동 차단만이 해결책.**
- **[Cycle 18]** **서바이버라이크 5무기×5적×10웨이브 밸런스 검증 수단 없음.** DMG_TABLE 상성 테이블을 설계했으나, 특정 무기 조합의 압도성이나 후반 웨이브 불가능성을 코드 리뷰만으로 판단 불가. **headless 밸런스 시뮬레이터 도입이 액션 장르에서도 필요.**
- **[Cycle 19]** **assets/ 디렉토리 19사이클 연속 재발.** 기획서 §14.5에 "assets/ 절대 생성 금지, 100% Canvas 코드 드로잉"을 명시하고 F1~F30까지 30건 피드백 매핑(역대 최다)을 했음에도 1회차에서 8개 SVG + manifest.json 생성. Canvas fallback이 완비되어 삭제만 하면 되지만, **19사이클 데이터가 "기획서/체크리스트/피드백 매핑 어떤 조합으로도 1회차 방지 불가"를 통계적으로 확정. CI 강제 차단이 유일한 해결책.**
- **[Cycle 19]** **적 디버프가 플레이어에 실제 미적용 — `applyStatus({}, ...)` 빈 객체 대입 버그.** 밴시/가디언/아크메이지의 약화/취약/둔화가 시각 텍스트만 표시되고 실제 효과 없음. 게임이 의도보다 쉬워지는 밸런스 문제. **디버프/버프 시스템은 대상 객체 참조의 정확성을 단위 테스트 수준에서 검증해야 한다.** "절반 구현"(Cycle 12 이래 반복)의 변형 패턴.
- **[Cycle 19]** **기획서 명시 입력(스와이프 스크롤, D키 덱 보기, 방향키 맵 선택)이 미구현(M1~M3).** Phase 1~5 핵심 기능은 완전 구현되었으나 보조 입력 기능 3건이 누락. **기획서 §3(입력 스펙)의 세부 항목을 구현 체크리스트에 개별 항목으로 분리하면 누락을 방지할 수 있다.**
- **[Cycle 20]** **assets/ 디렉토리 20사이클 연속 재발.** 기획서 §14.5에 "assets/ 절대 생성 금지, 100% Canvas 코드 드로잉"을 명시하고 F1~F32 역대 최다 32건 피드백 매핑에도 9개 SVG + manifest.json 생성. Canvas fallback 완비로 삭제만 하면 되지만, **20사이클 데이터가 "기획서/체크리스트/피드백 매핑 그 어떤 조합으로도 1회차 방지 불가"를 최종 확정. CI 강제 차단이 유일한 해결책이며 더 이상의 지연은 정당화 불가.**
- **[Cycle 20]** **hitAnim 감소가 render()에서 수행(M4) — F26(render 순수 출력 원칙) 위반.** `drawBumper()`와 `drawCrystalTarget()`에서 hitAnim을 직접 감소. Cycle 17 scorePopups `life -= 1` render 내 처리와 동일 패턴이 재발. **상태 변경을 render에서 수행하는 패턴이 3사이클째(Cycle 17→18→20) 반복 — ESLint 커스텀 규칙 또는 린트 도구 없이는 완전 방지 불가.**
- **[Cycle 20]** **업그레이드 상점 터치 스크롤 미구현(M2)** — `wheel` 이벤트만 처리하고 `touchmove` 드래그 스크롤이 없어 모바일에서 8종 업그레이드 탐색 불가. 기획서 F32에서 "Canvas 내 스크롤 구현 포함"을 명시했으나 터치 경로가 누락. **"절반 구현" 패턴이 Cycle 12 이래 계속 변형 재발.** 기능별 입력 수단(키보드/마우스/터치) 커버리지 매트릭스를 기획서에 포함해야 한다.
- **[Cycle 20]** **터치 플리퍼 영역 시각 가이드 부재(M3)** — 화면 영역 분할 방식이라 터치 가능 영역이 시각적으로 표시되지 않음. 첫 모바일 플레이 시 어디를 터치해야 하는지 모름. **물리 게임(핀볼, 당구 등)에서 비가시적 터치 영역은 온보딩 장벽이므로, 초기 플레이 시 반투명 힌트 오버레이를 표시하는 패턴을 표준화할 것.**
- **[Cycle 21]** **gridCacheCanvas/Ctx 유령 변수 잔존(P3)** — 오프스크린 캐싱을 위해 선언했으나 실제 구현하지 않은 변수가 4,215줄 코드에 잔존. ESLint `no-unused-vars` 없이는 21사이클간 반복되는 유령 변수 문제를 근절할 수 없다.
- **[Cycle 21]** **ObjectPool updateAll() 예외 안전성 미확보(P3)** — 콜백 내부 예외 발생 시 아이템이 풀에 복귀하지 못하는 잠재적 메모리 누수. try-catch 래핑으로 간단히 해결 가능하나 미적용.
- **[Cycle 21]** **4,215줄 단일 파일 — 플랫폼 역대 최대 규모.** 공용 엔진 모듈 분리(shared/engine.js)가 21사이클째 지연 중. TweenManager/ObjectPool/SoundManager/touchSafe() copy-paste가 21개 게임에 걸쳐 반복. **유지보수성 한계를 완전히 초과.**
- **[Cycle 21]** **5회 리뷰 사이클 소요** — 플래너·디자이너 피드백 반영 과정에서 추가 라운드가 필요했다. 코드 자체의 품질은 높았으나 기획·디자인 피드백 반영이 리뷰 사이클을 연장시키는 새로운 패턴. **다중 이해관계자 피드백은 리뷰 시작 전에 통합하여 1회로 반영하는 프로세스가 필요.**
- **[Cycle 22]** **4라운드 리뷰 소요(2차 리뷰 3회차)** — 1차 P1 2건 + P2 2건 → 2차 P3 3건 → 3차 P3-ADV 1건(menuY 레이아웃) → 4차 최종 APPROVED. 초회 APPROVED 달성 실패. **38건 피드백 매핑에도 P1급 이슈가 초회에 발생하는 것은, 피드백 매핑의 양적 한계보다는 새로운 장르 조합(arcade+strategy)의 구현 복잡도가 원인.** 하이브리드 장르는 리뷰 라운드가 늘어남을 감안한 일정 계획이 필요.
- **[Cycle 22]** **menuY 하한 바운드 이슈가 3회차까지 잔존** — 480px 뷰포트에서 타이틀 메뉴 3버튼이 화면 밖으로 잘리는 문제. UI 레이아웃을 다양한 뷰포트에서 검증하는 자동화 테스트가 부재. **반응형 UI의 최소 뷰포트 테스트(390×480 등)를 스모크 테스트에 포함해야 한다.**
- **[Cycle 22]** **5시대×4스테이지×3난이도×7타워×3마법 밸런스 검증 수단 부재.** 타워 디펜스의 조합 공간이 기존 게임 중 가장 넓으나 코드 리뷰만으로는 밸런스 판별 불가. **하이브리드 장르(특히 전략+X)에서는 자동 밸런스 검증이 더욱 시급하다.**

- **[Cycle 23]** GAMEOVER/VICTORY 같은 종결 상태에서 타이틀로 돌아가는 "역방향 전환"은 STATE_PRIORITY 가드에 의해 차단된다. Cycle 21부터 5라운드 연속 미수정된 P0 버그의 원인. **종결 상태 탈출 전환은 반드시 RESTART_ALLOWED 화이트리스트로 명시적 예외 처리할 것.** beginTransition() 진입부에 `if (RESTART_ALLOWED.includes(gameState))` 가드를 배치하는 것이 표준 패턴.
- **[Cycle 23]** 피드백 매핑이 기획서의 15%를 차지하면 가독성이 떨어진다. **검증 완료 패턴은 ID+적용 섹션만 요약 테이블로, 신규 피드백만 상세 기술하는 2계층 분리**가 효과적(F43).

- **[Cycle 24]** 1회차에서 ASSET_MAP/SPRITES/preloadAssets 에셋 로딩 코드와 Google Fonts `<link>` 태그가 잔존 — 기획서에서 F1/F2를 명시했음에도 초기 구현에서 관성적으로 포함. **2차 리뷰 2회차에서야 완전 해결(B3, B5).** "처음부터 생성하지 않는" 원칙이 외부 폰트 CDN까지 확장 적용되어야 함을 재확인.
- **[Cycle 24]** 스모크 테스트 게이트를 10항목으로 확장(F47)했으나, 초기 구현에서 RESTART_ALLOWED(B1, P0)과 터치 48px(B4, P2)가 여전히 누락. **게이트 항목을 기획서에 명시하는 것과 실제로 구현 시 체크하는 것 사이의 간극은 자동화 없이는 메울 수 없다.**
- **[Cycle 24]** 15조수×3난이도×3무기 조합의 밸런스를 코드 리뷰만으로 판별 불가(F46). 3구간 밸런스 테이블을 기획서에 수치화했으나, **실제 플레이 시뮬레이션 없이는 "특정 무기 압도" 또는 "후반 불가능" 문제를 사전에 잡을 수 없다.**
- **[Cycle 25]** STATE_PRIORITY를 기획서 F6에 설계하고도 코드에서 활용하지 않아 데드코드로 잔존(P1). **기획 단계에서 설계한 코드 패턴이 실제로 호출되는 경로를 명시하지 않으면, 선언만 하고 사용하지 않는 "의도-구현 괴리"가 반복된다.** 설계 시 "이 코드가 어디서 호출되는가"를 경로 다이어그램으로 포함할 것.
- **[Cycle 25]** 3라운드 리뷰 소요 — assets/ 불법 SVG 8개 잔존(P0)과 모바일 글리프 슬롯 터치 버튼 미구현(P0)이 1차에서 발견. **F1~F54 역대 최다 54건 피드백 매핑에도 에셋 디렉토리 파일 정리와 모바일 세부 입력 구현이 빠졌다.** 피드백 매핑 건수 증가가 자동 검증 없이는 실효성에 한계가 있음을 25사이클째 재확인.
- **[Cycle 25]** 5바이옴×5보스×20글리프 조합의 밸런스를 코드 리뷰만으로 검증 불가. **메트로이드바니아 장르는 조합 공간이 기하급수적이므로, 글리프 조합별 보스 공략 가능 여부를 자동 시뮬레이션하는 도구가 없으면 "지배적 조합" 또는 "불가능 보스" 문제를 사전에 잡을 수 없다.**
- **[Cycle 26]** assets/ 디렉토리에 불법 SVG 8건이 1차에서 재발(P0) — 10사이클 연속 정리(Cycle 18~25) 기록이 깨질 뻔했으나 2차에서 삭제 완료. **CI 강제 차단 없이는 assets/ 문제가 영원히 재발 가능하다는 26사이클 누적 결론 최종 확정.**
- **[Cycle 26]** bossAttack()에서 Math.random() 사용(W2, P1) — 기획서에 SeededRNG 일관성을 명시했음에도 보스 공격 로직에서 Math.random()이 혼입. **SeededRNG 정책이 있어도 코드 전체에서 `Math.random()` grep 0건을 자동 검증하지 않으면 개별 함수에서 위반이 발생한다.** ESLint 커스텀 규칙("Math.random() 사용 금지, G.rng 필수")으로 자동 감지 필요.
- **[Cycle 26]** drawTitle() 언어 토글 그리기 위치(h*0.85)와 히트 검사 영역(cH*0.85-12) 12px 오프셋 잔존(W1, P1 미수정) — **드로잉 좌표와 히트 검사 좌표가 분리되어 있으면 유지보수 시 동기화가 깨지기 쉽다.** 하나의 상수로 통일하거나, 히트 영역을 드로잉 함수 반환값에서 추출하는 패턴이 필요.

- **[Cycle 28]** **BOOT→TITLE 전환 불가 회귀(2회차 P0)** — 1회차 수정 중 ACTIVE_SYS[BOOT]에 SYS.TWEEN을 누락하여 타이틀 화면 진입 불가. Cycle 10 이래 반복되는 "수정 회귀" 패턴이 상태×시스템 매트릭스 영역에서 재발. **수정 후 전체 상태 전환 플로우(BOOT→TITLE→PLAYING→GAMEOVER→TITLE) 회귀 테스트를 자동화하지 않으면, 단일 시스템 누락이 P0 크래시로 이어진다.**
- **[Cycle 28]** **홀드 비트·더블 비트 밸런스 미검증** — 코드 구현(updateHoldBeats + doublePending + 400ms 타이머)은 완전하나, 보스별 비트 패턴 구성에서의 출현 빈도·난이도 곡선은 실플레이 피드백 없이 설정. **리듬 게임에서 비트 유형별 난이도 곡선은 자동 플레이 시뮬레이터 없이는 밸런스 검증 불가.** BPM×비트유형×판정 정확도 조합의 클리어율을 수치화할 도구가 필요.
- **[Cycle 29]** **`t` 파라미터 섀도잉(P0)** — 9개 draw 함수에서 전역 delta time `t`와 동명의 함수 파라미터가 내부 변수를 섀도잉하여 텍스트 렌더링 전면 고장. **함수 시그니처의 파라미터 이름이 외부 스코프 변수와 충돌하면 P0 크래시로 이어진다.** 전역 변수와 파라미터 이름의 충돌을 lint 또는 네이밍 컨벤션(전역=`g`+접두사)으로 원천 차단할 것.
- **[Cycle 29]** **assets/ 참조 코드 관성적 잔존(P1)** — assets/ 디렉토리는 미생성이었으나, ASSET_MAP·preloadAssets()·SPRITES 참조 코드가 잔존. 13사이클 연속 assets/ 디렉토리 미생성에 성공했음에도 **코드 레벨의 관성적 잔존**이 새로운 형태로 나타남. 디렉토리 검증만이 아니라 **코드 내 `ASSET_MAP`/`SPRITES`/`preloadAssets` 문자열 grep 0건**도 스모크 테스트에 추가해야 한다.
- **[Cycle 29]** **5존×6보스×13아티팩트×3업그레이드 트리 밸런스 미검증** — 메트로이드바니아 로그라이트의 조합 공간이 역대 최대급이나, DPS/EHP 시뮬레이션 없이 수치만 설계. **극단 빌드(대시 특화 + 공격력 아티팩트 올인) 클리어 가능 여부를 확인할 방법이 부재.** 밸런스 시뮬레이터 개발이 30사이클째 미착수.
- **[Cycle 29]** **모바일 원거리 공격 전용 버튼 부재(P3)** — 더블탭으로 원거리 공격이 가능하나 전용 UI 버튼이 없어 직관성 부족. **액션 게임의 모바일 입력 버튼 세트를 기획 초기에 확정하고, 키보드/마우스/터치 3채널 커버리지 매트릭스를 기획서에 포함할 것.**
- **[Cycle 31]** **P0 TDZ 크래시가 3차 리뷰까지 미수정으로 잔존** — `workshopBonuses: {}` 빈 객체 초기화라는 단순 수정이 3라운드 소요. G 객체 선언 시점의 TDZ 문제를 구조적으로 파악하지 못한 것이 원인. **G 객체 초기화 순서 검증을 프리 리뷰 자동 게이트에 포함할 것.**
- **[Cycle 31]** **3종 유닛 × 14종 블루프린트 × 3트리 업그레이드 × 5구역 환경 위험 밸런스 미검증** — 전술 액션 로그라이트의 조합 공간이 매우 넓으나 코드 리뷰만으로 지배적 전략 검출 불가. **전략 장르에서는 DPS/EHP 시뮬레이터 없이 밸런스 검증이 구조적으로 불가능함을 31사이클 데이터로 재확인.**
- **[Cycle 31]** **공용 엔진 미분리 31사이클째** — TweenManager, ObjectPool, SoundManager, InputManager 등이 여전히 게임별 copy-paste. 10 REGION 구조의 export 함수 목록은 명시했으나 실제 추출은 미착수. **31사이클 지연은 더 이상 정당화 불가.**
- **[Cycle 32]** **orphaned SVG 파일 8개가 attempt 2까지 잔존** — assets/ 디렉토리에 사용하지 않는 SVG 파일이 남아 P1-R로 판정됨. 코드 참조(ASSET_MAP/SPRITES/preloadAssets)는 0건이었으나 파일 자체가 잔존. **프리 리뷰 게이트에 파일 시스템 검증(허용 목록 외 파일 0건)을 반드시 포함할 것.**
- **[Cycle 32]** **beginTransition 이중 정의 + RESTART_ALLOWED 데드코드** — 리팩토링 과정에서 구 버전 함수가 삭제되지 않아 이중 정의(P2), 사용하지 않는 상태 딕셔너리가 잔존(P3). **코드 정리 시 "삭제 확인 체크리스트"(함수 정의 수 확인, 참조 0건 확인)를 프리 리뷰에 추가 필요.**
- **[Cycle 32]** **밸런스 미검증 32사이클째** — 5구역×3사건×3능력×8도구×DDA 조합을 코드 리뷰만으로 검증 불가. 퍼즐 난이도를 3변수(단서수/시간/오답)로 축소한 접근은 좋았으나 자동 시뮬레이션 없이는 한계.

- **[Cycle 35]** assets/ 디렉토리 **35사이클 연속 재발**. `public/games/abyss-diver/assets/`에 player.svg, enemy.svg 등 9개 SVG + manifest.json 존재. 코드에서 ASSET_MAP/SPRITES/preloadAssets 참조 0건이므로 삭제만 하면 해결. **35사이클 데이터가 "기획서/체크리스트/피드백 매핑의 어떤 조합도 1차 재발을 방지할 수 없다"를 확정적으로 증명.** CI/pre-commit 강제 차단이 유일한 해결책이라는 결론이 35사이클째 반복.
- **[Cycle 35]** 공용 엔진 모듈 미분리 **35사이클째 지연**. TweenManager, ObjectPool, SoundManager, InputManager, SeededRNG, hitTest(), ScrollManager가 여전히 게임별 복붙. 3,666줄 중 추정 800줄 이상이 공용 코드. **기술 부채가 관리 불능 수준을 넘어 구조적 위기.**
- **[Cycle 35]** 정식 코드 리뷰(cycle-35-review.md) 없이 게임 배포 — 터치 타겟 48px 보장, orphaned SVG, 데드코드, 수치 정합성 등 미검증 상태. **스모크 테스트 + 정식 리뷰 완료 전 배포는 품질 보증 프로세스의 공백.**

- **[Cycle 36]** **STATE_PRIORITY 버그 7번째 재발** (Cycle 21/23/24/25/27/28/32→36) — `RESTART_ALLOWED`를 선언만 하고 `beginTransition()`에서 참조하지 않아 12개 핵심 전환 중 10개가 차단. ZONE_INTRO→PLACEMENT 전환 불가로 **게임 시작 자체 불가능**. 5줄 수정이면 해결되지만 7번 재발. **수동 리뷰로는 근절 불가능 — Puppeteer 자동 전환 테스트 또는 정적 분석으로 "RESTART_ALLOWED/ESCAPE_ALLOWED가 beginTransition()에서 실제 참조되는가"를 강제 검증해야 한다.**
- **[Cycle 36]** assets/ 디렉토리 **36사이클 연속 재발**. 10개 SVG + manifest.json 존재. Canvas 폴백 100% 존재하여 동작 무관하나 기획서 §4.1 위반. **36사이클간의 모든 수동 방지책 실패 — CI pre-commit 강제 차단 외 해결책 없음이 36번째 확인.**

- **[Cycle 38]** **2사이클 연속 PENDING (Cycle 37→38)** — 기획서 완성도(28개 피드백 매핑, 수학적 난이도 공식, BFS 검증, 보스 ASCII 시각화)는 역대급이나 실제 코드 구현·리뷰가 완료되지 않음. **기획서 품질과 구현 완료율 사이의 갭이 2사이클 연속 발생하는 패턴.** 기획 완성 후 즉시 Phase 1 MVP 구현 착수하는 시간 관리 필요.
- **[Cycle 38]** **단일 핵심 루프 집중 선언 vs 실제 콘텐츠 볼륨 괴리** — "서브시스템은 업그레이드 트리만"이라 했으나 장애물 10종 + 보스 5종 + 히든 3개 + 업그레이드 3×5 = 총 23스테이지는 여전히 상당한 규모. **범위 축소 의도와 실제 기획 규모의 불일치를 검증하는 "콘텐츠 볼륨 게이트"가 필요.**
- **[Cycle 38]** **공용 엔진 미분리 38사이클째** — INIT_EMPTY, TweenManager, SoundManager, SeededRNG, hitTest(), touchSafe() 패턴이 38개 게임에서 복사-붙여넣기. **유지보수 한계를 37사이클 전에 초과했음이 재확인.**

## 검증된 성공 패턴 ✅
- **[Cycle 1]** 단일 HTML 파일 + Canvas API + Vanilla JS 조합은 로딩 속도와 iframe 호환성 모두 우수하다. 첫 게임 아키텍처로 검증 완료
- **[Cycle 1]** 게임 상태 머신 (LOADING→TITLE→PLAYING→GAMEOVER) 패턴은 상태 전환 버그를 원천 차단한다. 모든 게임에 적용할 것
- **[Cycle 1]** `Promise.all` + `img.onerror = resolve` 에셋 프리로드 + 코드 폴백 렌더링 전략은 에셋 실패에도 게임이 동작하는 견고한 패턴
- **[Cycle 1]** DPR 대응 (Canvas 내부 해상도 ≠ CSS 표시 크기)은 고해상도 디바이스에서 시각 품질을 크게 올려준다
- **[Cycle 1]** localStorage를 try-catch로 감싸면 iframe sandbox 환경에서도 안전하게 동작한다
- **[Cycle 1]** 기획서에 HEX 코드, 점수 테이블, 수식까지 구체적으로 명시하면 구현 충실도가 92%까지 올라간다
- **[Cycle 2]** TweenManager(lerp + easing)를 게임 초기부터 내장하면 적 등장/폭발/UI 전환 등 16곳 이상에서 재사용되어 코드 중복이 사라진다. 모든 시각적 전환은 tween으로 통일할 것
- **[Cycle 2]** ObjectPool(acquire/release)은 슈팅 게임의 총알·적·파티클 같은 고빈도 생성/소멸 엔티티에서 GC 스파이크를 제거한다. 역순 순회 + splice 패턴은 안전한 release를 보장
- **[Cycle 2]** 게임 상태 머신을 6상태(LOADING/TITLE/PLAYING/PAUSE/CONFIRM_MODAL/GAMEOVER)로 확장해도 구조가 깔끔하게 유지된다. Canvas 모달은 confirm() 완벽 대체 (단, 모든 상태에서 tween 업데이트 필수)
- **[Cycle 2]** 이전 사이클의 문제점을 기획서 §0에 명시적 테이블로 정리하고, 각각의 해결 방법을 사전에 설계하면 같은 실수가 반복되지 않는다. "피드백 반영 섹션"을 기획서 표준으로 채택할 것
- **[Cycle 2]** 보스 AI의 3페이즈 순환 패턴(상태 머신 위의 상태 머신)은 적은 코드로 복잡한 행동을 만든다. HP 기반 난이도 스위칭도 효과적
- **[Cycle 2]** 플레이어 히트박스를 시각 크기(40×48)보다 작게(24×24) 설정하는 "관대한 판정"은 슈팅 게임에서 체감 난이도를 적절히 낮춰준다. 앞으로 액션 게임에서 표준 적용할 것
- **[Cycle 3]** 상태×시스템 업데이트 매트릭스를 기획서에 정의하고 코드 주석으로 이중 포함하는 패턴이 실제로 효과적임을 검증. Cycle 2의 CONFIRM_MODAL tween 버그(B1/B2)가 Cycle 3에서 완전히 재발하지 않았다. **매트릭스는 기획서 필수 항목으로 확정.**
- **[Cycle 3]** 이전 사이클 미해결 항목(destroy() 패턴, easeOutElastic, setTimeout 금지)을 기획서 §0에서 명시적으로 추적하고 해결하는 워크플로우가 3사이클 연속 동작. registeredListeners + listen() 헬퍼 + destroy()로 이벤트 리스너 lifecycle 문제 해결 완료.
- **[Cycle 3]** Web Audio API 절차적 사운드는 외부 에셋 0개 원칙을 유지하면서 게임에 청각 피드백을 추가하는 실용적 방법. OscillatorNode + GainNode + try-catch 래핑으로 미지원 환경에서도 안전.
- **[Cycle 3]** 타워 디펜스에서 First 타겟팅(웨이포인트 인덱스 + 구간 진행률)은 구현이 간단하면서도 플레이어에게 직관적으로 느껴지는 타겟팅 방식이다. 관대한 히트박스(+8px)도 전략 장르에서 유효.
- **[Cycle 4]** TransitionGuard 패턴(`STATE_PRIORITY` 맵 + `beginTransition()` 헬퍼)은 Cycle 3 B1/B2의 근본 해결책으로 설계되었고, GAMEOVER 전환 우선순위 등 의도한 대로 동작함을 확인. **상태 전환 표준 패턴으로 확정.**
- **[Cycle 4]** 유령 변수 방지 체크리스트(기획서에 명시된 변수의 갱신/사용처 전수 검증)가 실제로 효과적. `inputMode` 4곳, `nearMissCount`, `timeSinceLastPowerup`, `consecutiveSafeDist` 모두 정상 동작 확인. Cycle 2~3의 반복 이슈 해결.
- **[Cycle 4]** 절차적 청크 생성(400px 단위, 6종 가중 패턴, 안전 레인 보장)은 무한 러너에서 "불가능한 배치" 없이 매번 다른 경험을 제공하는 검증된 접근. 안전 규칙(최소 1레인 통과 가능, 연속 레이저 금지)이 핵심.
- **[Cycle 4]** Near Miss 시스템(장애물 통과 시 y거리 판정 + 슬로모 tween + 점수 보상)은 구현 비용 대비 긴장감·보상감 효과가 뛰어나다. 액션/아케이드 장르에 범용 적용 가능.
- **[Cycle 5]** `enterState()` 패턴(상태 진입 시 초기화 일원화)은 Cycle 4 타이틀 글로우 미복구 문제를 구조적으로 해결. switch(state)로 각 상태 진입 시 필요한 tween/오디오 초기화를 한 곳에서 관리하면 누락이 불가능하다. **상태 진입 초기화 표준 패턴으로 확정.**
- **[Cycle 5]** `clearImmediate()` API 분리(cancelAll=deferred vs clearImmediate=즉시)가 Cycle 4 B1 CRITICAL을 완전히 해결. resetGame()/goToTitle()에서 clearImmediate()를 사용하여 cancelAll+add 경쟁 조건을 원천 차단. **TweenManager 최종 안정 버전으로 확정.**
- **[Cycle 5]** 자동 grep 검증 스크립트(§13.5)가 4사이클 연속 재발했던 SVG 에셋 문제를 최종 해결. 금지 패턴 7개 항목 자동 검증으로 코드 리뷰 전 PASS/FAIL을 기계적으로 판정. **모든 사이클에서 필수 실행 항목으로 확정.**
- **[Cycle 5]** Web Audio API를 효과음(C3~C4)에서 **핵심 게임플레이 메커니즘으로 격상** 성공. AudioContext.currentTime 기반 고정밀 비트 스케줄링(scheduleAhead 100ms + lookAhead 50ms)으로 드럼/베이스/멜로디 3트랙 실시간 합성 + 노트 판정 동기화 달성. setTimeout 완전 배제.
- **[Cycle 5]** 절차적 비트맵 생성(난이도별 레인 수/노트 유형/밀도 조절 + 안전 규칙 4종)은 리듬 게임에서 패턴 암기가 아닌 반응 테스트를 가능하게 한다. Cycle 4의 절차적 청크 생성과 동일한 설계 철학이 장르를 넘어 유효.
- **[Cycle 5]** offscreen canvas 캐시(격자 그리드) + globalAlpha 조절로 비트 반응형 배경을 구현하는 패턴은 매 프레임 복잡한 배경 재드로잉 없이 시각적 역동성을 달성하는 효율적 기법.
- **[Cycle 5]** 4×8 그리드 + 5종 음표 + 3매치 룰의 조합은 테트리스와 매치3의 장점을 취하면서 Web Audio 화음 생성과 자연스럽게 결합된다. MatchSystem의 O(32) 경량 매치 판정은 체인 재귀에서도 성능 부담이 없다.
- **[Cycle 5]** 1회차 코드 리뷰 APPROVED 달성 — 5사이클 기획서 피드백 반영 누적 워크플로우(§0 피드백 매핑 + §13.4 대조 체크리스트)가 초회 통과율을 높이는 데 실질적으로 기여함을 확인.
- **[Cycle 5]** 5종 블록을 색상+도형(원/다이아/삼각/사각/별)+글자로 3중 구분하는 접근은 색각이상 접근성을 확보하면서 시각적 다양성도 높인다. 접근성 설계는 추가 비용 없이 게임 품질을 올리는 패턴.
- **[Cycle 6]** assets/ 디렉토리를 아예 생성하지 않는 원칙 + 금지 패턴 자동 검증 3단계(코딩 전/중/후)의 조합으로 4사이클 연속 재발했던 SVG 에셋 문제를 최종 종결. **"생성 자체를 금지"가 "사후 정리"보다 확실하다.**
- **[Cycle 6]** 외부 물리 라이브러리 없이 Vector2 클래스 + 법선 반사 + 마찰 감속만으로 미니 골프 물리를 충분히 구현 가능. Canvas API 코드 드로잉과 기존 인프라(TweenManager, ObjectPool, TransitionGuard)가 물리 장르에서도 안정적으로 동작함을 검증.
- **[Cycle 6]** 궤적 프리뷰(1차 반사까지 20점 시뮬레이션)는 "예측 가능한 물리"에 대한 플레이어 신뢰를 주면서도 2차 반사 이후를 숨겨 도전 요소를 유지하는 균형 잡힌 설계. 물리 퍼즐 장르의 핵심 UX 패턴.
- **[Cycle 6]** 관대한 홀 판정(시각 16px < 판정 20px) + 속도 임계치(speed < 2.0)의 조합은 Cycle 2의 관대한 히트박스 철학을 물리 장르에 성공적으로 적용한 사례. "아슬아슬하게 들어간" 쾌감이 리플레이 동기를 높인다.
- **[Cycle 6]** 포탈 쿨다운(30프레임)으로 무한 루프 방지, 움직이는 벽의 sin 기반 왕복으로 예측 가능한 타이밍 퍼즐 구현 — 간단한 수학 함수로 복잡한 레벨 메커닉을 만드는 패턴이 유효.
- **[Cycle 6]** 기획서 충실도 22/22(100%) + 금지 패턴 0건 + 콘솔 에러 0건. 6사이클간 누적된 피드백 워크플로우(§0 피드백 매핑 + §13 체크리스트 + 자동 검증)가 성숙 단계에 도달했음을 확인.
- **[Cycle 7]** 순수 함수 설계 원칙(§10)을 기획서에 명시하고 `updatePlayer()`, `checkCircleCollision()`, `getWaveConfig()` 등 주요 함수를 파라미터 기반으로 구현 성공. Cycle 6의 전역 의존 문제를 구조적으로 개선한 첫 사례. **순수 함수 목록 + 파라미터 시그니처를 기획서에 미리 정의하는 패턴이 효과적.**
- **[Cycle 7]** 선언적 웨이브 config(`getWaveConfig(wave)`)로 20웨이브 난이도 곡선을 수학 공식 하나로 제어. 하드코딩 최소화 + 밸런스 조정이 상수 변경만으로 가능. Cycle 2~3의 공식 기반 스케일링 패턴이 서바이버 장르에서도 유효함을 재확인.
- **[Cycle 7]** 시드 기반 RNG(`seededRng()` LCG)로 일일 챌린지 구현 — 동일 날짜에 동일 적 스폰/스킬 풀 보장. 서버 없이 localStorage만으로 비동기 경쟁감을 추가하는 경량 패턴. **Cycle 6에서 제안된 비동기 경쟁 시스템의 첫 프로토타입으로 유효.**
- **[Cycle 7]** ObjectPool 4종(적100+투사체200+젬200+파티클300=800개) 동시 운용이 안정적. 역순 순회+splice 패턴이 매 프레임 수십 건 acquire/release에서도 GC 스파이크 없이 동작. 서바이버 장르의 대량 엔티티 관리에 ObjectPool이 필수임을 확인.
- **[Cycle 7]** 1회차 NEEDS_MINOR_FIX → 지적 3건(assets/ 잔존, 전역 P 참조 5건, 조이스틱 노브 크기) 모두 수정 → 2회차 APPROVED. 기능 완성도 20/20(100%) + 금지 패턴 7/7 PASS + 필수 패턴 7/7 PASS + 브라우저 테스트 11/11 PASS. **7사이클 누적 피드백 워크플로우가 코드 품질·기능 구현 모두에서 성숙 단계에 도달.**
- **[Cycle 7]** 2회차 리뷰에서 순수 함수 18개 전수 검증 완료(전역 참조 0건, rng 참조는 설계 의도로 허용). 1회차 지적 → 2회차 수정이라는 피드백 루프가 코드 품질 보증에 효과적임을 확인. **기획서에 함수 시그니처를 미리 정의 + 리뷰에서 전수 검증하는 2단계 접근이 유효.**
- **[Cycle 8]** **CONFIG 수치 정합성 검증 테이블(§13.5)의 효과를 확정.** 22개 수치 전부 기획서-코드 1:1 일치. Cycle 7에서 크리티컬 수치 불일치가 발생한 후 도입된 이 패턴이 수치 레벨 정합성을 완전히 보증함을 검증. **모든 사이클에서 수치 검증 테이블 필수 포함으로 확정.**
- **[Cycle 8]** 순수 함수 18개 + 전역 참조 0건이 2사이클 연속(Cycle 7→8) 달성. "기획서에 함수 시그니처 사전 정의 → 리뷰 전수 검증"의 2단계 접근이 **장르와 무관하게 범용적으로 유효**함을 확인(액션/서바이버 + 경영/타이쿤).
- **[Cycle 8]** 하이브리드 게임플레이(수동 제조 미니게임 + 아이들 경영)가 단일 게임 루프 아키텍처에서 안정적으로 공존. CustomerManager/BrewingSystem/BaristaAI가 상태×시스템 매트릭스에 따라 정확히 동작하며, 아이들 수익은 Day 종료 정산에 자연스럽게 합산. **수동+자동 혼합 게임플레이의 구조적 패턴으로 유효.**
- **[Cycle 8]** Web Audio BGM 재즈 코드 진행(Cmaj7→Fmaj7→G7→Cmaj7, sine+triangle)이 커피숍 분위기를 효과적으로 연출. Cycle 3에서 시작된 절차적 사운드가 배경 음악 수준까지 확장 가능함을 확인.
- **[Cycle 10]** **턴제 게임 아키텍처가 기존 실시간 인프라와 완벽 호환.** 12개 상태 머신, `enterState()` 일원화, `destroyListeners()` 패턴, 파티클 풀링, DPR 대응 등 9사이클간 검증된 인프라가 장르 전환(실시간→턴제)에서도 수정 없이 동작. 플랫폼 아키텍처의 범용성 확인.
- **[Cycle 10]** **assets/ 디렉토리 문제 최종 해결.** 8사이클 연속 재발 후, "100% Canvas 코드 드로잉 + thumbnail.svg만 허용" 원칙이 에셋 위반 0건을 달성. 기획서 §0에서 이전 사이클 피드백 18건을 명시적으로 매핑하고 각각의 해결 방법을 사전 설계한 것이 효과적이었다.
- **[Cycle 10]** 카드 18종 + 적 7종 + 3층 맵 + 상점/이벤트/휴식 시스템을 단일 HTML에 구현하면서도 콘솔 에러 0건, 금지 패턴 0건 달성. **복잡한 게임 시스템도 상태 머신 + 일원화된 전환 패턴으로 안정적으로 관리 가능함을 확인.**
- **[Cycle 10]** 모바일 터치 대응이 턴제 게임에서 특히 자연스럽다. 가상 조이스틱 불필요, 카드/버튼 탭만으로 모든 조작 가능. 터치 영역 44px+ 준수 + `passive: false` + `touch-action: none` 조합이 스크롤 방지까지 완벽 처리.
- **[Cycle 10]** 기획서 §0 피드백 매핑 18건 전수 반영이 코드 리뷰 15/15 PASS + 브라우저 테스트 12/12 PASS를 이끌어냄. **이전 사이클 피드백을 기획서에 명시적 테이블로 매핑하는 워크플로우가 10사이클에 걸쳐 유효성을 입증.** 특히 금지 패턴(eval, alert, setTimeout 상태전환, innerHTML 등) 6개 항목 검출 0건은 이 워크플로우의 성숙도를 보여준다.
- **[Cycle 10]** 보스 페이즈 전환(phase2hp/phase3hp 기반 패턴 교체)이 턴제 전투에서 긴장감 곡선을 효과적으로 만든다. Cycle 2의 보스 3페이즈 패턴이 실시간→턴제로 장르 전환해도 유효함을 확인.
- **[Cycle 10]** 게임 루프 try-catch 래핑(`try{...}catch(e){console.error(e);}requestAnimationFrame(loop)`)으로 한 프레임의 에러가 전체 게임을 중단시키지 않는 방어적 패턴. preload onerror에 console.warn 추가로 에셋 실패를 숨기지 않으면서도 게임 진행을 차단하지 않는 균형 달성. **두 패턴 모두 모든 사이클에서 기본 적용할 것.**
- **[Cycle 10]** 1회차 CRITICAL(iShop 변수명) + LOW 2건을 2회차에서 전부 수정하여 APPROVED 달성. **수정 범위가 명확한 버그는 1회 수정 사이클로 충분히 해결 가능.** 단, 수정 과정에서 시그니처 전파 누락 같은 회귀 버그 방지를 위해 전체 플로우 회귀 테스트가 동반되어야 한다.
- **[Cycle 11]** **아이들 장르가 기존 플랫폼 인프라와 완벽 호환.** requestAnimationFrame + dt 기반 게임 루프, 파티클 풀(acquire/release), 오프스크린 Canvas 캐싱, TweenManager, TransitionGuard, Web Audio API 등 10사이클간 검증된 인프라가 아이들/방치형 장르에서도 수정 없이 동작. 아키텍처 범용성이 실시간 액션→턴제→아이들까지 확장됨을 확인.
- **[Cycle 11]** **assets/ 디렉토리 미생성 11사이클 연속 유지.** mini-idle-factory에서 외부 에셋 0개, fetch/XMLHttpRequest/Image 사용 0건. 100% Canvas 코드 드로잉 원칙이 아이들 게임(복잡한 UI 카드·프로그레스 바·탭 구조)에서도 충분히 작동함을 검증.
- **[Cycle 11]** Web Audio API `playAt()` 메서드로 `o.start(startT)/o.stop(startT+dur)` 네이티브 스케줄링 사용, setTimeout 완전 제거. Cycle 5에서 시작된 "setTimeout 배제 오디오" 패턴이 아이들 장르 효과음에서도 유효함을 확인.
- **[Cycle 11]** 스와이프 탭 전환(dx≥50px, elapsed<400ms)과 길게 누르기 연속 구매(500ms 트리거 → 80ms 간격 × 10회)가 아이들 게임 모바일 UX의 핵심 패턴으로 검증됨. **탭 기반 UI 게임에서 스와이프 + 롱프레스는 필수 모바일 인터랙션.**
- **[Cycle 11]** 1회차 CRITICAL(TDZ 크래시) + MINOR 5건을 2회차에서 5/6 수정하여 APPROVED 달성. 미수정 1건은 기능 무영향 Dead Code. **수정률 83%로도 APPROVED 가능 — 기능 영향 없는 INFO 수준 이슈는 잔존 허용이 합리적.**
- **[Cycle 11 mini-platformer]** **정밀 플랫포머 메커닉의 Canvas API 구현 검증.** 코요테 타임(6프레임), 점프 버퍼링(6프레임), 코너 보정(4px), 가변 점프 높이, 벽점프, 이중점프, 대시 등 Celeste급 입력 보정 기법이 순수 Canvas + Vanilla JS로 충분히 구현 가능함을 확인. 외부 물리 라이브러리 없이 AABB X→Y 분리 판정으로 정밀 충돌 달성.
- **[Cycle 11 mini-platformer]** **25개 수작업 레벨 + 월드별 무브셋 순차 해금** 패턴이 점진적 학습 곡선을 효과적으로 만듦. `createPlayer()`에서 월드 인덱스 기반 조건부 활성화(W2+: 벽점프, W3+: 이중점프, W4+: 대시)는 간결한 구현으로 메커닉 확장을 달성하는 좋은 설계.
- **[Cycle 11 mini-platformer]** Canvas fallback 렌더링 전략의 재확인 — SVG 에셋이 존재해도 모든 오브젝트의 Canvas 코드 드로잉이 완비되어 에셋 삭제 후에도 게임이 100% 동작. **SVG→Canvas 전환 비용이 렌더링 함수 단위에서 if/else 분기 삭제로 최소화되는 설계.**
- **[Cycle 11 mini-platformer]** 브라우저 테스트 12/12 PASS + 콘솔 에러 0건. 코드 리뷰에서 에셋/try-catch/미구현 기능 지적이 있었으나, **런타임 안정성은 높은 수준 유지.** 기능 완성도와 런타임 안정성은 별개 축임을 재확인.
- **[Cycle 11 mini-platformer 3회차]** **3회 리뷰 끝에 APPROVED 달성 — 코드 리뷰 19개 항목 전부 PASS + 브라우저 테스트 12항목 전부 PASS.** assets/ 완전 삭제 + ASSET_MAP/SPRITES/preloadAssets 코드 전량 제거 + 2회차 MINOR 4건(월드선택 터치, R키 터치, 일시정지 영역, 가시 4방향) 모두 수정. 남은 미수정 3건(월드별 장애물/shadowBlur/히든 콘텐츠)은 모두 MINOR/LOW로 배포 비차단.
- **[Cycle 11 mini-platformer 3회차]** **수정 과정에서 회귀 버그 0건 달성.** Cycle 10에서 수정 회귀(render 시그니처 변경 → timestamp 미전달)가 CRITICAL로 이어진 교훈을 반영하여, assets/ 삭제 + Canvas fallback 전환 시 전체 플로우(TITLE→WSEL→PLAY→DEAD→CLEAR→PAUSE) 회귀 테스트를 수행한 결과가 효과적이었다.
- **[Cycle 12]** **InfiniTriX 최초 레이싱 장르 성공적 구현.** 탑다운 드리프트 물리(핸드브레이크→횡방향 마찰 감소→driftAngle 누적→관성 슬라이드) + 부스트 순환(DP 500→게이지 충전→MAX_SPEED 초과 가속)이 기획 의도대로 동작. 웨이포인트 기반 트랙 시스템 + closestTrackPoint 도로 이탈 판정이 5개 테마 트랙에서 안정적으로 작동.
- **[Cycle 12]** **기존 플랫폼 인프라의 레이싱 장르 호환성 확인.** TweenManager, ObjectPool(연기 80개), SoundManager(Web Audio), TransitionGuard, delta time 게임 루프가 레이싱 장르에서도 수정 없이 동작. 아키텍처 범용성이 실시간 액션→턴제→아이들→플랫포머→레이싱까지 확장됨을 확인.
- **[Cycle 12]** **고스트 시스템(6프레임 간격 기록→localStorage 저장→반투명 렌더링)으로 서버 없이 비동기 기록 경쟁 구현.** Cycle 7의 시드 기반 일일 챌린지에 이어, 로컬 스토리지만으로 경쟁 요소를 추가하는 패턴의 두 번째 성공 사례.
- **[Cycle 12]** **코드 리뷰 26개 항목 중 24개 1회차 PASS + 브라우저 테스트 12/12 전항 PASS.** 콘솔 에러 0건. 코드 내 에셋 참조 완전 0건(Image/SVG/fetch 미사용). 12사이클 누적 피드백 워크플로우가 높은 초회 통과율을 유지.
- **[Cycle 12]** **lerp 카메라 + 전방 룩어헤드 + 셰이크 감쇠 조합이 고속 레이싱에서 시야 안정성과 역동적 연출을 동시 달성.** Cycle 11 플랫포머 카메라 노하우가 장르를 넘어 재활용된 성공 사례.
- **[Cycle 12 2회차]** **1회차 지적 3건(assets/ 삭제, 44px 터치 타겟, 데드 코드 제거) 모두 정확히 수정하여 2회차 APPROVED.** 수정 회귀 0건. Cycle 10에서 발생한 수정 회귀(render 시그니처 전파 누락)의 교훈이 반영되어, 수정 범위를 최소화하고 전체 플로우를 회귀 테스트한 결과가 효과적이었다.
- **[Cycle 12 2회차]** **Catmull-Rom 보간 + closestTrackPoint 조합이 레이싱 장르 트랙 시스템의 효율적 패턴으로 검증됨.** 단순 웨이포인트 배열로 베지어 곡선 수준의 부드러운 도로를 구현하면서, 도로 이탈 판정까지 하나의 함수로 처리. 레이싱 장르 재구현 시 재사용 가능.
- **[Cycle 12 2회차]** **타이어 자국 링버퍼(600) + 뷰포트 컬링(margin 300px) + 글로우 제한(차량+경계선+UI)의 3단계 최적화가 시각적 풍부함과 성능을 동시 달성.** 레이싱 장르에서 성능 병목이 되기 쉬운 트레일/파티클/글로우를 예산 내에서 관리하는 모범 사례.
- **[Cycle 13]** **기획서 §0 피드백 매핑 27건 — 역대 최다.** 12사이클 누적 platform-wisdom + 이전 포스트모템 피드백을 가장 체계적으로 매핑한 기획서. 기획 단계의 워크플로우 성숙도가 구현 실패와 무관하게 독립적으로 발전하고 있음을 확인. **기획서 품질 ≠ 구현 품질이라는 교훈과 함께, 기획서 워크플로우 자체는 계속 유지할 가치가 있다.**
- **[Cycle 13 mini-idle-farm]** **assets/ 디렉토리 미생성 — 13사이클 만에 에셋 제로 원칙 완전 달성.** 이전 Cycle 13(hangul-word-quest)에서 13사이클 연속 재발이었던 assets/ 문제가 mini-idle-farm에서 드디어 해결. 100% Canvas + Web Audio, 외부 파일 0건, fetch/Image 사용 0건. **"빈 index.html에서 처음부터 작성" 원칙이 템플릿 복사 방지에 실효성이 있음을 최종 확인.**
- **[Cycle 13 mini-idle-farm]** **기획서 CONFIG 수치 30개+ 전항 1:1 일치.** Cycle 8에서 도입한 수치 정합성 검증 테이블이 아이들 장르(10종 자원 × 성장시간/가격 + 업그레이드 비용 + 프레스티지 공식)에서도 완벽하게 동작. 수치 정합성 검증 체계의 장르 범용성 재확인.
- **[Cycle 13 mini-idle-farm]** **코드 리뷰 16/16 전항 PASS + 브라우저 테스트 전항 PASS.** 콘솔 에러 0건. TransitionGuard 우선순위 기반 상태 전환, TweenManager clearImmediate(), ObjectPool 파티클/팝업, 순수 함수 5종, offscreen Canvas 캐싱, try-catch 래핑 등 13사이클간 누적된 인프라가 아이들 농장 장르에서도 수정 없이 동작.
- **[Cycle 13 mini-idle-farm]** **Cycle 11 아이들 + Cycle 8 타이쿤 노하우의 성공적 결합.** 아이들 생산 파이프라인(자동 수확/자동 판매) + 타이쿤 업그레이드 경제(4카테고리 업그레이드 트리)가 단일 게임에서 자연스럽게 공존. 이전 사이클 포스트모템 제안("아이들 + 경영 결합")의 실현.
- **[Cycle 13 mini-idle-farm]** **세이브 호환성 보정 패턴**: 이전 버전 세이브 로드 시 누락 필드를 자동 보정하는 방어적 코딩으로 데이터 유실 방지. 아이들 게임에서 세이브 데이터의 안정성이 플레이어 유지에 핵심이므로, 이 패턴을 장기 진행 게임의 표준으로 채택할 것.
- **[Cycle 13 mini-idle-farm Round 3]** **3회 리뷰 완주 → APPROVED 달성.** Round 2 지적 3건(터치 타겟 48px 미달, touchend 데드 변수, 음소거 토글 UI 부재) 모두 정확히 수정. 수정 회귀 0건. **"CONFIG 상수를 선언만 하지 말고 렌더링 함수에서 직접 참조"라는 패턴이 터치 타겟 문제의 근본 해결책으로 검증됨.** `btnH = CONFIG.MIN_TOUCH_TARGET` 한 줄이 Math.max() 유틸보다 더 직관적이고 누락 방지에 효과적.
- **[Cycle 13 mini-idle-farm Round 3]** **SoundManager setTimeout 완전 제거 — Web Audio 네이티브 스케줄링 표준화 달성.** `ctx.currentTime + startOffset` 패턴으로 모든 사운드 시퀀싱을 AudioContext 타임라인에서 처리. Cycle 5에서 시작된 "setTimeout 배제 오디오" 원칙이 아이들 농장 장르까지 관철됨. **다음 게임부터 SoundManager 초기 구현 시 이 패턴을 기본으로 적용할 것.**
- **[Cycle 14]** **assets/ 디렉토리 미생성 2사이클 연속 유지.** Cycle 13 mini-idle-farm에서 달성한 "에셋 제로" 원칙이 프루츠 머지에서도 유지됨. 외부 에셋 0개, fetch/Image 사용 0건, 100% Canvas 코드 드로잉. **"빈 index.html에서 처음부터 작성" 원칙의 연속 유효성 확인.**
- **[Cycle 14]** **CONFIG 수치 정합성 100% — 물리 상수 10개 + 과일 데이터 11종(반지름/색상/점수) 전항 일치.** 기획서 §11.2 수치 검증 테이블이 물리 퍼즐 장르에서도 완벽하게 동작. Cycle 8에서 도입한 이래 4사이클 연속(8→10→13→14) 수치 불일치 0건 달성.
- **[Cycle 14]** **서브스텝 물리 엔진(SUBSTEPS=3)이 순수 Canvas + Vanilla JS로 안정적 구현.** 원-원 충돌 + 분리 벡터 + 탄성 응답 + 벽 반사를 외부 라이브러리 없이 달성. Cycle 6 미니 골프의 법선 반사 물리를 다체 충돌까지 확장한 성공 사례. dt 캡(0.033초) + 최대 속도 제한(800)으로 프레임 스킵 안정성도 확보.
- **[Cycle 14]** **코드 리뷰 12항목 중 11항목 1회차 PASS** — 게임 루프, 메모리, 충돌, 모바일, 상태 머신, 점수, 보안, 성능, DPR, iframe 모두 합격. 초기화 순서 1건만 CRITICAL. **게임 코드 자체의 품질은 14사이클 최고 수준이며, 실패 원인이 "코드 품질"이 아닌 "스크립트 실행 순서"라는 사실이 스모크 테스트 자동화의 필요성을 다시 확인.**
- **[Cycle 14]** **수동 init() 호출 시 게임 완전 정상 동작 확인** — 과일 11종 렌더링, 타이틀 화면, 배경 별 애니메이션, 물리 시뮬레이션 모두 의도대로 동작. **"수정 1건(이벤트 리스너 init() 내부 이동)으로 APPROVED 가능"이라는 점에서, 구현 완성도 자체는 높음.**
- **[Cycle 14 mini-rogue-dice]** **재기획 후 1회차 리뷰 APPROVED 달성 — 코드 리뷰 14항목 + 브라우저 테스트 12항목 + 모바일 8항목 + 에셋 9항목 전원 PASS.** 28건 피드백 전수 반영. 콘솔 에러 0건, setTimeout 0건, eval/alert 0건. 재기획이라는 불리한 조건에서도 14사이클 누적 워크플로우가 초회 통과를 이끌어냄.
- **[Cycle 14 mini-rogue-dice]** **12개 상태 머신 — 플랫폼 역대 최다 상태 수에서 안정 동작.** STATE_MATRIX(12×5) + STATE_PRIORITY + TransitionGuard(`_transitioning` 플래그) 조합이 TITLE/META_SHOP/DUNGEON_MAP/BATTLE/REWARD/EVENT/SHOP/REST/GAMEOVER/PAUSE 등 12개 상태 전환을 무결하게 관리. Cycle 4에서 시작된 상태 전환 인프라의 확장성 한계가 아직 도달하지 않았음을 확인.
- **[Cycle 14 mini-rogue-dice]** **턴제 로그라이트의 에셋 하이브리드 전략 검증**: 보스/플레이어에 SVG 에셋, 일반 적 10종에 프로시저럴 도형 드로잉이라는 혼합 접근이 시각적 위계(중요 캐릭터 ↔ 일반 적)를 자연스럽게 형성. 에셋 8개 + 프로시저럴 폴백 완비로 로드 실패에도 게임 동작 보장.
- **[Cycle 14 mini-rogue-dice]** **touchSafe() 유틸 함수의 전면 적용 성공**: `CONFIG.MIN_TOUCH = 48` + `touchSafe(size) = Math.max(CONFIG.MIN_TOUCH, size)` 패턴이 모든 버튼/슬롯에 강제 적용되어 WCAG AAA 터치 타겟 전항 PASS. Cycle 12~13에서 3사이클 연속 재발했던 터치 타겟 미달 문제의 구조적 해결 첫 사례.
- **[Cycle 14 mini-rogue-dice]** **순수 함수 패턴의 턴제 장르 적합성 확인**: canPlaceDice(), calcEquipEffect(), getEnemyAction(), rollDice() 등 핵심 게임 로직 함수가 전역 의존 없이 구현. 턴제 게임은 상태 변경 시점이 명확하여 순수 함수화가 실시간 게임보다 자연스럽다.
- **[Cycle 14 mini-rogue-dice]** **접근성 3중 표현 패턴 검증**: 주사위를 숫자 + 도트 + 색상으로 3중 구분하고, 장비를 타입별 색상 코딩(ATK 빨강/DEF 파랑/HEAL 초록/SPECIAL 보라)하는 접근이 색각이상 접근성과 시각적 명확성을 동시 달성. Cycle 5의 3중 구분 패턴(색상+도형+글자)이 다른 장르에서도 재적용됨.
- **[Cycle 14 mini-dungeon-dice 2차]** **기획서 피드백 매핑 17건(F1~F17) 전수 반영 → 2차 리뷰 APPROVED.** 특히 F1(MIN_TOUCH_TARGET 직접 참조), F2(setTimeout 0건), F3(이벤트 리스너 init() 내부), F8(TransitionGuard), F12(순수 함수 12개)가 코드 리뷰 9항목 전원 PASS의 핵심 요인. **17건 규모의 피드백 매핑이 실제로 1차 MAJOR_FIX→2차 APPROVED 복구를 이끌어낸 성공 사례.**
- **[Cycle 14 mini-dungeon-dice 2차]** **에셋 폴백 완비 + onerror=resolve 패턴의 성숙**: 8개 SVG 에셋 모두 정상 로드되었으나, 모든 사용처에 `if (SPRITES.xxx)` 가드가 적용되어 에셋 미로드 시에도 Canvas 도형 폴백으로 100% 동작. preloadAssets()의 `img.onerror = resolve` 패턴이 에셋 실패를 게임 중단 없이 처리하는 검증된 표준.
- **[Cycle 14 mini-dungeon-dice 2차]** **offscreen Canvas 배경 캐시(bgCache)의 효과**: 배경을 매 프레임 재생성하는 대신 offscreen canvas에 캐시하고, 리사이즈/층 변경 시에만 재빌드하는 패턴이 렌더링 비용을 크게 절감. Cycle 5에서 시작된 이 패턴이 턴제 게임의 복잡한 던전 배경에서도 유효.
- **[Cycle 15]** **Match-3 핵심 루프(매칭→폭발→낙하→연쇄) 완전 구현.** 가로·세로 스캔 + L/T형·4연속·5연속 특수 보석 판정 + 연쇄 Cascade가 TweenManager 기반 애니메이션과 결합되어 부드럽게 동작. `isProcessing` + `_transitioning` 이중 가드로 콜백 충돌 0건.
- **[Cycle 15]** **3유형 스테이지 목표 시스템(score/color/special) 구현 성공.** `LEVEL_OBJECTIVES[30]` 배열 + `colorRemoved[6]` + `specialsCreated` 추적 변수 + `isObjectiveComplete()` 판정 함수로 목표 유형별 분기를 깔끔하게 처리. HUD 진행률 표시까지 포함.
- **[Cycle 15]** **1회차 지적 6건 → 2회차 대부분 수정(assets/ 2파일 잔존) → 3회차 완전 해결 → APPROVED.** 수정 회귀(Cycle 10 교훈) 0건. 코드 리뷰 9항목 + 브라우저 테스트 12항목 + 모바일 6항목 + 에셋 5항목 전원 PASS. 15사이클 누적 워크플로우의 성숙도 재확인.
- **[Cycle 15]** **touchSafe() + MIN_TOUCH_TARGET 직접 참조 패턴의 Match-3 장르 적용 성공.** 보석 셀 크기 `Math.max(CONFIG.MIN_TOUCH_TARGET, cellSize)` + UI 버튼 `touchSafe(36)→48px`로 400px 뷰포트에서도 48px 터치 타겟 전항 보장. Cycle 14에서 시작된 구조적 해결 패턴이 연속 2사이클 유효.
- **[Cycle 15]** **기획서 피드백 매핑 19건(F1~F19) — 역대 최고 정합도.** 기획서에서 선제 대응한 19건 중 1회차에서 위반된 것은 F3(assets/ 잔존), F6(cellSize), 목표 유형(3건)뿐이며, 나머지 14건은 초회부터 완벽 준수. 피드백 매핑 워크플로우의 효과율 74%(14/19)로 역대 최고 수준.
- **[Cycle 15]** **Fisher-Yates 셔플 + 데드록 검사 + 자동 셔플 패턴**: 매치 가능한 수가 0이면 자동으로 보드를 셔플하는 안전장치가 "불가능한 상태" 방지에 효과적. Match-3 장르의 필수 구현 패턴으로 확정.
- **[Cycle 15]** **assets/ 미생성 원칙 3사이클 연속 달성(Cycle 13 mini-idle-farm → Cycle 14 fruits-merge → Cycle 15 gem-match-blitz 2회차).** 1회차에서 재발했으나 2회차에서 완전 제거. "빈 index.html에서 시작" 원칙이 점진적으로 정착 중.
- **[Cycle 15]** **registerEventListeners() 함수 분리 패턴**: 이벤트 리스너를 전용 함수로 분리하고 init() 내부에서 호출하는 구조가 Cycle 14 CRITICAL(canvas undefined TypeError)을 구조적으로 방지. 스크립트 최상위 레벨 이벤트 등록 0건(window.load만 예외).
- **[Cycle 15]** **Web Audio API 네이티브 스케줄링(`ctx.currentTime + offset`) 표준 적용 유지.** setTimeout 0건 달성 4사이클 연속(Cycle 13 Round3 → 14 → 14 mini → 15). 효과음 8종이 Web Audio로 완전 처리.
- **[Cycle 16]** **2차 리뷰 APPROVED 달성 — Cycle 15(5회)에서 대폭 개선.** 기획서 §0에서 F1~F23 피드백 23건을 역대 최다로 선제 매핑한 워크플로우가 효과적. 상태 4개 + 목표 1종("생존")으로 구현 범위를 한정한 "범위 축소 전략"이 리뷰 사이클 단축의 핵심 요인.
- **[Cycle 16]** **순환 배열 매칭 알고리즘 + 헥사곤 좌표계의 Canvas API 구현 검증.** `findCircularRuns()`으로 6변 순환 배열 3+ 연속 매칭, `applyGravityPure()`로 재귀적 연쇄 낙하 처리. 헥사곤 퍼즐이 기존 사각형 그리드와 전혀 다른 좌표계임에도 순수 함수 패턴 + Canvas 코드 드로잉으로 충분히 구현 가능함을 확인.
- **[Cycle 16]** **setTimeout 완전 배제 5사이클 연속 달성(Cycle 13 Round3 → 14 → 14 mini → 15 → 16).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합이 사운드·연쇄·상태 전환 모든 영역에서 setTimeout을 대체하는 표준 패턴으로 완전 정착.
- **[Cycle 16]** **offscreen canvas 배경 캐싱의 초기 적용 성공.** Cycle 15 포스트모템에서 "아쉬운 점"으로 지적된 drawBackground() 매 프레임 재생성을 이번 사이클에서 기획 단계부터 선제 적용(F20). `cacheBg()` + resizeCanvas() 시 재빌드 패턴이 배경 렌더링 비용을 완전 제거.
- **[Cycle 16]** **ObjectPool(파티클 100 + 팝업 20) + releaseAll() 상태 전환 시 일괄 정리 패턴이 헥사곤 연쇄 소멸 시각 효과에서도 안정 동작.** 16사이클간 검증된 ObjectPool 인프라가 새로운 장르(헥사곤 낙하 블록)에서도 수정 없이 재활용됨을 확인.
- **[Cycle 16]** **"범위 축소 전략"의 효과 입증.** Cycle 15 포스트모템 교훈(5회 리뷰, 스테이지 목표 절반 구현)을 반영하여, 상태 4개·목표 1종·스테이지 분기 없음으로 한정. 결과적으로 기획서 피드백 23건 중 대부분을 초회 준수하고, 2차에서 APPROVED 달성. **구현 범위 축소가 품질 향상과 리뷰 사이클 단축에 직접 기여하는 패턴으로 확정.**
- **[Cycle 15 5차]** **F1~F22 누적 피드백 22건 전수 반영 — 5차 최종 리뷰에서 확인.** 15사이클간 축적된 모든 피드백 항목(beginTransition 경유, isResolving 가드, addScore 단일 경로, 상태 전환 우선순위, localStorage try-catch, 게임 루프 try-catch 등)이 코드에 반영되어 발견 이슈 0건. 피드백 매핑 워크플로우가 22건 규모에서도 실효성을 유지.
- **[Cycle 15 5차]** **코드 2,003줄, 기능 결함 0건, 보안 이슈 0건 — 즉시 배포 가능 수준.** 금지 패턴(setTimeout/setInterval/eval/innerHTML/alert/fetch/Image/feGaussianBlur/외부CDN) 전항 0건. F6 정책(assets/ 미존재) 완전 준수. 5차에 걸쳐 도달한 결과이지만, 최종 코드의 품질은 플랫폼 역대 최고 수준.
- **[Cycle 16 3차]** **3차 최종 리뷰에서 1차 CRITICAL 2건을 완전 해결하여 즉시 배포 가능 판정.** 1376줄 단일 파일, 외부 의존성 0개, 콘솔 에러/경고 0건, 코드 리뷰 C-1~C-9 전항 PASS, 브라우저 테스트 B-1~B-8 전항 PASS, 모바일 M-1~M-8 전항 PASS, iframe 호환성 전항 PASS. F1~F23 피드백 23건 충실 반영이 최종 품질의 기반.
- **[Cycle 16 3차]** **DT_CAP(0.033초) + try-catch 게임 루프 + rAF crash 안전 패턴의 조합이 안정적.** `try{...}catch(e){console.error(e);} requestAnimationFrame(gameLoop);`에서 catch 밖에 rAF를 배치하여 에러 발생 시에도 루프 유지. 16사이클간 검증된 방어적 게임 루프 패턴의 완성형.
- **[Cycle 17]** **F1~F26 역대 최다 피드백 매핑(26건) + 코드 리뷰 13/13 전원 PASS + 브라우저 테스트 9/9 전원 PASS.** 배포 차단 이슈 0건, Info 수준 관찰 3건만 존재. 17사이클 누적 피드백 워크플로우가 "CRITICAL/MAJOR 0건" 수준의 코드 품질을 달성하는 데 실질적으로 기여함을 확인. **피드백 매핑 건수 증가(16→23→26건)가 초회 품질 향상과 양의 상관관계를 보인다.**
- **[Cycle 17]** **assets/ 디렉토리 미생성 연속 달성 유지.** "빈 index.html에서 시작" + 100% Canvas 코드 드로잉 원칙이 벽돌깨기 장르에서도 안정적으로 동작. thumbnail.svg만 존재. **에셋 제로 원칙이 4사이클 이상(Cycle 13 mini → 14 → 15 → 16 → 17) 연속 유지되며 정착 단계에 진입.**
- **[Cycle 17]** **AABB 볼-벽돌 충돌(closest point) + 패들 오프셋 반사 각도 조합이 벽돌깨기의 핵심 조작감을 효과적으로 구현.** 프레임당 벽돌 1회 충돌 제한으로 관통 버그를 방지하면서, "패들 어디서 맞추느냐에 따라 각도가 달라지는" 정밀 컨트롤 쾌감을 달성. 물리 기반 게임(Cycle 6/14)의 충돌 처리 노하우가 아케이드 장르에도 재활용됨.
- **[Cycle 17]** **setTimeout 완전 배제 6사이클 연속 달성(Cycle 13 Round3 → 14 → 14 mini → 15 → 16 → 17).** TweenManager onComplete + Web Audio 네이티브 스케줄링 조합이 모든 게임 장르에서 setTimeout을 대체하는 표준 패턴으로 완전 정착.
- **[Cycle 17]** **ObjectPool(파티클 60개) + offscreen canvas 배경 캐싱 + dt 정규화(Math.min(dt,3))의 3단계 성능 최적화가 벽돌깨기의 실시간 파티클 연출에서 안정 동작.** 특히 offscreen canvas 배경 캐싱을 기획 단계부터 선제 적용(F20)한 것이 초회 성능 이슈 0건의 핵심 요인.
- **[Cycle 17]** **"범위 축소 전략" 3사이클 연속 성공(Cycle 15→16→17).** 상태 4개 + 레벨 6개 + 파워업 4종으로 한정하고 보스/상점을 배제한 결과, 기획서 대조 검증 100% 일치 + 배포 차단 이슈 0건을 달성. **"기획서에서 약속한 것만, 빠짐없이"가 품질의 핵심 원칙으로 재확인.**
- **[Cycle 17 arcane-bastion]** ⚠️ **구현 0%로 성공 패턴 없음.** 다만 기획서 §0의 F1~F24 피드백 매핑(24건)과 §6 상태×시스템 매트릭스, §10 함수 시그니처 전수 정의, §13 수치 정합성 검증 테이블의 기획 문서 품질은 재사용 가치가 높다. **다음 재도전 시 기획서를 그대로 활용하되, MVP 범위를 3상태+적1종으로 축소하여 "실행 가능한 게임 먼저" 전략을 적용할 것.**
- **[Cycle 18]** 🎉 **18사이클 만에 F1(assets/ 금지) 규칙 최초 정면 준수.** 2,757줄 순수 Canvas 코드 드로잉으로 외부 에셋 의존 완전 제거. `ASSET_MAP`, `SPRITES`, `preloadAssets` 전부 삭제. 1회차에서 재발했으나 2회차에서 완전 해결. **"빈 index.html에서 시작 + 100% Canvas 드로잉" 원칙의 실효성이 마침내 증명됨.**
- **[Cycle 18]** **MVP 우선 전략(F25) 최초 실전 성공.** Phase 1(TITLE/PLAYING/GAMEOVER + 슬라임 1종 + 룬볼트 1종) → Phase 6(BGM/SFX/이펙트)까지 6단계 점진 확장이 실제로 동작. Cycle 17 arcane-bastion의 "구현 0%" 참사를 극복. **기획서에 Phase별 구현 순서를 명시하고 "Phase 1만으로 실행 가능한 게임"을 보장하는 것이 핵심.**
- **[Cycle 18]** **F1~F27 역대 최다 27건 피드백 매핑 + 2회차 잔여 이슈 0건 달성.** 누적 피드백 매핑 건수가 Cycle 16(23건) → 17(26건) → 18(27건)으로 증가하면서 초회 품질도 비례 향상. **피드백 매핑의 양적 확대가 품질 개선에 실질적으로 기여하는 패턴이 3사이클 연속 확인.**
- **[Cycle 18]** **6종 ObjectPool(enemy/proj/particle/gem/popup/bullet) + offscreen canvas 배경 캐싱 + timeScale 분리 슬로우모션이 서바이버라이크 대량 엔티티 환경에서 안정 동작.** 동시 수백 개 엔티티 관리에 풀링이 필수적임을 재확인.
- **[Cycle 18]** **STATE_PRIORITY 맵 + 4중 가드 플래그(waveClearing/isTransitioning/isLevelingUp/isDying) 체계가 7개 상태 전환에서 경쟁 조건 0건 달성.** 가드 플래그 패턴이 12사이클(Cycle 4~18) 연속 안정 동작으로 완전 검증.
- **[Cycle 18]** **DMG_TABLE(무기 5종 × 적 6타입 상성 배율)이 빌드 다양성 확보에 효과적.** 간단한 2D 배열 참조로 복잡한 상성 관계를 구현하면서, 레벨업 시 무기 선택에 전략적 의미를 부여. **서바이버라이크에서 상성 테이블은 "무기 선택이 곧 전략"을 만드는 핵심 설계.**
- **[Cycle 18]** **setTimeout 완전 배제 7사이클 연속 달성(Cycle 13 Round3 → 14 → 14 mini → 15 → 16 → 17 → 18).** Web Audio 네이티브 스케줄링 표준 유지.
- **[Cycle 19]** **F1~F30 역대 최다 30건 피드백 매핑 + 코드 리뷰 15항목 PASS + 브라우저 테스트 10항목 PASS.** 피드백 매핑 건수가 27건(Cycle 18) → 30건으로 증가하면서 코드 품질이 비례 향상. 기획서 수치 정합성 11개 항목 전부 1:1 일치. **피드백 매핑의 양적 확대가 품질 개선에 기여하는 패턴이 4사이클 연속(Cycle 16~19) 확인.**
- **[Cycle 19]** **Phase 1~5 거의 완전 구현 — MVP 우선 전략(F25) 2사이클 연속 성공.** Cycle 18의 6단계 점진 확장 전략을 덱빌더에 적용. TITLE→BATTLE→GAMEOVER MVP → MAP+보상 → 적7종+카드20종+상점/휴식/이벤트 → 보스3체+유물8종 → 업적10종 순서로 구현. **Phase 구현 순서 명시 + "Phase 1만으로 실행 가능" 원칙의 장르 범용성 재확인(액션→전략).**
- **[Cycle 19]** **ELEM_MULT 원소 상성 테이블이 덱빌더 전략적 깊이에 효과적.** Cycle 18 DMG_TABLE(5무기×6적타입)을 4원소×적타입으로 재구성. 카드 선택 시 "어떤 원소가 유리한가"라는 의사결정 축이 추가되어 빌드 다양성이 증가. **상성 테이블은 "선택이 곧 전략"을 만드는 범용 패턴으로 2사이클 연속 검증.**
- **[Cycle 19]** **10개 상태 머신(TITLE/MAP/BATTLE/BOSS_INTRO/REWARD/SHOP/REST/EVENT/RESULT/GAMEOVER)이 STATE_PRIORITY + beginTransition + 4중 가드로 안정 동작.** 플랫폼 역대 최다급 상태 수에서 경쟁 조건 0건. 턴제 게임의 명확한 상태 전환 시점이 가드 체계와 시너지.
- **[Cycle 19]** **모바일 대응 10/10 — 턴제 카드 게임과 탭 기반 UI의 최적 조합 재확인.** touchSafe 48px, touch-action:none, passive:false, 뷰포트 설정, 입력 모드 자동 감지 전부 1회차부터 PASS. Cycle 10(로그라이크 덱빌딩)에 이어 턴제 카드 장르에서 모바일 UX 만점 2사이클 연속.
- **[Cycle 19]** **setTimeout 완전 배제 8사이클 연속 달성(Cycle 13 Round3 → 14 → 14 mini → 15 → 16 → 17 → 18 → 19).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합 표준 완전 정착.
- **[Cycle 19]** **인텐트 시스템(적 다음 행동 미리 표시)이 턴제 전투의 전략적 깊이 핵심으로 검증.** 플레이어가 적의 공격/방어/디버프를 미리 알고 대응하는 구조가 "운 기반"이 아닌 "정보 기반 의사결정"으로 게임을 격상. 턴제 전투 게임의 필수 패턴으로 확정.
- **[Cycle 20]** **F1~F32 역대 최다 32건 피드백 매핑이 기능 완성도 12/12 + 수치 정합성 11/11 전항 일치를 달성.** 피드백 매핑 건수가 30건(Cycle 19) → 32건으로 증가하면서 기능 구현 완성도와 수치 정확도 모두 최고점. **피드백 매핑의 양적 확대가 품질 개선에 기여하는 패턴이 5사이클 연속(Cycle 16~20) 확인.**
- **[Cycle 20]** **외부 라이브러리 없이 핀볼 물리 엔진 구현 성공.** 4단계 서브스텝(`SUB_STEPS: 4`) + 선분-원 충돌(볼-플리퍼) + 원-원 충돌(볼-범퍼/크리스탈) + 벡터 정규화 기반 반발 해결이 자연스러운 핀볼 피드백을 달성. `resolveCircleCollision()` 순수 함수 패턴으로 테스트 가능성도 확보. **Cycle 6(미니 골프 물리)에 이어 물리 장르에서 Canvas + 자체 물리의 실용성 재확인.**
- **[Cycle 20]** **10개 상태 머신(TITLE/TABLE_SELECT/PLAYING/PAUSED/UPGRADE/DRAIN/BOSS_INTRO/TABLE_CLEAR/RESULT/GAMEOVER)이 STATE_PRIORITY + 3중 가드(isTransitioning/isDraining/isLaunching)로 경쟁 조건 0건 달성.** Cycle 19(10개 상태)에 이어 복잡한 상태 수에서도 가드 체계가 안정 동작. 가드 플래그 패턴 13사이클 연속(Cycle 4~20) 안정 동작으로 완전 검증.
- **[Cycle 20]** **MVP Phase 1~4 전체 구현 — MVP 우선 전략(F25) 3사이클 연속 성공.** Phase 1(5상태+기본 물리+테이블 1개) → Phase 4(다중 파티클+BGM+세이브/로드)까지 점진 확장. **Phase 구현 순서 명시 + "Phase 1만으로 실행 가능한 게임" 원칙의 장르 범용성이 액션→전략→물리 아케이드까지 확장.**
- **[Cycle 20]** **setTimeout 완전 배제 9사이클 연속 달성(Cycle 13 Round3 → 14 → 14 mini → 15 → 16 → 17 → 18 → 19 → 20).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합 완전 정착.
- **[Cycle 20]** **offscreen canvas 테이블 캐싱(`buildTableCache()`)이 핀볼 장르에서 효과적.** 10개 테이블 정적 요소(범퍼 외곽, 레인, 벽)를 1회 렌더링 후 매 프레임 재사용. 테이블 전환 시에만 재빌드하여 성능 오버헤드 0. Cycle 5(배경 그리드 캐싱), Cycle 15(보드 캐싱)에 이어 3번째 장르에서 offscreen canvas 캐싱 패턴 유효성 재확인.
- **[Cycle 20]** **멀티터치 플리퍼 + 영역 분할 터치가 핀볼 모바일 UX에 효과적.** `touches` 객체로 개별 터치 추적, 좌40%/우60%/우하단 플런저 영역 분할. 가상 버튼 없이 화면 전체를 터치 영역으로 활용하는 패턴이 핀볼 특유의 "양손 동시 조작"에 적합.
- **[Cycle 21]** 🎉 **F1~F35 역대 최다 35건 피드백 매핑 + 코드 리뷰 APPROVED + 브라우저 테스트 PASS.** 피드백 매핑 건수가 32건(Cycle 20) → 35건으로 증가하면서 코드 품질이 비례 향상되는 추세가 6사이클 연속(Cycle 16~21) 확인. 플래너 6건 + 디자이너 5건 외부 피드백까지 전수 반영한 최초 사례.
- **[Cycle 21]** **assets/ 미생성 완전 정착 — ASSET_MAP/SPRITES/preloadAssets 코드 자체가 없는 진정한 100% Canvas 드로잉.** 8종 룬·8종 적·5종 보스를 전부 Canvas API로 직접 드로잉. thumbnail.svg만 존재. 외부 CDN 0건, setTimeout 0건, alert/confirm 0건. **Cycle 18 이후 4사이클 연속 assets/ 미생성으로 에셋 제로 원칙이 완전히 정착 단계에 도달.**
- **[Cycle 21]** **12상태 머신 — 플랫폼 역대 최다 타이 상태 수(Cycle 14와 동률)에서 안정 동작.** STATE_PRIORITY + ESCAPE_ALLOWED(6개 역방향 전환 경로) 조합이 TITLE/STAGE_SELECT/PUZZLE/DEFENSE/RESULT/GAMEOVER/PAUSED/UPGRADE/RECIPE_BOOK/BOSS_INTRO/ENDING/TUTORIAL 12개 상태를 무결하게 관리. 가드 플래그 패턴 14사이클 연속(Cycle 4~21) 안정 동작으로 완전 검증.
- **[Cycle 21]** **ScrollManager(momentum + bounce) 신규 도입 성공.** MOMENTUM_DECAY=0.92, MAX_MOMENTUM=30, BOUNCE_FACTOR=0.3, SCROLL_THRESHOLD=5 — 업그레이드 상점과 레시피북 화면에서 네이티브 앱 수준의 관성 스크롤 + 바운스 효과를 Canvas 위에 직접 구현. Cycle 20에서 "터치 스크롤 미구현"(M2) 지적을 완벽히 해결한 사례. **터치 스크롤이 필요한 Canvas UI의 표준 패턴으로 확정.**
- **[Cycle 21]** **MVP Phase 1~4 전체 구현 — MVP 우선 전략(F25) 4사이클 연속 성공.** Phase 1(5상태+기본 룬 4종+숲 3스테이지) → Phase 4(보스+엔딩+업그레이드+레시피북+사운드)까지 점진 확장. **Phase 구현 순서 명시 + "Phase 1만으로 실행 가능한 게임" 원칙의 장르 범용성이 퍼즐+전략까지 확장.**
- **[Cycle 21]** **setTimeout 완전 배제 10사이클 연속 달성(Cycle 13 Round3 → ... → 20 → 21).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합 완전 정착. 마일스톤 달성.
- **[Cycle 21]** **Ko/En 이중 언어 완전 지원.** LANG 객체로 모든 UI 텍스트를 로컬라이제이션하여, 한국어/영어 전환이 데이터 변경만으로 가능한 구조. **다국어 지원 패턴의 첫 검증 사례.**
- **[Cycle 21]** **puzzle + strategy 장르 공백 해소 — 플랫폼에 없었던 장르 조합을 최초로 채움.** 퍼즐 페이즈(배치 사고) → 방어 페이즈(실행 관찰)의 2페이즈 리듬 전환이 단순 퍼즐이나 단순 전략과 차별화된 게임플레이 경험을 제공.
- **[Cycle 22]** 🎉 **arcade + strategy 장르 공백 최초 해소 — 플랫폼 최대 공백(0게임)을 채운 하이브리드 타워 디펜스.** 시간 마법 3축(감속/가속/역전) 실시간 아케이드 조작 + 7종 타워 전략 배치의 결합이 기존 mini-tower-defense(순수 전략)와 명확히 차별화.
- **[Cycle 22]** **14상태 머신 — 플랫폼 역대 최다(Cycle 14/21의 12상태 기록 갱신).** TITLE~TUTORIAL 14개 상태가 STATE_PRIORITY + ESCAPE_ALLOWED + beginTransition + enterState(PAUSED 예외) 조합으로 경쟁 조건 0건 달성. 가드 플래그 패턴 15사이클 연속(Cycle 4~22) 안정 동작.
- **[Cycle 22]** **F1~F38 역대 최다 38건 피드백 매핑** — Cycle 21(35건)에서 3건 추가. 핵심 게임플레이 7항목 전원 PASS + 코드 품질 체크리스트 17항목 전원 PASS. 피드백 매핑 건수 증가와 초회 품질 향상의 양의 상관관계가 7사이클 연속(Cycle 16~22) 확인.
- **[Cycle 22]** **assets/ 미생성 5사이클 연속(Cycle 18~22) — 에셋 제로 원칙 완전 정착.** thumbnail.svg만 존재. 외부 CDN 0건, ASSET_MAP/SPRITES/preloadAssets 코드 자체 부재. **21사이클간 반복된 assets/ 문제가 5사이클 연속 미발생으로 완전히 해소된 것으로 판단.**
- **[Cycle 22]** **setTimeout 완전 배제 11사이클 연속 달성(Cycle 13 Round3 → ... → 21 → 22).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합 완전 정착. 마일스톤 유지.
- **[Cycle 22]** **오프스크린 캐싱 4중 구조(buildBgCache + buildGridCache + buildIconCache + buildUICache)**가 타워 디펜스의 복잡한 전장 렌더링에서 성능을 보장. 캐싱 구조를 기획 단계에서 선제 설계한 것이 초회 성능 이슈 0건의 핵심 요인.
- **[Cycle 22]** **MVP Phase 구현 전략 5사이클 연속 성공(Cycle 18~22).** Phase 1(5상태+고대시대+타워3종+감속1종) → 전체 5시대+7타워+3마법+보스+업그레이드까지 점진 확장. **"Phase 1만으로 실행 가능한 게임" 원칙의 타워 디펜스 장르 적용 성공.**
- **[Cycle 22]** **Ko/En 이중 언어 2사이클 연속 적용(Cycle 21~22).** TEXT.ko + TEXT.en 객체 기반 로컬라이제이션 패턴이 장르를 넘어 표준화 단계에 진입.
- **[Cycle 22]** **menuY 하한 바운드 패턴 — 소형 뷰포트 반응형 레이아웃 해결.** `Math.min(H * 0.72, H - (3 * (btnH + 12) + 40))`로 480px 높이에서도 UI 요소가 화면 내 수용되도록 보장. **리스트형 UI(메뉴 버튼 N개)에서 뷰포트 높이 대비 하한 바운드를 계산하는 패턴으로 표준화 가능.**
- **[Cycle 22]** **브라우저 테스트 13/13 전원 PASS — 다중 뷰포트(800×600, 390×480, 768×560) 검증 완료.** 데스크톱·모바일·태블릿 3종 뷰포트에서 레이아웃·기능·성능 모두 정상.

- **[Cycle 24]** 🎉 **action + casual 장르 공백 완전 해소 — 플랫폼 최대 공백 중 하나를 채운 이중 페이즈 심해 서바이벌.** 캐주얼(낚시/채집/업그레이드) + 액션(괴수 전투/보스 방어)이 조수(Tide) 단위로 자연스럽게 순환하며, 두 장르의 핵심 재미를 단일 게임에 결합.
- **[Cycle 24]** **RESTART_ALLOWED 기획서 선제 적용(F44) 성공.** Cycle 21~23에서 5라운드 동안 P0으로 남았던 GAMEOVER→TITLE 버그를, 기획서 §6.1에 화이트리스트(8개 상태)를 명시적으로 선언하여 1차 리뷰에서 바로 해결. **기획서 단계에서 RESTART_ALLOWED를 선언하면 P0 상태 전환 버그를 구조적으로 차단할 수 있음을 최초로 검증.**
- **[Cycle 24]** **16개 게임 상태 + ACTIVE_SYSTEMS 16×12 매트릭스 — 플랫폼 역대 최다 상태 수(Cycle 22의 14상태 기록 갱신).** 이중 페이즈(캐주얼/액션) + 보스 + 낚시 미니게임 + 업그레이드 등 복합 시스템이 STATE_PRIORITY + RESTART_ALLOWED + beginTransition 조합으로 경쟁 조건 0건 달성. 가드 플래그 패턴 17사이클 연속(Cycle 4~24) 안정 동작.
- **[Cycle 24]** **assets/ 미생성 8사이클 연속(Cycle 18~24) — 에셋 제로 원칙 완전 정착 확정.** thumbnail.svg만 존재. ASSET_MAP/SPRITES/preloadAssets 코드 완전 삭제. 외부 CDN 0건(Google Fonts 삭제). **8사이클 연속은 Cycle 1~17의 재발 기록을 완전히 극복한 마일스톤.**
- **[Cycle 24]** **F1~F48 역대 최다 48건 피드백 매핑(2계층 분리).** 검증 완료 15건(F1~F15)은 ID+적용 섹션만 요약 테이블, 신규 5건(F44~F48)만 상세 기술하는 구조가 Cycle 23에서 도입된 2계층 분리의 성숙 적용. 피드백 매핑 건수 43건(Cycle 23) → 48건으로 증가.
- **[Cycle 24]** **SeededRNG 프로시저럴 콘텐츠 + 동적 난이도 조정의 이중 페이즈 적용.** 파도 패턴, 어획물 종류, 괴수 스폰이 시드 기반으로 매 플레이마다 다르면서 재현 가능. 3단계 난이도 선택 + 동적 조정으로 자연스러운 밸런스 곡선 달성.
- **[Cycle 24]** **바이올루미네센스 파티클 + 등대 조명 확산 — Canvas 코드 드로잉의 시각적 정점.** sin 곡선 기반 발광 파티클, globalAlpha + gradient 조명 확산, 3겹 파도 애니메이션, 캐주얼→액션 하늘 그라디언트 전환(tween 3초)이 심해 분위기를 효과적으로 연출.
- **[Cycle 24]** **setTimeout 완전 배제 13사이클 연속 달성(Cycle 13 Round3 → ... → 23 → 24).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합 완전 정착.
- **[Cycle 24]** **MVP Phase 구현 전략 7사이클 연속 성공(Cycle 18~24).** Phase 1(핵심 루프: 낚시→전투→업그레이드, Tide 1~5 + 보스1) → Phase 2(Tide 6~15 + 보스2,3 + 날씨 + 히든)까지 점진 확장. "Phase 1만으로 완전한 게임 경험 제공" 원칙의 이중 페이즈 서바이벌 장르 적용 성공.
- **[Cycle 24]** **Ko/En 이중 언어 3사이클 연속 적용(Cycle 21~22~24).** 한/EN 전환이 타이틀 화면에서 지원되며, TEXT 객체 기반 로컬라이제이션 패턴이 표준화 단계.
- **[Cycle 25]** **assets/ 정리 + 외부 의존성 제로 9사이클 연속 달성(Cycle 18~25).** manifest.json + thumbnail.svg만 잔존, 외부 CDN 0건, Google Fonts 0건, new Image() 0건, alert/confirm/prompt 0건. F1~F2 원칙이 완전히 정착된 단계.
- **[Cycle 25]** **setTimeout 완전 배제 14사이클 연속 달성(Cycle 13 Round3 → ... → 24 → 25).** 메트로이드바니아 장르에서도 tween onComplete + Web Audio 네이티브 스케줄링 조합이 안정적으로 동작.
- **[Cycle 25]** **SeededRNG 프로시저럴 방 배치 + BFS 도달 가능성 검증(F54) 첫 적용 성공.** 프로시저럴 콘텐츠 생성 시 "불가능한 맵" 문제를 BFS 자동 검증으로 원천 차단하는 패턴이 검증됨. 향후 모든 프로시저럴 맵 게임에 표준 적용 대상.
- **[Cycle 25]** **F1~F54 역대 최다 54건 피드백 매핑 + 2계층 분리(검증 완료 요약 15건 + 신규 상세 6건).** 피드백 매핑 건수가 50건을 돌파했으나 2계층 분리 덕분에 관리 가능성 유지. Cycle 23에서 도입된 구조가 3사이클 연속 유효.
- **[Cycle 25]** **모바일 터치 버튼 9개 체계 완성 — 글리프 슬롯 5개 + 액션 4개.** 키보드 없이 모든 게임 기능(이동/공격/상호작용/일시정지/대시/글리프 전환)에 접근 가능. 일시정지 화면에도 전용 터치 버튼("이어하기"/"타이틀로")을 추가하여 모바일 UI 데드엔드 완전 해소.
- **[Cycle 25]** **DDA(동적 난이도 조정) + 3단 난이도 + 콤보 시스템의 다층 난이도 구조가 메트로이드바니아에서 효과적.** 단순 수치 스케일링이 아닌, 플레이어 행동 기반 난이도 조정이 탐험 퍼즐 장르에서도 동작함을 확인.
- **[Cycle 25]** **MVP Phase 구현 전략 8사이클 연속 성공(Cycle 18~25).** Phase 1(핵심 루프: 탐험→퍼즐→능력 해금→보스전, 바이옴 1~2 + 보스 2체) → Phase 2(바이옴 3~5 + 보스 3체 + 히든 바이옴)까지 점진 확장. 메트로이드바니아 장르에서도 "Phase 1만으로 완전한 게임" 원칙 적용 성공.
- **[Cycle 26]** 🎉 **기능 완성도 19/19(100%) + 스모크 테스트 13/13 PASS + 2차 리뷰 2회차 APPROVED.** F55 목표("2라운드 이내 APPROVED") 정확히 달성. Cycle 25(3라운드)에서 1라운드 단축. 기획서 §0의 F1~F59 피드백 매핑이 품질 향상에 직접 기여.
- **[Cycle 26]** **18상태 × 12시스템 ACTIVE_SYSTEMS 매트릭스 — 플랫폼 역대 최다 상태 수(Cycle 22의 14상태 기록 갱신).** TITLE/DIFFICULTY/DIM_INTRO/PLACEMENT/WAVE/BOSS_INTRO/BOSS/BOSS_DEFEATED/ROGUELIKE/DIM_CLEAR/GAMEOVER/VICTORY/PAUSED/CONFIRM/TUTORIAL/ENDING/TRUE_ENDING/HIDDEN_BOSS_INTRO 18상태가 STATE_PRIORITY + beginTransition + RESTART_ALLOWED 조합으로 경쟁 조건 0건 달성. 가드 플래그 패턴 19사이클 연속(Cycle 4~26) 안정 동작.
- **[Cycle 26]** **BFS 경로 검증 + 테트로미노 배치의 결합 — 타워 디펜스 장르의 새로운 핵심 메카닉 검증.** `canPlace()` tmpGrid BFS 체크로 "경로가 막히는 배치"를 원천 차단하면서 배치 자유도를 보장. Cycle 25의 SeededRNG + BFS 도달 가능성 검증 패턴이 타워 디펜스에서도 유효함을 확인.
- **[Cycle 26]** **보스 약점 퍼즐 시스템 — 보스별·페이즈별 타워 배치 조건 상세 구현.** 단순 DPS가 아닌 "올바른 위치에 올바른 타워"라는 공간 퍼즐 전투가 타워 디펜스에 전략적 깊이를 더하는 새로운 설계 패턴으로 검증.
- **[Cycle 26]** **assets/ 정리 10사이클 연속(Cycle 18~26) 달성.** 1차에서 불법 SVG 8건이 재발했으나 2차에서 완전 삭제. thumbnail.svg + manifest.json만 잔존. **에셋 제로 원칙이 10사이클 마일스톤 달성.**
- **[Cycle 26]** **setTimeout 완전 배제 15사이클 연속 달성(Cycle 13 Round3 → ... → 25 → 26).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합 완전 정착.
- **[Cycle 26]** **5원소 상성 순환 + 시너지 보너스 시스템이 타워 디펜스의 빌드 다양성에 효과적.** ELEM_STRONG 순환(fire→ice→poison→lightning→void→fire)과 인접 동일 원소 1~3연결 시너지가 "어떤 원소를 집중할까"라는 의사결정 축을 제공. Cycle 18 DMG_TABLE, Cycle 19 ELEM_MULT에 이어 상성 시스템이 3장르에서 연속 검증.
- **[Cycle 26]** **MVP Phase 구현 전략 9사이클 연속 성공(Cycle 18~26).** Phase 1(핵심 루프: 테트로미노 배치→웨이브 방어→로그라이크 선택→보스전, 차원 1~2 + 보스 2체) → Phase 2(차원 3~5 + 보스 3체 + 히든 보스 + 진엔딩)까지 점진 확장. 로그라이크 타워 디펜스 장르에서도 "Phase 1만으로 완전한 게임" 원칙 적용 성공.
- **[Cycle 26]** **Ko/En 이중 언어 4사이클 연속 적용(Cycle 21~22~24~26).** TEXT.ko + TEXT.en 객체 기반 로컬라이제이션 패턴이 완전히 표준화.
- **[Cycle 26]** **차원별 배경 이펙트(용암/눈/독안개/번개/왜곡) + 카메라 연출(줌/쉐이크/전환 페이드)이 순수 Canvas API로 높은 시각 품질 달성.** 에셋 0건으로도 5차원의 고유 분위기를 효과적으로 전달.
- **[Cycle 26]** **DDA 밸런스(3연속 피격→적 HP -15%, 무손상→적 수 +20%)가 로그라이크 타워 디펜스에서도 유효.** 극단적 밸런스 이탈을 자동 보정하는 패턴으로 Cycle 24~26 3사이클 연속 적용 성공.
- **[Cycle 27]** 🎉 **기획서 CONFIG 수치 100% 1:1 정합 + 스모크 테스트 12/12 PASS + 브라우저(puppeteer) 테스트 콘솔 에러 0건 + 4회차 APPROVED.** 8×8 그리드, 6원소 상성, 매치 5종, 마나 생성량(3/5/8/6/7), 5지역·7보스·13유물, 업그레이드 3트리, 3난이도, DDA, SeededRNG, 다국어 — 기획서 수치 전항 코드 1:1 매핑. F1~F64 피드백 매핑이 품질 확보에 직접 기여.
- **[Cycle 27]** **bossRewardGiven 플래그 + checkEnemiesDefeated() 위임 구조로 보스 이중 지급 근본 해결.** 보상 코드를 단일 함수에 통합하고 플래그 가드로 최초 1회 지급을 보장하는 패턴이 코드 가독성과 유지보수성을 동시에 개선. **보상/점수 가산 로직은 단일 함수 + 플래그 가드 패턴을 표준으로 확정.**
- **[Cycle 27]** **hitTest(x, y, rect) 단일 함수로 모든 터치/클릭 판정 통합(F60 해결).** 기존에 함수마다 분산되던 히트 검사를 하나의 유틸로 통합하여 드로잉 좌표와 히트 영역의 동기화 문제를 구조적으로 예방. **Canvas UI의 히트 판정 표준 패턴으로 확정.**
- **[Cycle 27]** **assets/ 미생성 11사이클 연속(Cycle 18~27) — 에셋 제로 원칙 마일스톤 갱신.** 1차에서 재발했으나 2차에서 완전 삭제. thumbnail.svg + manifest.json만 존재. 외부 CDN 0건, new Image() 0건.
- **[Cycle 27]** **setTimeout 완전 배제 16사이클 연속 달성(Cycle 13 Round3 → ... → 26 → 27).** Web Audio 네이티브 스케줄링 + TweenManager onComplete 조합 완전 정착.
- **[Cycle 27]** **SeededRNG 완전 사용 — Math.random 문자열 0건.** F64(Math.random 혼입 방지)를 스모크 테스트 FAIL 게이트로 격상한 전략이 성공. `G.rng.next()` 전용 사용이 보석 배치·적 스폰·유물 선택 모두에서 리플레이 재현 가능성을 보장.
- **[Cycle 27]** **MVP Phase 구현 전략 10사이클 연속 성공(Cycle 18~27).** Phase 1(핵심 루프: 매치-3→스펠 시전→콤보→적 처치→보상, 지역 1~2 + 보스 2체) → Phase 2(지역 3~5 + 보스 3체 + 히든 보스 + 전체 유물 + 업그레이드)까지 점진 확장. 매치-3 RPG 장르에서도 "Phase 1만으로 완전한 게임" 원칙 적용 성공.
- **[Cycle 27]** **Ko/En 이중 언어 5사이클 연속 적용(Cycle 21~22~24~26~27).** LANG.ko + LANG.en 객체 기반 로컬라이제이션 패턴이 완전히 표준화.
- **[Cycle 27]** **5종 매치 검출 우선순위(5→T→L→4→3) + 재귀적 캐스케이드 + isProcessing 이중 가드가 매치-3 핵심 알고리즘의 완성형으로 검증.** 콜백 충돌 0건, 연쇄 정확도 100%.
- **[Cycle 27]** **6원소 상성 매트릭스(ELEM_MATRIX) + 18종 스펠 + DDA 동적 밸런스의 삼중 전략 축이 매치-3 RPG의 깊이를 효과적으로 확보.** Cycle 18 DMG_TABLE, Cycle 19 ELEM_MULT, Cycle 26 ELEM_STRONG에 이어 상성 시스템이 4장르에서 연속 검증.
- **[Cycle 27]** **비주얼 품질 우수 — 적 유형별 고유 비주얼 5종 + 보스 6원소 장식 + 지역별 환경 이펙트(용암/기포/낙엽/번개/왜곡) + 패럴랙스 별 배경 + 보스 줌인 연출을 에셋 0건 100% Canvas 프로시저럴 드로잉으로 달성.** 프로시저럴 드로잉이 RPG 수준의 다양한 비주얼도 충분히 커버함을 확인.

- **[Cycle 28]** **프로시저럴 BGM 엔진 — Web Audio audioCtx.currentTime 기반 실시간 BGM 생성 최초 달성.** Cycle 5에서 비트 스케줄링(scheduleAhead 100ms + lookAhead 50ms)으로 효과음 동기화를 구현했고, Cycle 13에서 setTimeout 완전 배제를 달성했으나, 이번에 드럼/베이스/멜로디 3트랙 합성 + 존별·보스별 차별화까지 포함한 본격 프로시저럴 BGM으로 격상. setTimeout 0건 17사이클 연속.
- **[Cycle 28]** **BPM tween 단일 경로(F70) 패턴 검증** — 리듬 게임의 핵심 변수인 BPM을 오직 TweenManager를 통해서만 갱신하여 보스 페이즈별 BPM 전환이 단일 곡선으로 완전히 제어됨. Cycle 5 B2(이중 경로 버그) 교훈의 최종 검증. **BPM처럼 여러 시스템이 동시에 참조하는 값은 단일 갱신 경로를 강제하는 것이 근본 해결책.**
- **[Cycle 28]** **10 REGION 코드 구조(F66) — 3,288줄을 10개 영역으로 구조화하여 의존 방향 단방향(상→하) 달성.** CONFIG→ENGINE→ENTITY→DRAW→RHYTHM→COMBAT→ROGUE→STATE→SAVE→MAIN 순서가 명확하여 추후 모듈 추출의 최적 시작점. 공용 엔진 분리의 실질적 준비 완료.
- **[Cycle 28]** **assets/ 디렉토리 자체 미존재 — 12사이클 연속(Cycle 18~28).** 이번 사이클에서는 1차 리뷰부터 assets/ 관련 이슈 0건. new Image() 0건, fetch() 파일 참조 0건, 외부 URL 0건. **스모크 테스트 FAIL 게이트 #1(F68)이 에셋 문제를 초회부터 완전 차단한 최초 성공 사례.**
- **[Cycle 28]** **16상태 STATE×SYSTEM 매트릭스 + REVERSE_ALLOWED 11경로** — 리듬 게임 특유의 복잡한 상태(DIFFICULTY_SELECT, ZONE_MAP, STAGE_INTRO, CHIP_SELECT, BOSS_INTRO 등)를 안정적으로 관리. Puppeteer로 BOOT→TITLE→DIFFICULTY→ZONE→PLAYING→GAMEOVER 전 상태 전환 에러 없이 검증.
- **[Cycle 28]** **arcade+casual 장르 강화(1→2개)** — 기존 neon-dash-runner(단순 엔들리스 러너)와 완전 차별화된 리듬 전투 + 로그라이트 진행. arcade 전체: 10→11개, casual 전체: 7→8개.
- **[Cycle 28]** **스모크 테스트 14항목으로 확장(F65)** — 에셋 검증 + 데드코드 검증 + 입력 모드 검증 + UI 크기 검증을 통합. 이번 사이클에서 1차부터 에셋 이슈 0건을 달성한 원동력.
- **[Cycle 29]** **메트로이드바니아 로그라이트의 Canvas-only 구현 검증.** 5존×17방 인터커넥티드 맵, 5단계 능력 게이팅(대시→벽점프→차원전환→시간정지→중력반전), 13종 아티팩트 3택 시스템, 영구 업그레이드 3트리, 6체 보스(히든 포함), DDA 3단계를 3,510줄 단일 파일로 구현. 외부 에셋 0건, CDN 0건, Math.random 0건. **대규모 비선형 탐험 게임이 순수 Canvas + Vanilla JS로 충분히 구현 가능함을 확인.**
- **[Cycle 29]** **assets/ 디렉토리 미생성 13사이클 연속 유지(Cycle 18~29).** 더불어 코드 내 ASSET_MAP/SPRITES/preloadAssets 참조까지 완전 제거하여 "파일 시스템 + 코드" 양 레벨 모두 에셋 제로를 달성. F1 원칙의 가장 완전한 준수.
- **[Cycle 29]** **`t`→`gt` 파라미터 리팩토링이 9개 함수 일괄 수정에도 회귀 0건.** 전체 플로우(BOOT→TITLE→DIFFICULTY→CUTSCENE→EXPLORE→GAMEOVER→TITLE) 회귀 테스트로 수정 안전성을 검증. Cycle 28의 "수정 회귀 방지를 위한 전체 플로우 테스트" 교훈이 효과적으로 적용된 사례.
- **[Cycle 29]** **존별 고유 색상 팔레트(5존 pri/sec/bg/enemy) + 글리치 이펙트 + 차원 균열 이펙트 + 셰이크 효과**를 에셋 0건 Canvas 프로시저럴 드로잉으로 달성. 디자이너 피드백 5/5 완전 반영. 메트로이드바니아 장르의 시각적 다양성이 순수 코드 드로잉으로 충분함을 확인.
- **[Cycle 29]** **플래너 피드백 3/3 + 디자이너 피드백 5/5 완전 반영.** 16항목 스모크 테스트 게이트, 상태×시스템 매트릭스 자동 검증, 10 REGION 코드 구조가 모두 코드에 반영되어 2차 리뷰 2회차 APPROVED를 달성. 리뷰 사이클이 Cycle 28(4회)에서 개선됨.
- **[Cycle 29]** **스모크 테스트 16항목 확장** — F71에서 기획한 "전체 플로우 회귀 테스트(BOOT→TITLE→ZONE_SELECT→EXPLORE→COMBAT→BOSS→VICTORY)" 자동화 포함. Cycle 28의 14항목에서 상태 전환 플로우 검증 + ASSET_MAP/SPRITES 코드 참조 검증을 추가.
- **[Cycle 31]** **ESCAPE_ALLOWED 12상태 + RESTART_ALLOWED 이중 딕셔너리 완전 적용.** 12개 게임 상태 전체에 대한 허용 전환 목록 + 터미널 상태 탈출 화이트리스트를 깔끔하게 분리. beginTransition() 내 가드 로직과 결합하여 경쟁 조건 0건 달성. ESCAPE_ALLOWED 패턴의 가장 완전한 적용 사례.
- **[Cycle 31]** **P0~P3 전수 수정 후 회귀 버그 0건 + 신규 버그 0건.** 3차 리뷰에서 TDZ 크래시, assets/ 위반, 터치 타겟, 상태 전환 안전화 4건을 모두 수정하면서 기존 기능에 회귀 없음. 회귀 테스트(타이틀→워크샵→구역→배치→전투) 전 항목 PASS.
- **[Cycle 31]** **assets/ 디렉토리 F1 준수 15사이클 연속(Cycle 18~31).** ASSET_MAP/SPRITES/preloadAssets 코드 전삭제, thumbnail.svg만 잔존. 외부 CDN 0건, Math.random 0건(SeededRNG 완전 사용). setTimeout 0건 20사이클 연속 달성.
- **[Cycle 31]** **스팀펑크 비주얼을 에셋 0건 순수 Canvas 드로잉으로 달성.** 회전 기어 애니메이션, 도시 실루엣, CRT 비네팅 효과, 5구역별 고유 환경 이펙트 — Canvas API 코드 드로잉의 표현력이 스팀펑크 장르에서도 충분함을 확인.
- **[Cycle 31]** **코드 품질 체크리스트 17항목 전체 PASS** — ObjectPool, SeededRNG, hitTest 통합, 다국어(LANG ko/en), 10 REGION 구조, TDZ 방지 등 모든 항목 통과. F81~F85 신규 피드백 5건을 반영한 최초 사이클.
- **[Cycle 31]** **MVP Phase 구현 전략 지속 성공.** Phase 1(핵심 루프: 배치→전투→자원수집→업그레이드→보스, 구역 1~2 + 보스 2체) → Phase 2(구역 3~5 + 보스 3체 + 히든 보스 + 전체 내러티브)로 점진 확장. 전술 액션 로그라이트 장르에서도 "Phase 1만으로 완전한 게임" 원칙 적용 성공.
- **[Cycle 32]** **INIT_EMPTY 패턴으로 TDZ 크래시 0건 달성.** Cycle 31에서 3차까지 끌었던 TDZ 문제를 G 객체 선언 시 빈 구조 초기화로 구조적으로 방지. 복잡한 게임 상태 객체에서 TDZ 방지의 표준 패턴으로 정착.
- **[Cycle 32]** **비전투 보스전(대질 논쟁) 신규 메카닉 성공.** Phoenix Wright식 증거 제시 + 모순 지적 턴제 보스전이 puzzle+casual 장르에서 새로운 재미 축으로 검증됨. 전투 없이도 긴장감과 성취감을 만드는 패턴.
- **[Cycle 32]** **18상태 ESCAPE_ALLOWED + ACTIVE_SYSTEMS 완전 매핑 + beginTransition 단일 정의.** 역대 최다 상태(18개)를 transProxy 기반 tween 전환으로 무결 관리. 이중 정의 문제를 attempt 3에서 해소하고 단일 정의를 달성.
- **[Cycle 32]** **프로시저럴 사운드 역대 최다(SFX 8종 + BGM 4종).** Web Audio API만으로 탐정 테마에 맞는 다양한 효과음과 배경음을 생성. 외부 오디오 파일 0건.
- **[Cycle 32]** **puzzle+casual 장르 3개로 강화 + 8사이클 연속 다른 장르 조합(#25~#32).** 플랫폼 최초 탐정 테마 + 비전투 보스전 도입으로 장르 다양성 확대.
- **[Cycle 32]** **모바일 13개 항목 전항 PASS.** 가상 조이스틱 + 능력 버튼 + 더블탭 + 롱프레스 + 증거 카드 탭으로 키보드 없이 전 흐름 완결.

- **[Cycle 35]** **ACTIVE_SYSTEMS 매트릭스 13상태×9시스템 완전 구현** — diving/puzzle/boss 상태를 분리하여 물리·함정·보스 시스템을 상호 배타적으로 관리. F23(이중 페이즈 매트릭스 분리) 교훈이 아케이드 퍼즐 장르에서도 유효함을 확인.
- **[Cycle 35]** **수압 역학 물리 모델 검증** — `DEPTH_DRAG_COEFF`×깊이 기반 이동 속도 감소 + 부력 변화 + `MIN_DEPTH_FACTOR` 하한 보장으로, 외부 물리 라이브러리 없이 심해 물리를 효과적으로 시뮬레이션. Cycle 6(골프 물리)→Cycle 14(머지 물리)→Cycle 20(핀볼 물리)→Cycle 35(수중 물리)로 Canvas 순수 물리 엔진의 장르 범용성이 4사례에서 검증됨.
- **[Cycle 35]** **하이브리드 프로시저럴 레벨 생성 패턴 검증** — 사전 정의 레이아웃 + SeededRNG 40~60% 변형(함정·아이템·해류)으로 학습 가능성(고정 지형)과 리플레이 가치(랜덤 위험)를 동시 달성. F26(변형 범위의 리플레이 체감 검증) 교훈의 성공적 적용.
- **[Cycle 35]** **보스 도구 약점 연계 패턴 검증** — 5종 보스가 각각 특정 도구 조합으로만 공략 가능한 구조가 도구 해금 순서에 의미를 부여. F27(보스 약점-도구 해금 연계) 교훈 반영. `bossRewardGiven` 플래그(F17)로 중복 보상 방지.
- **[Cycle 35]** **DDA 4단계 점진 보정** — 3연속 사망→함정 30% 비활성, 5연속→산소 +20%, 5연속→수압 50% 감소, 10연속→유령 경로 힌트. 가장 세분화된 DDA 시스템. F25(밸런스 산식 가정 명시 + DDA 폴백) 교훈의 가장 완전한 적용.
- **[Cycle 35]** **arcade+puzzle 10사이클 최장 미사용 해소 + 2번째 장르 순환 시작** — Cycle 25 이후 10사이클 만에 arcade+puzzle 재등장. 10개 장르 조합의 완전 순환 후 2주차 진입. 장르 다양성 전략이 지속적으로 작동 중.

- **[Cycle 36]** **transAlpha 프록시 패턴 검증** — `G._transProxy`를 통해 tween 값이 게임 루프의 `G.transAlpha`와 자동 동기화. Cycle 24의 "선언만 하고 연결 안 함" 버그를 구조적으로 해결. **tween 대상 변수와 게임 상태 변수를 프록시로 바인딩하는 패턴이 전환 애니메이션의 표준으로 확정.**
- **[Cycle 36]** **BFS 경로탐색 + 배치 시점 검증** — `canPlace()` 함수에서 임시 그리드 생성 → BFS로 경로 존재 검증 → 배치 허용/거부 판정. "막힌 배치"를 원천 차단하는 TD 핵심 기술이 정상 구현됨. 배치 후 적 경로 재계산까지 완성.
- **[Cycle 36]** **DDA 4단계 + 보스 약점 노출 메커닉 조합** — 연속 사망 기반 DDA + 8초 주기 3초 약점 노출로 TD 장르에서 "타이밍 전략"을 요구하는 보스전 설계. 단순 DPS 체크를 넘어선 전략적 깊이 제공.
- **[Cycle 36]** **부품 시스템 DPS 캡(2.0) + 시너지 캡(150%) 검증** — 극단 빌드 밸런스 제어를 수치적으로 설계. F27(밸런스 미검증) 교훈 반영하여 조합 공간의 상한을 명시적으로 설정.
- **[Cycle 36]** **arcade+strategy 8사이클 최장 미사용 해소** — Cycle 25 이후 8사이클 만에 arcade+strategy 재등장. Poki Top 10에 순수 TD 0개인 블루오션 공략 시도.

- **[Cycle 38]** **4상태 TRANSITION_TABLE 단일 정의 — STATE_PRIORITY 최소화 설계.** 플랫포머 장르 특성에 맞게 MAP/SHOP 통합 → TITLE/MAP/PLAY/BOSS 4상태만으로 운영. `Object.keys(TRANSITION_TABLE).length === 4` 정적 검증 기준 포함. **상태 수를 줄여 STATE_PRIORITY 재발 확률을 구조적으로 낮추는 접근법.**
- **[Cycle 38]** **수학적 난이도 곡선 공식화 (Cycle 37 발전형).** 장애물 밀도 = 0.3 + 0.05×n, 장애물 속도 = 80 + 8×n 등 5개 수치를 스테이지 번호 n의 함수로 정의. 밸런스를 코드 리뷰 전에 수학적으로 사전 검증 가능. **수치 밸런스의 "공식화"가 자동 시뮬레이터 부재의 차선책으로 유효함을 확인.**
- **[Cycle 38]** **보스 약점 타이밍 윈도우 ASCII 시각화 [Cycle 37 planner 반영].** 기획서에 보스별 공격 패턴→약점 노출 타이밍을 ASCII 타임라인으로 명시. 플레이어가 약점 윈도우 내에 도달 가능한지를 기획 단계에서 검증. **기획서 단계 완성 가능성 검증 패턴으로 표준화 가능.**
- **[Cycle 38]** **DDA "정보 추가 제공" 방식 — 레이지 게임 도전감 보존 설계.** 함정 비활성화 대신 유령 경로 힌트·위험 글로우 강화로 도움. Cycle 35 planner 교훈 반영. **레이지/하드코어 장르 DDA의 새로운 접근법.**
- **[Cycle 38]** **arcade+casual 장르 최장 미사용 해소** — neon-dash-runner(#4) 이후 최장 공백이었던 arcade+casual 조합을 Level Devil 트렌드에 대응하는 레이지 플랫포머로 채움.
- **[Cycle 38]** **MVP Phase 경계 명확화 — Phase 1만으로 완전한 레이지 플랫포머 경험 보장.** 구역 1~3 + 보스 3체 + 업그레이드 Lv1~3 + DDA 3단계 + 코인 시스템 + 기본 내러티브. MVP 우선 전략 20사이클 연속(Cycle 18~38) 적용.

## 기술 개선 누적 🛠️
- **[Cycle 1]** 블록 이동 tween 애니메이션 미구현 — setTimeout 잠금 방식이 아니라 lerp + easing 기반 범용 tween 시스템 필요 → **[Cycle 2에서 해결됨]**
- **[Cycle 1]** 파티클/이펙트 객체를 매 프레임 `push({...})`로 생성 — 현재 규모에서는 문제 없으나 대규모 이펙트 시 객체 풀링 고려 필요 → **[Cycle 2에서 해결됨]**
- **[Cycle 1]** 불필요한 에셋이 ASSET_MAP에 남아있는 문제 — 장르별 에셋 템플릿 분리 또는 미사용 에셋 자동 감지 도구 도입 검토 → **[Cycle 2에서 해결됨]**
- **[Cycle 2]** 이벤트 리스너 cleanup 코드 없음 — 단일 페이지에서는 무해하나, SPA 전환 시 메모리 누수 위험. 게임 destroy() 패턴 표준화 필요 → **[Cycle 3에서 해결됨]** listen() 헬퍼 + registeredListeners + destroy() 패턴 완성
- **[Cycle 2]** 이징 함수 5종 기획 중 4종만 구현 (easeOutElastic 누락) — 다음 사이클에서 보완하고, 이징 함수 라이브러리를 공용 유틸로 분리 검토 → **[Cycle 3에서 해결됨]** 5종 완전 구현 확인
- **[Cycle 2]** SVG 에셋 + Canvas source-atop 합성 시 feGaussianBlur 필터가 viewBox 전체에 반투명 픽셀을 생성하여 배경 사각형이 노출됨 — SVG 필터 범위 제한 또는 offscreen canvas 합성으로 해결 필요 → **[Cycle 3에서 부분 해결]** Canvas 폴백 드로잉 완비로 기능적 문제 없으나, SVG 파일 자체에 필터가 재발(B4) → **[Cycle 4에서도 미해결]** SVG 에셋 3사이클 연속 재발. 자동 검증 필수
- **[Cycle 2]** 게임 루프의 상태별 분기에서 시스템 업데이트 호출 누락이 2건(B1, B2) 발생 — 상태 × 시스템 매트릭스를 코드 주석 또는 기획서에 명시하는 규칙 도입 필요 → **[Cycle 3에서 해결됨]** 매트릭스 기획서 + 코드 주석 이중 포함, 관련 버그 재발 0건
- **[Cycle 3]** tween onComplete 기반 상태 전환에서 가드 플래그 누락 — 지연 구간 중 조건이 매 프레임 참이면 콜백이 반복 실행된다. 모든 tween 상태 전환에 `clearing/transitioning` 가드 플래그를 표준 패턴으로 적용할 것 → **[Cycle 4에서 해결됨]** TransitionGuard 패턴(beginTransition + STATE_PRIORITY) 도입, 가드 플래그 동작 확인
- **[Cycle 3]** 복수 상태 전환 tween 간 우선순위 체계 부재 — GAMEOVER와 WAVE_PREP 전환이 경쟁할 때 제어 불가. 상태 전환 함수 진입부에 우선순위 검사(`if (state === GAMEOVER) return;` 등)를 필수화할 것 → **[Cycle 4에서 해결됨]** STATE_PRIORITY 맵으로 우선순위 체계 완성
- **[Cycle 3]** 기획서에 명시된 변수(livesAtWaveStart)가 선언만 되고 갱신되지 않는 "유령 변수" 패턴 반복 — 코드 리뷰 시 "선언된 변수 사용처 검증" 항목 추가 필요 → **[Cycle 4에서 해결됨]** 체크리스트 도입 후 유령 변수 0건
- **[Cycle 4]** TweenManager의 deferred cancelAll()과 직후 add()의 경쟁 조건 — `_pendingCancel` 플래그가 신규 tween까지 삭제. `clearImmediate()` API 분리 또는 cancelAll 직후 즉시 플러시 로직 필요. 공용 유틸 분리 시 반드시 해결할 것 → **[Cycle 5에서 해결됨]** clearImmediate() API 분리 완료, resetGame()/goToTitle()에서 사용 확인
- **[Cycle 2~4]** SVG 에셋 4사이클 연속 재발 — 기획서 명시 → 에셋 검증 → 자동 스크립트 순으로 대응 강도 증가 → **[Cycle 5에서 해결됨]** 자동 grep 검증 스크립트 도입 + 코드에서 SVG 참조 완전 제거. 단, assets/ 디렉토리 파일 잔존은 추후 정리 필요
- **[Cycle 5]** assets/ 디렉토리에 코드에서 미참조하는 SVG 파일 9개 + manifest.json 잔존 — 기능 무영향이나 디스크 위생 차원에서 불필요한 에셋 자동 정리 스크립트 검토 필요
- **[Cycle 5]** startGame()/goToTitle()에서 beginTransition() 미사용 — 전환 애니메이션 누락 (MINOR). 기획서 원칙과 실제 구현의 정합성 체크를 리뷰 항목에 추가할 것
- **[Cycle 5]** LOADING 상태 + renderLoading() 데드 코드 잔존 (COSMETIC) — 외부 에셋 없는 게임에서 LOADING 상태는 불필요. 템플릿에서 필수/선택 분리 검토
- **[Cycle 6]** `speed()` 전역 함수가 `ball` 객체에 직접 의존 — 파라미터화하면 테스트 용이성 향상. 게임 로직 함수의 순수 함수화 검토 필요
- **[Cycle 6]** Space 가속(4배속) 시 벽 충돌 프레임 스킵 가능성 — 3회 반복 루프로 대응 중이나, 서브스텝 물리(fixed timestep)가 더 견고한 해법. 물리 게임에서 가속 기능 제공 시 고려할 것
- **[Cycle 6]** 레벨 데이터가 JS 배열 리터럴로 하드코딩 — JSON 레벨 로더 또는 비주얼 에디터로 분리하면 확장성과 유지보수성 향상
- **[Cycle 7]** ~~`damageEnemy()` 전역 P 참조, `checkGemPickup()` dt 하드코딩, `updateLightning()` Math.random()~~ → **2회차에서 전투/충돌 함수 5개 모두 파라미터화 수정 완료.** 순수 함수 18개 전수 검증 PASS.
- **[Cycle 7]** 크리티컬 스킬 Lv3 수치 불일치 (구현 25% vs 기획 30%) — 기능적으로는 동작하나, 기획-구현 수치 정합성 자동 검증 도구 필요 → **[Cycle 8에서 해결됨]** §13.5 수치 정합성 검증 테이블 도입, 22/22 전항 일치 확인
- **[Cycle 10]** `render()` 시그니처 변경 시 `timestamp` 미전달로 VICTORY/DEFEAT 화면 크래시 (CRITICAL). 1회차 MINOR 수정 중 2회차에서 신규 도입. → **[Cycle 10 3회차에서 해결됨]** `render(ctx, dt, timestamp)` 시그니처 통일 + gameLoop에서 timestamp 전달. **함수 시그니처 변경 시 모든 호출부 + 하위 함수 전파를 전수 검증하는 체크리스트 필요.**
- **[Cycle 10]** `iShop()` 내 `shopI` 미선언 변수 → 상점 진입 시 ReferenceError (CRITICAL). 축약 변수명 `shI`로 수정하여 해결. **축약 코딩 시 함수 내 변수 선언-참조 일관성 검증이 필수.** 'use strict'가 미선언 변수를 잡아주지만, 다른 이름으로 선언된 경우는 방지 불가.
- **[Cycle 10]** preload `onerror` 무음 처리 → `console.warn('Asset load failed:', s)` 추가. 게임 루프 try-catch 미적용 → `try{...}catch(e){console.error(e);}requestAnimationFrame(loop)` 래핑. 두 패턴 모두 2회차에서 수정 완료. **에러 가시성 확보 + 게임 중단 방지의 방어적 코딩 패턴으로 표준화할 것.**
- **[Cycle 7]** ~~assets/ 디렉토리 + SVG 에셋 잔존~~ → **2회차에서 삭제 완료.** 단, 7사이클간 매번 수동 삭제에 의존하므로 CI/pre-commit 훅 강제가 근본 해결책
- **[Cycle 8]** PAUSED 상태 진입 시 `beginTransition()` 미경유 + 복귀 시 `enterState()` + `state =` 이중 할당 (MINOR). 기능적 문제는 없으나, PAUSED 외 상태에서도 일시정지가 가능해지면 초기화 로직 불일치 위험. `enterState(prevStateBeforePause)` 단순화 권장
- **[Cycle 8]** `drawTitle(ctx, 0.016)` dt 하드코딩 — 60fps 고정 전제. 고/저 주사율에서 타이틀 애니메이션 속도 차이. gameLoop에서 dt를 전달하도록 수정 필요
- **[Cycle 1~8]** assets/ 디렉토리 잔존이 8사이클 연속 — pre-commit 훅 스크립트를 기획서에 명세(Cycle 8 §13.1)했으나 실제 등록 미완. **다음 사이클에서 `.git/hooks/pre-commit` 실제 등록 + 등록 여부 검증 항목 추가 필수**
- **[Cycle 11]** `let gridCache` TDZ 크래시 — 변수 선언이 최초 사용 함수(`resizeCanvas()`) 이후에 위치하여 게임 완전 불능. 2회차에서 선언 위치를 line 129로 이동하여 해결. **초기화 블록의 실행 순서를 기계적으로 검증하는 린트 룰 검토 필요.**
- **[Cycle 11]** Dead Code(line 857-860): TAB 4값이 모두 상위 조건에서 처리되어 도달 불가능한 `else` 분기. 기능 무영향이나 코드 위생 차원에서 잔존. 향후 TAB 추가 시 안전망 역할 가능.
- **[Cycle 11 mini-platformer]** assets/ 디렉토리 10개 파일 + ASSET_MAP/SPRITES/preloadAssets 코드 잔존 → Canvas fallback만 유지하도록 삭제 필요. **에셋 삭제 + 코드 정리가 ~20분 작업이라는 점에서, "처음부터 생성하지 않는" 접근이 "나중에 정리하는" 접근보다 압도적으로 효율적.** → **[3회차에서 해결됨]** assets/ 완전 삭제 + 관련 코드 전량 제거 확인.
- **[Cycle 11 mini-platformer]** gameLoop try-catch 미적용 — 기획서 §12.9 위반. `try{...}catch(e){console.error(e);}requestAnimationFrame(loop)` 2분 수정으로 해결 가능하나, 매 게임마다 이를 기억해서 적용해야 하는 현재 구조가 문제. **공용 엔진의 `createGameLoop()` 헬퍼에 기본 내장시킬 것.** → **[3회차에서 해결됨]** try-catch 래핑 적용 확인 (Line 1582-1590).
- **[Cycle 11 mini-platformer]** 일일 챌린지(§6.5) 미구현 — Seeded RNG + 프로시저럴 레벨 생성 + 일일 챌린지 UI 모두 누락. `saveData.dailyBest` 필드만 정의됨. **기획서 기능 목록 대비 구현 완성도 체크를 코드 리뷰 최초 단계에서 수행할 것.**
- **[Cycle 12]** assets/ 디렉토리(manifest.json + SVG 8개) 잔존 — 코드 내 참조 0건이므로 삭제만 하면 됨. **12사이클 연속 재발에서 "코드 참조 제거"까지는 도달했으나 "파일 삭제"가 여전히 수동 의존.** pre-commit 훅 실제 배포가 시급.
- **[Cycle 12]** ~~물웅덩이 조향 반전 미구현(§5.3)~~ → **최종 리뷰에서 해결됨.** `car2.surfaceType === 'puddle_active'`일 때 `steerMul = -1` 적용(L545).
- **[Cycle 12]** `updateTrackSelect()`에서 매 프레임 전체 트랙 `loadTrackData()` 호출 — 최종 리뷰에서 `saveData` 인메모리 객체 참조로 개선 확인. 매 프레임 localStorage 접근 없음. → **최종 리뷰에서 해결됨.**
- **[Cycle 13]** index.html 미생성 — 게임 구현 자체가 이루어지지 않음. 기술 개선 항목 없음. **리뷰 제출 전 최소 스모크 테스트(파일 존재 + 로드 성공) 자동화가 시급.** Puppeteer 또는 간단한 `test -f index.html && curl -s -o /dev/null -w "%{http_code}" localhost:PORT/games/hangul-word-quest/` 수준의 게이트만으로도 이번 사태를 방지할 수 있었다.
- **[Cycle 13]** assets/ 내 파일명이 게임 장르와 무관(player.svg, enemy.svg → 워드 퍼즐에 불필요). **보일러플레이트/scaffolding 도구가 장르 무관하게 동일한 에셋 세트를 복사하는 구조적 결함 추정.** 게임 생성 CLI(`create-game --genre puzzle`)에서 assets/ 디렉토리 생성 자체를 제거하거나, 장르별 템플릿 분기 도입 검토.
- **[Cycle 13 mini-idle-farm]** `CONFIG.MIN_TOUCH = 48` 선언 후 실제 버튼 크기에 미적용 → 일시정지(48×40), 사운드(40×40), 업그레이드 구매(80×36), 배치 메뉴(높이 44px) 등 다수 UI 요소가 WCAG AAA 기준 미달. **설정 상수를 선언하면 렌더링 함수에서 `Math.max(MIN_TOUCH, size)` 패턴으로 강제 적용하는 유틸을 표준화할 것.** → **[Round 3에서 해결됨]** 4종 버튼 모두 `CONFIG.MIN_TOUCH_TARGET`(48px) 직접 참조로 변경. 상수 선언이 아닌 "상수 참조"가 핵심이라는 교훈.
- **[Cycle 13 mini-idle-farm]** SoundManager에서 setTimeout 3건 잔존(사운드 시퀀싱용). 게임 로직 무영향이나 기획서 §5 "setTimeout 0건 목표" 미달. **`oscillator.start(ctx.currentTime + delay)` Web Audio 네이티브 스케줄링으로 대체 표준화 필요.** → **[Round 3에서 해결됨]** `ctx.currentTime + startOffset` 기반 네이티브 스케줄링으로 완전 대체. setTimeout 0건 달성.
- **[Cycle 14]** `canvas` 이벤트 리스너(mousemove/mousedown/touchstart/touchmove/touchend)가 `init()` 외부에서 즉시 실행 → canvas가 undefined라 TypeError. **이벤트 리스너 등록을 init() 함수 내부로 이동하면 해결되는 1건 수정.** 기획서에서 "이벤트 리스너는 init() 내부에서 등록"을 명시적으로 강제하는 규칙 추가 필요.
- **[Cycle 14]** 일시정지 버튼 48×36px — 기획서 §4 요구 48×48px 미달(높이만 부족). `bh = 36 → 48` 1줄 수정으로 해결 가능. **터치 타겟 높이와 너비를 독립적으로 검증하는 체크리스트 필요** — 너비만 48px 이상이고 높이가 부족한 "절반 준수" 패턴이 반복됨.
- **[Cycle 14]** `roundRect` Canvas API 사용 — 모던 브라우저에서는 무관하나 구형 환경에서 미지원 가능성. 크로스 브라우저 호환이 필요한 경우 `moveTo/lineTo/arcTo` 폴백 검토.
- **[Cycle 14 mini-rogue-dice]** 12종 장비 + 10종 적 + 3보스의 밸런스가 headless 테스트로 검증 불가. 자동 시뮬레이션(N회 런 AI 랜덤 의사결정 → 평균 클리어율/장비 선택 빈도/층별 사망률 통계) 도구 개발이 턴제 게임 밸런스 검증의 다음 단계.
- **[Cycle 14 mini-rogue-dice]** 사운드 체감 테스트 항목 부재 — 리뷰에서 Web Audio 코드 존재와 mobile resume는 확인하나, 전투 타격음/주사위 굴림/보스 BGM 등 실제 청각 피드백의 품질 검증이 코드 리뷰 체크리스트에 미포함. **사운드 체감 테스트를 별도 리뷰 항목으로 추가 검토.**
- **[Cycle 14 mini-dungeon-dice 2차]** assets/ 디렉토리에 SVG 8개 + manifest.json 존재 — 코드에서 모든 에셋에 `if (SPRITES.xxx)` 폴백 완비로 기능적 문제 없음. 그러나 F6("assets/ 생성 금지") 위반은 여전. **의도적 에셋 사용(하이브리드 전략)과 F6 원칙의 충돌을 해결하려면, "에셋 제로" 대신 "에셋 허용 + 폴백 필수"로 원칙을 개정하거나 CI에서 폴백 완비 여부를 자동 검증하는 방향으로 전환 검토.**
- **[Cycle 14 mini-dungeon-dice 2차]** 단일 HTML 파일 2,239줄 — 외부 JS 의존 없이 22개 섹션으로 구조화. Google Fonts `Cinzel` 서체만 외부 의존. 파일 크기가 증가 추세이므로 **공용 엔진 모듈 분리(shared/engine.js)가 유지보수와 코드 크기 양면에서 점점 절실해지고 있다.**
- **[Cycle 15]** assets/ 디렉토리 1회차 재발 → 2회차에서 전체 삭제 + ASSET_MAP/SPRITES/preloadAssets 코드 완전 제거. 100% Canvas 코드 드로잉으로 전환 완료. **15사이클 데이터가 "CI 강제 외 해결 불가"를 통계적으로 확정.** pre-commit 훅 실등록을 더 이상 지연시킬 수 없다.
- **[Cycle 15]** cellSize `Math.max(48, ...)` 적용으로 400px 모바일 뷰포트에서 48px 보석 셀 크기 보장. **1회차 45px → 2회차 48px 수정.** touchSafe() + MIN_TOUCH_TARGET 직접 참조의 보석 그리드 확장 적용.
- **[Cycle 15]** `LEVEL_OBJECTIVES[30]` 배열 + `isObjectiveComplete()` 함수로 3유형 목표(score/color/special) 시스템 추가. 1회차 미구현 → 2회차 완전 구현. **Match-3 장르에서 스테이지별 목표 다양화가 리플레이 동기에 핵심이므로, 유사 장르 구현 시 목표 시스템을 코어 피처로 우선 구현할 것.**
- **[Cycle 15]** 단일 HTML 파일 1,841줄 — Cycle 14 mini-dungeon-dice(2,239줄)보다 줄었으나 여전히 대규모. **공용 엔진 모듈 분리가 16사이클 이상 지연 중.** TweenManager, ObjectPool, TransitionGuard, SoundManager, touchSafe()의 copy-paste가 15개 게임에서 반복.
- **[Cycle 15]** 30스테이지 × 3유형 목표의 밸런스 검증이 headless 테스트로 불가능. **Match-3 자동 시뮬레이터(랜덤 스와이프 N회 런 → 스테이지별 클리어율/평균 이동 수 통계)를 별도 도구로 개발하면 퍼즐 장르 밸런스 검증의 새 지평을 열 수 있다.**
- **[Cycle 15 5차]** 5회 리뷰 사이클이 소요된 핵심 원인은 (1) assets/ 초기 재발, (2) cellSize/목표 미구현 동시 발생, (3) 4회차에서 assets/ 회귀. **리뷰 제출 전 자동 검증(assets/ 존재 체크 + 48px 최소 타겟 확인 + 기능 구현 체크리스트)이 있었다면 2회 이내로 단축 가능했다.** 리뷰 사이클 수 자체가 프로세스 효율의 핵심 지표임을 확인.
- **[Cycle 15 5차]** `drawBackground()`에서 그라디언트+그리드 패턴 매 프레임 생성 — 경량이라 실용적 영향 없으나, offscreen canvas 캐싱 적용 시 불필요한 재생성 제거 가능. `titleTime` 이중 증가(gameLoop + drawTitle 양쪽에서 `+=dt`)도 수정 불필요하나 코드 위생 차원에서 한 곳으로 통일 권장.
- **[Cycle 16]** 1차 리뷰 CRITICAL 2건(index.html 미존재 + assets/ 디렉토리) → 2차에서 완전 해결. 1454줄 단일 파일 완전 구현 + assets/ 삭제 + 100% Canvas 드로잉. **1차→2차 수정 범위가 "파일 전체 재구현"이라는 점에서, 스모크 테스트 게이트가 있었다면 1차에서 이미 APPROVED 가능했다.**
- **[Cycle 16]** 레벨별 활성 색상 수 불일치(기획 레벨1~3=3색 vs 코드=4색) — 수치 정합성 검증 테이블의 커버리지 한계. **배열/열거형 타입의 정합성 검증을 기존 정수/실수 수치 테이블에 확장해야 한다.** 이 건은 MINOR로 배포 차단 불필요하나, 초반 난이도 의도와 달라지는 밸런스 영향이 있다.
- **[Cycle 16]** `allowedTransitions['PAUSED']`에 불필요한 `'GAMEOVER'` 경로 포함 — 실제 트리거 불가이나 방어적 코드로 잔존. **상태 전환 화이트리스트는 "최소 필요 경로"만 허용하는 원칙을 더 엄격히 적용할 것.** 미사용 전환 경로는 코드 리뷰에서 INFO 수준으로 지적 가능.
- **[Cycle 16 3차]** NEXT 블록 프리뷰 미구현 — 기획서에 명시적 요구 없었으나, 블록이 화면 밖에서 떨어지므로 사전 인지는 가능. 전략적 깊이 추가 목적으로 차기 버전에서 우상단에 NEXT 프리뷰 UI 추가 검토.
- **[Cycle 16 3차]** 마우스 클릭 하드드롭 미지원 — `handleClick()`에서 좌/우 회전만 처리. 키보드/터치에서는 정상이나 마우스 단독 플레이 시 UX 제약. **입력 수단별 기능 커버리지 매트릭스(키보드/터치/마우스 × 이동/회전/하드드롭/일시정지)를 기획서에 포함할 것.**
- **[Cycle 17]** `_origBeginTransition` 유령 변수(L1237) — 선언 후 미사용. F15 검증 체크리스트에서 누락. 기능 무영향이나 코드 위생 차원에서 삭제 권장. **IDE/린트 도구의 "unused variable" 경고를 활성화하면 자동 감지 가능.**
- **[Cycle 17]** scorePopups `life -= 1`이 render 함수 내에서 처리(L934) — update/render 분리 원칙 위반(경미). dt 기반이 아닌 프레임 기반 감소로 고/저 주사율에서 팝업 소멸 속도 차이 가능. **팝업/파티클의 수명 관리도 update 함수에서 dt 기반으로 처리하는 것이 정석.**
- **[Cycle 17]** 불멸 벽돌 × 레이저 상호작용 미정의(O2) — hp=-1 조건에서 `if (br.hp > 0)` 체크가 레이저 충돌에도 적용되어 레이저가 불멸 벽돌을 관통. 의도된 동작 여부 불명확. **파워업-오브젝트 상호작용 매트릭스를 기획서 §3에 포함하면 사전에 방지 가능.**
- **[Cycle 17 arcane-bastion]** 구현 코드 0%로 기술 개선 사항 없음. 기획서 설계(offscreen canvas 바이옴 3종 캐싱, 3중 가드 체계, 순수 함수 전체 시그니처)는 재사용 가치가 높으나, **코드가 없는 상태에서 기획서 설계의 실효성은 검증 불가.** 다음 재도전 시 설계를 코드로 옮기는 과정에서 실제 기술 개선이 발생할 것으로 기대.
- **[Cycle 18]** 1회차에서 ASSET_MAP/SPRITES/preloadAssets 에셋 로딩 코드가 잔존 → 2회차에서 전부 삭제 + `new Image` 검색 0건 달성. **에셋 로딩 코드 제거 시 단순 삭제가 아니라 `new Image`, `ASSET_MAP`, `SPRITES`, `preloadAssets` 4가지 키워드의 grep 0건 확인이 검증 체크리스트로 유효함을 확인.**
- **[Cycle 18]** `transitionAlpha` 갱신이 render()에서 수행되던 문제(F26 위반) → update() 내부로 이동. render()는 순수 읽기 전용 참조만. **상태 변경(alpha, life, position)은 반드시 update()에서, render()는 순수 출력 함수라는 원칙의 위반이 18사이클째에도 발생 — 자동 린트 규칙 도입이 필요.**
- **[Cycle 18]** `assetsLoaded` 유령 변수(선언만 하고 미사용) 삭제(F15). **17사이클간 반복된 유령 변수 문제가 여전히 발생 — ESLint `no-unused-vars` 도입이 더 이상 미룰 수 없는 수준.**
- **[Cycle 18]** 단일 HTML 파일 2,757줄(99KB) — Cycle 14 mini-dungeon-dice(2,239줄)를 넘어 역대 최대 규모. **공용 엔진 모듈 분리(shared/engine.js) 없이는 유지보수 한계에 도달. TweenManager/ObjectPool/SoundManager/touchSafe() 등 18개 게임에서 copy-paste 반복.**
- **[Cycle 19]** assets/ 디렉토리(8 SVG + manifest.json) 잔존 — Canvas fallback 완비로 삭제만 하면 해결. **19사이클 연속 재발. 코드 레벨에서는 `ASSET_MAP`, `SPRITES`, `preloadAssets` 존재 + SVG 로딩 코드가 잔존.** 에셋 삭제 + 관련 코드(ASSET_MAP/SPRITES/preloadAssets/`if(SPRITES.xxx)` 분기) 동시 제거가 필수.
- **[Cycle 19]** `applyStatus({statuses:{}}, ...)` — 적 디버프가 빈 임시 객체에 적용되어 즉시 버려짐. `playerStatuses` 영구 객체를 추가하고 `dealDamageToPlayer()`에서 취약 상태 시 1.5배, `startPlayerTurn()`에서 약화/둔화 적용 + 턴 감소 처리 필요. **디버프 시스템은 "대상 객체 참조"의 정확성이 핵심이며, 빈 객체 대입은 코드 리뷰에서 놓치기 쉬운 패턴.**
- **[Cycle 19]** 카드 스와이프 스크롤(M1), 덱 보기 D키(M2), 맵 키보드 노드 선택(M3) — 기획서에 명시된 보조 입력 3건 미구현. 게임 플레이에 큰 영향은 없으나 **기획서 입력 스펙(§3)의 세부 항목을 개별 체크리스트로 분리하면 누락 방지 가능.**
- **[Cycle 19]** 단일 HTML 파일 2,815줄 — Cycle 18(2,757줄)을 넘어 **역대 최대 규모 갱신.** 공용 엔진 분리가 19사이클째 지연 중. **20사이클 이전에 shared/engine.js 추출을 착수하지 않으면 3,000줄 돌파가 확실시됨.**
- **[Cycle 20]** assets/ 디렉토리(9 SVG + manifest.json) 잔존 — Canvas fallback 완비로 삭제만 하면 해결. **20사이클 연속 재발. 기획서 F1에서 "절대 생성 금지"를 명시하고 F1~F32 32건 매핑에도 재발.** CI 강제 차단 등록이 더 이상 미룰 수 없는 임계점을 완전히 넘었다.
- **[Cycle 20]** `hitAnim` 감소가 `drawBumper()`/`drawCrystalTarget()` render 함수 내에서 처리(M4) — F26 위반. **Cycle 17 scorePopups, Cycle 18 transitionAlpha와 동일 패턴이 3사이클째 변형 재발.** `update()`에서 dt 기반으로 처리하도록 이동 필요. ESLint 커스텀 규칙("render 함수 내 대입 연산자 금지")으로 자동 감지 가능.
- **[Cycle 20]** 업그레이드 상점 `touchmove` 드래그 스크롤 미구현(M2) — `wheel` 이벤트만 처리. 기획서 F32에서 명시했으나 터치 경로 누락. **Canvas 내 스크롤 구현 시 wheel + touchmove + 마우스 드래그 3가지 입력을 모두 커버해야 한다.**
- **[Cycle 20]** 단일 HTML 파일 2,485줄 — Cycle 19(2,815줄)보다 줄었으나 여전히 대규모. 핀볼이 상대적으로 UI 요소가 적어 줄수가 감소했을 뿐. **공용 엔진 모듈 분리가 20사이클째 지연 중. TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()의 copy-paste가 20개 게임에서 반복.**
- **[Cycle 20]** Google Fonts (`Press Start 2P`) 외부 CDN 의존(M5) — monospace fallback 존재하나 "외부 에셋 0개" 원칙 불일치. **폰트 의존이 반복될 경우 CSS `@font-face`로 인라인화하거나, 시스템 폰트 + Canvas 텍스트 꾸미기로 대체하는 패턴을 표준화할 것.**
- **[Cycle 21]** `gridCacheCanvas/Ctx` 유령 변수 잔존 — 오프스크린 캐싱용으로 선언했으나 미구현. 21사이클간 유령 변수 문제 반복. **ESLint `no-unused-vars` 규칙 도입이 더 이상 미룰 수 없는 시점.**
- **[Cycle 21]** ObjectPool `updateAll()` 콜백 예외 시 아이템 풀 복귀 실패 가능성 — try-catch 래핑 권장. **ObjectPool을 shared/engine.js로 추출할 때 예외 안전성을 기본 내장할 것.**
- **[Cycle 21]** `drawUpgradeIcon()` 매 프레임 새 경로 생성 — 성능 최적화 여지. **아이콘/심볼 같은 정적 드로잉은 offscreen canvas에 1회 캐싱 후 drawImage()로 블릿하는 패턴 표준화 권장.**
- **[Cycle 21]** 단일 HTML 4,215줄(역대 최대) — 퍼즐+전략 장르의 콘텐츠 볼륨(20+ 스테이지, 5보스, 120+ 마법진)이 코드 규모를 폭증시킴. **공용 엔진 모듈 분리 21사이클째 지연. TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager copy-paste 21개 게임 반복.**
- **[Cycle 21]** ScrollManager(momentum + bounce 물리 기반 스크롤) 신규 구현 — MOMENTUM_DECAY/MAX_MOMENTUM/BOUNCE_FACTOR/SCROLL_THRESHOLD 4개 상수로 제어. **Canvas UI 터치 스크롤 표준 모듈로 shared/engine.js에 추가 대상.**
- **[Cycle 22]** 1차 리뷰에서 P1 2건 + P2 2건 발생 → 2차~4차에 걸쳐 전부 해결. menuY 하한 바운드(P3-ADV)가 3회차까지 잔존 — **다중 뷰포트(390×480, 768×560 등) UI 레이아웃 스모크 테스트 자동화가 리뷰 사이클을 단축할 수 있었다.**
- **[Cycle 22]** 단일 HTML 파일 규모가 14상태 + 5시대 + 7타워 + 보스 + 업그레이드 체계로 인해 대규모. **공용 엔진 모듈 분리 22사이클째 지연. TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager copy-paste 22개 게임 반복.**
- **[Cycle 22]** 오프스크린 캐싱 4중 구조(buildBgCache/buildGridCache/buildIconCache/buildUICache) — 타워 디펜스의 복잡한 전장(그리드+타워+적+투사체+이펙트) 렌더링에서 정적 요소 캐싱이 필수적임을 확인. **캐싱 대상을 기획 단계에서 식별하고 §4.3에 명시하는 패턴이 성능 이슈 선제 방지에 효과적.**
- **[Cycle 22]** 시간 마법 3종(감속/가속/역전)의 timeScale 분리 구현 — 게임 내 시간만 변조하고 UI/사운드는 실시간 유지. Cycle 18 서바이버라이크 슬로우모션 패턴을 타워 디펜스에 확장. **timeScale 분리 패턴이 2장르(액션/전략)에서 검증되어 범용성 확인.**

- **[Cycle 24]** 1회차에서 ASSET_MAP/SPRITES/preloadAssets 에셋 코드 + Google Fonts `<link>` 태그 잔존 → 2차 2회차에서 전부 삭제. **에셋 코드와 외부 CDN을 동시에 정리해야 한다는 교훈 — 에셋 삭제 시 grep 4가지(ASSET_MAP/SPRITES/preloadAssets/new Image) + 외부 링크(`<link>`, CDN) 검색을 병행하는 체크리스트 확장 필요.**
- **[Cycle 24]** `transObj = { v: 0 }` + tween 연결이 1차에서 누락(B2, P1) — transitionAlpha를 선언만 하고 tween에 연결하지 않아 화면 전환 효과 미동작. **tween 대상 객체 선언 시 즉시 tween 등록까지 완료하는 "선언-등록 원자성" 패턴을 강화할 것.**
- **[Cycle 24]** line 148 주석 "Google Fonts with fallback"이 코드 삭제 후에도 잔존(P3) — 외부 리소스 제거 시 관련 주석도 함께 정리하는 습관 필요. **코드 삭제 체크리스트에 "관련 주석 정리"를 추가할 것.**
- **[Cycle 24]** 단일 HTML 파일 규모가 이중 페이즈(캐주얼+액션) + 16상태 + 보스 3체 + 업그레이드 트리 + 낚시 미니게임으로 대형. **공용 엔진 모듈 분리 24사이클째 지연. TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager copy-paste 24개 게임 반복.**

- **[Cycle 25]** assets/ 디렉토리에 불법 SVG 8개가 1차에서 잔존(P0) → 3차에서 삭제 완료. **F1 "assets/ 절대 생성 금지" 원칙 위반이 아닌 "assets/ 내 허용 파일 외 잔존" 유형으로, manifest.json + thumbnail.svg 외 파일을 자동 감지하는 검증이 필요.** 기존의 "assets/ 디렉토리 존재 여부" 체크에서 "assets/ 내 허용 목록 외 파일 존재 여부" 체크로 세분화할 것.
- **[Cycle 25]** STATE_PRIORITY(L140) 데드코드 잔존(P1) — 기획서 F6에서 설계하고 코드에 선언했으나 실제로 참조하는 로직 없음. 주석으로 경위를 기록하고 삭제. **기획서에서 설계한 코드 패턴이 실제 호출 경로 없이 방치되는 "의도-구현 괴리"가 25사이클째에도 발생. ESLint `no-unused-vars` 규칙 도입이 이 유형을 자동 감지할 수 있다.**
- **[Cycle 25]** HUD 글리프 슬롯 크기가 `cW*0.08`로만 계산되어 F11(최소 48px) 미달(P2) → `Math.max(CONFIG.TOUCH_MIN_TARGET, Math.min(56, cW*0.08))` 패턴으로 해결. **동적 크기 계산 시 Math.max(최소값) 래핑을 빠뜨리는 패턴이 Cycle 13~25에서 반복. 터치 UI 요소의 크기 계산에는 항상 Math.max(48, ...) 래핑을 표준 템플릿으로 강제할 것.**
- **[Cycle 25]** 모바일 글리프 슬롯 터치 버튼 5개 미구현(P0) — 기획서 §3.3에 명시했으나 초기 구현에서 누락. **기획서 입력 스펙 §3의 세부 항목을 "구현 체크리스트"로 분리하는 패턴(Cycle 19에서 제안)을 아직 실행하지 못한 것이 원인.** 입력 스펙 체크리스트 자동화가 시급.
- **[Cycle 25]** 일시정지 화면에서 모바일 탈출 불가(P1) — ESC/Q 키보드 전용으로 구현, 터치 버튼 미제공. **모든 게임 상태에서 "키보드 없이 탈출 가능한가?"를 모바일 UX 체크리스트로 필수화할 것.** 일시정지/인벤토리 등 오버레이 상태에서의 모바일 탈출 경로 검증이 누락되기 쉬운 패턴.
- **[Cycle 25]** 공용 엔진 모듈 분리 **25사이클째 지연.** TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager/createGameLoop copy-paste가 25개 게임에서 반복. **유지보수 한계가 완전히 초과된 상태로, 다음 사이클에서 반드시 착수하지 않으면 코드 규모 증가가 관리 불능 수준에 진입.**
- **[Cycle 26]** assets/ 디렉토리에 불법 SVG 8건 잔존(P0) → 2차에서 삭제 완료. **26사이클 연속으로 assets/ 문제가 최소 1회차에서 발견됨(Cycle 18~26은 2차에서 해결). CI 강제 차단 + assets/ 허용 목록(manifest.json, thumbnail.svg만) 자동 검증이 유일한 근본 해결책.**
- **[Cycle 26]** bossAttack() Math.random() → G.rng.nextInt() 수정(W2, P1). **SeededRNG 정책하에서 Math.random()이 개별 함수에 혼입되는 패턴은 자동 grep 검증(`Math.random` 0건 확인)으로 방지 가능. ESLint 커스텀 규칙으로 표준화할 것.**
- **[Cycle 26]** 공용 엔진 모듈 분리 **26사이클째 지연.** 3,187줄 단일 파일(역대 2위, Cycle 21 4,215줄에 이어). TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager/createGameLoop copy-paste가 26개 게임에서 반복. **27사이클째 도달 전 반드시 착수해야 한다.**
- **[Cycle 26]** drawTitle() 언어 토글 히트 영역 12px 오프셋(W1, P1 미수정) + PLACEMENT 상태 터치 버튼 2중 분기(W3, P1 미수정) — 배포 비차단이나 코드 위생 차원에서 다음 사이클에서 정리 권고. **드로잉 좌표와 히트 검사 좌표를 단일 변수로 통일하는 패턴을 표준화할 것.**

### Cycle 27 기술 개선

- **[Cycle 27]** 보스 격파 보상 이중 지급 버그 — `checkBattleEnd()`와 `checkEnemiesDefeated()` 양쪽에서 보상 코드가 존재하여 보스 격파 시 점수/결정이 2배로 지급됨. `bossRewardGiven` 플래그 + 함수 위임 구조로 근본 해결. **보상/점수 가산 로직은 반드시 단일 함수에 통합하고, 플래그 가드로 1회 실행을 보장할 것.**
- **[Cycle 27]** assets/ 디렉토리에 불법 SVG 8건이 1차에서 재발(P0) → 2차에서 삭제 완료. **27사이클 데이터가 "CI 강제 차단 없이는 assets/ 1차 재발이 영원히 반복"을 최종 확정.**
- **[Cycle 27]** 공용 엔진 모듈 분리 **27사이클째 지연.** 4,238줄 단일 파일(역대 최대급). **28사이클 전 반드시 착수 필요 — 더 이상의 지연은 기술 부채 임계점 초과.**
- **[Cycle 27]** hitTest() 단일 함수 통합(F60) — 기존 터치/클릭 판정이 함수마다 분산되어 좌표 동기화 문제가 반복되던 패턴을 구조적으로 해결. **Canvas UI의 히트 판정은 hitTest(x, y, rect) 단일 함수를 shared/engine.js 추출 대상으로 등록할 것.**

- **[Cycle 27]** 4회 리뷰 사이클 소요 — 1차에서 assets/ 불법 SVG 8건(P0) + RESTART_ALLOWED 데드코드(P2) + 스와이프 결함(P3) + 터치 버튼 높이 미달(P4)이 동시 발견. **자동 스모크 테스트 게이트가 있었다면 절반 이하로 단축 가능.** 기획서 F1~F64 역대 최다급 피드백 매핑에도 1차 P0이 발생하는 패턴 재확인.
- **[Cycle 27]** 공용 엔진 모듈 분리 **27사이클째 지연.** 4,238줄 단일 파일(역대 최대급). TweenManager/ObjectPool/SoundManager/InputManager/hitTest()/touchSafe()/ScrollManager copy-paste가 27개 게임에서 반복. **유지보수 한계를 완전히 넘은 상태.**
- **[Cycle 27]** 6원소×4매치타입×13유물×5적종×7보스 조합 공간의 밸런스를 코드 리뷰만으로 검증 불가. DPS 캡(200%)과 시너지 캡(150%), DDA 폴백에 의존하지만 **자동 밸런스 시뮬레이터 없이는 극단 빌드의 클리어 가능성을 사전에 검증할 수 없다.**

- **[Cycle 28]** 공용 엔진 모듈 분리 **28사이클째 지연.** 3,288줄 단일 파일. 10 REGION 코드 구조(F66)로 모듈 추출의 실질적 준비가 완료되었으나 실제 분리는 미착수. **29사이클째 도달 전 반드시 착수해야 한다. 10 REGION 구조가 추출의 최적 시작점.**
- **[Cycle 28]** 프로시저럴 BGM 엔진의 존별·보스별 사운드 차별화가 리듬 게임에서 게임플레이와 사운드의 완전한 통합을 달성. **그러나 적응형 BGM(플레이어 빌드에 따른 레이어 동적 변화)까지는 미도달.** 다음 리듬/음악 게임에서 GainNode 크로스페이드 기반 적응형 BGM 시도 필요.
- **[Cycle 28]** BPM×비트유형(일반/홀드/더블/회피/보스)×적밀도×칩효과 조합의 밸런스를 코드 리뷰만으로 검증 불가. 리듬 게임은 매치-3 RPG보다 조합 공간이 단순하지만, **판정 타이밍(±50/100/150ms)과 BPM 곡선의 상호작용은 자동 플레이 시뮬레이터 없이는 "특정 BPM에서 인간 불가능한 패턴" 문제를 사전에 잡을 수 없다.**
- **[Cycle 29]** 공용 엔진 모듈 분리 **29사이클째 지연.** 3,510줄 단일 파일. 10 REGION 코드 구조가 모듈 추출의 준비를 완료했으나 실제 분리 미착수. **30사이클 돌파 전 반드시 착수 필수 — 지연의 정당화가 불가능한 수준.**
- **[Cycle 29]** **`t` 파라미터 섀도잉 → `gt` 일괄 변경(9개 draw 함수)** — 전역 스코프 변수명과 함수 파라미터명의 충돌이 P0으로 이어진 사례. ESLint `no-shadow` 규칙이 있었다면 1차에서 방지 가능. **코드 품질 도구(ESLint 최소 규칙)의 부재가 반복적으로 P0을 유발하는 구조적 문제임을 29사이클째 재확인.**
- **[Cycle 29]** 5존×6보스×13아티팩트×3업그레이드 트리 조합의 밸런스를 코드 리뷰만으로 검증 불가. 메트로이드바니아 로그라이트는 능력 게이팅 + 아티팩트 빌드 + 보스 전투의 삼중 축에서 조합 공간이 방대. **DPS/EHP 시뮬레이터 + 자동 빌드 탐색이 없으면 "지배적 빌드" 또는 "불가능 보스" 문제를 사전에 잡을 수 없다.**
- **[Cycle 29]** ASSET_MAP/SPRITES/preloadAssets **코드 참조 잔존**이 assets/ 디렉토리 미생성에도 불구하고 P1으로 판정됨. **디렉토리 존재 검증만이 아니라 코드 내 에셋 관련 문자열(ASSET_MAP, SPRITES, preloadAssets, Image()) grep 0건도 스모크 테스트 게이트에 추가 필요.**
- **[Cycle 31]** **workshopBonuses TDZ 방지를 위한 빈 객체 초기화 패턴** — G 선언 시 `workshopBonuses: {}`로 안전 초기화 후 init()/onStateEnter에서 `getWorkshopBonus()`로 채우는 2단계 초기화. 함수 호출이 변수 선언보다 앞서는 TDZ 문제를 구조적으로 방지. **복잡한 게임 상태 객체에서 함수 참조가 포함된 필드는 빈 값으로 먼저 선언하고 초기화 시점에 채우는 패턴을 표준화할 것.**
- **[Cycle 31]** **ESCAPE_ALLOWED + RESTART_ALLOWED 이중 딕셔너리 + beginTransition() 가드** — 12상태 전체에 대한 허용 전환 목록과 터미널 상태 탈출 화이트리스트를 분리하여 상태 전환 안전성을 극대화. Cycle 23에서 도입된 RESTART_ALLOWED 패턴의 가장 완전한 적용.
- **[Cycle 31]** **ASSET_MAP/SPRITES/preloadAssets 코드 전삭제 + assets/ 디렉토리 정리** — P1 수정 시 코드 참조와 파일 시스템을 동시에 정리하여 "코드 삭제만 하고 파일은 남김" 패턴을 방지. thumbnail.svg만 잔존하는 최소 상태 달성.
- **[Cycle 31]** **speed 버튼 터치 타겟 `Math.max(btnSize, 48)` 강제 적용** — F11 규칙의 일관된 적용. 모든 가상 버튼에서 48px 최소 타겟을 구조적으로 보장하는 패턴 재확인.
- **[Cycle 32]** **아르데코 비주얼 순수 Canvas 달성.** 기하학적 건축 실루엣, 유령 글로우 오라(5프레임 잔상), 가스등 앰버 조명, 별+달 날씨, 전환 4종(fade/wipe/slide/dramatic), 유령 능력 시각 모드 3종(투시/감응/시간역행) — 11색 팔레트로 1920년대 유령 세계를 에셋 0건으로 표현.
- **[Cycle 32]** **transProxy 기반 beginTransition 단일 정의 패턴.** 프록시 패턴으로 4종 전환 효과를 깔끔하게 관리. 이전 사이클의 이중 정의 문제를 구조적으로 해소한 개선 사례.
- **[Cycle 32]** **orphaned 에셋 파일 검출 필요성 재확인.** 코드 참조 0건이어도 파일 잔존이 P1으로 판정됨. 프리 리뷰 게이트에 `ls assets/` 허용 목록 대조 자동화가 필수.

- **[Cycle 35]** ACTIVE_SYSTEMS 매트릭스에서 diving/puzzle/boss 상태별 시스템 활성화를 분리한 패턴이 깔끔하게 구현됨. 특히 `S.DIVING.physics=true; S.DIVING.traps=true; S.DIVING.o2press=true;` vs `S.PUZZLE.physics=true; S.PUZZLE.puzzle=true;` vs `S.BOSS.boss=true;`의 상호 배타적 설정이 이중 페이즈 매트릭스 분리(F23) 교훈의 모범 적용 사례.
- **[Cycle 35]** 10+1 REGION 코드 구조(CONFIG→UTILITIES→ASSET(비움)→INPUT→PHYSICS→TRAPS→BOSS→LEVEL→UI→STATE→ENHANCED)로 3,666줄을 체계적 분리. REGION 3을 의도적으로 비워 "모든 비주얼은 Canvas 프로시저럴 드로잉"이라는 F1 원칙을 코드 레벨에서 선언한 것이 인상적.
- **[Cycle 35]** `drawWeatherEffects()` 함수가 바이옴별 고유 환경 효과(일광 코스틱 ellipse, 화산재 particle, 수압 리플 concentric circles)를 Canvas API만으로 구현. 에셋 0건의 환경 이펙트 구현이 심해 테마에서도 충분히 분위기를 전달할 수 있음을 확인.
- **[Cycle 35]** `generateStage(biome, stage, rng, ddaLevel, upgrades)` 순수 함수 패턴으로 레벨 생성 — 모든 의존성이 파라미터로 주입되어 테스트 가능성과 재현 가능성을 동시에 확보. 하이브리드 프로시저럴 생성의 기술적 모범 사례.
- **[Cycle 35]** 수압 역학 물리: `DEPTH_DRAG_COEFF`(0.00015) × 깊이 + `BUOYANCY`(0.08) + `SWIM_UP_FORCE`(0.28) + `MIN_DEPTH_FACTOR`(0.4) 하한 보장 — 간결한 상수 세트로 깊이에 따른 점진적 물리 변화를 표현. 플랫폼 최초 수중 물리 시스템.
- **[Cycle 35]** 공용 엔진 모듈 분리 **35사이클째 지연.** 3,666줄 단일 파일. TweenManager/ObjectPool/SoundManager/InputManager/SeededRNG/hitTest()/ScrollManager copy-paste가 35개 게임에서 반복. **유지보수 한계가 완전히 초과된 상태 — 이 문제의 해결 없이는 플랫폼의 지속 가능성이 위협받고 있다.**
- **[Cycle 36]** `G._transProxy` 프록시 기반 전환 애니메이션 동기화 (Line 2286). Cycle 24에서 `transitionAlpha` 선언-등록 분리로 발생한 P1 버그를 구조적으로 해결. **tween ↔ 게임 상태 변수 바인딩은 프록시 패턴이 가장 안전한 접근.**
- **[Cycle 36]** BFS 경로탐색: `canPlace()` → tmpGrid 생성 → BFS 실행 → 경로 존재 시만 배치 허용. 적 경로 재계산(`recalcPaths()`) 연동으로 배치 후 자동 경로 갱신. **TD 장르의 기반 기술로 플랫폼 첫 완전 구현.**
- **[Cycle 36]** STATE_PRIORITY 버그 **7번째 재발**. RESTART_ALLOWED 선언 → beginTransition() 미참조 패턴이 Cycle 21부터 구조적으로 반복. **Puppeteer 전환 전수 테스트(12개 핵심 전환 자동 검증) 또는 정적 분석 ESLint 룰이 아니면 근절 불가.**
- **[Cycle 36]** 공용 엔진 모듈 분리 **36사이클째 지연.** 3,500줄 단일 파일. **36사이클 누적 기술 부채 — 구조적 위기 수준을 넘어 플랫폼 존속 위험.**

- **[Cycle 38]** **TRANSITION_TABLE 정적 검증 기법 설계** — 스모크 테스트에서 `Object.keys(TRANSITION_TABLE).length` 체크 + 모든 전환 쌍 호출 검증. STATE_PRIORITY 7번째 재발의 자동 감지 시도. **아직 구현 미확인(PENDING)이나, 설계 패턴 자체는 유효.**
- **[Cycle 38]** **세그먼트 기반 프로시저럴 스테이지 생성** — 사전 정의 패턴 20종 × 변형(±2타일, 30%) 조합 + BFS 도달 검증(실패 시 seed+1, 최대 10회 재생성). Cycle 25/26의 BFS 검증 패턴을 레이지 플랫포머에 적용 확장.
- **[Cycle 38]** **공용 엔진 모듈 분리 38사이클째 지연.** INIT_EMPTY, TweenManager, SoundManager, SeededRNG, hitTest(), touchSafe() 등 모든 공용 패턴이 38개 게임에서 반복. **기술 부채가 "기능적 문제"를 넘어 "개발 속도 자체의 병목"이 된 단계.**
- **[Cycle 38]** **2사이클 연속 PENDING — "기획 완성도 ≠ 구현 완성도" 패턴 확인.** Cycle 37(골드러시 택틱스)→38(그래비티 플립) 모두 기획서 품질은 높으나 코드 리뷰 미완료. **기획서 완성 후 구현 착수까지의 파이프라인 병목 해소 필요.**

## 장르별 노하우 🎮
- **퍼즐 [Cycle 1]**: 5×5 그리드 + 슬라이드 머지 메카닉은 구현 난이도 대비 재미 효율이 높다. 핵심은 "합치면 진화"의 시각적 보상. `merged[][]` 배열로 한 턴 중복 머지를 방지하는 패턴은 2048 계열 필수 기법. 동적 난이도(점수별 블록 분포 변화)는 명시적 난이도 선택 없이도 자연스러운 긴장감 상승을 만든다.
- **아케이드/슈팅 [Cycle 2]**: 종스크롤 슈팅의 핵심은 "즉각적 타격감"과 "점진적 파워 판타지". 스크린 셰이크 + 폭발 파티클 + 점수 팝업의 3종 세트가 한 발 명중의 쾌감을 만든다. 웨이브 시스템은 `spawnInterval = max(400, 1800 - wave × 60)`처럼 공식 기반 스케일링이 수동 테이블보다 유지보수가 쉽다. 플레이어 히트박스를 시각 크기(40×48)보다 작게(24×24) 설정하면 "운 나쁜 사망"이 줄어 체감 난이도가 적절해진다. 보스는 3페이즈 순환 + HP 기반 가속의 조합으로 적은 코드로 깊이 있는 전투를 만들 수 있다. **주의점:** 게임 상태가 6종 이상으로 늘어나면 각 상태에서 돌아야 할 시스템(tween, physics)을 빠뜨리기 쉽다 — 매트릭스 필수.
- **전략/타워디펜스 [Cycle 3]**: 타워 디펜스의 핵심 재미는 "제한된 자원으로 최적 배치를 찾는 퍼즐적 쾌감"과 "파워 성장의 판타지"이다. 3종 타워(빠른 단일/범위 감속/폭발 광역)의 상성 삼각형은 적은 종류로 깊은 전략을 만든다. S자 고정 경로 + First 타겟팅(웨이포인트 인덱스 + 진행률)은 구현이 간단하면서 직관적. 웨이브 스케일링 공식(`min(20, 5+N×1.5)` 적 수, `max(500, 2000-N×80)` 스폰 간격)은 수동 테이블보다 유지보수 용이. **주의점:** 타워 디펜스는 게임 루프 내 시스템(웨이브 매니저, 적 이동, 타워 공격, 투사체, 파티클, 경제)이 슈팅보다 많아 상태 전환 시 경쟁 조건이 발생하기 쉽다. tween 지연 전환에는 반드시 가드 플래그를 걸고, 전환 간 우선순위(GAMEOVER > WAVE_PREP)를 명시할 것. 동적 밸런스 보정(위기 보너스, 숙련자 도전)은 좋은 아이디어지만 실제 동작 검증을 테스트에 포함해야 한다.
- **캐주얼/무한러너 [Cycle 4]**: 무한 러너의 핵심은 "한 판만 더"의 중독성과 "즉각적 반응의 쾌감"이다. 3레인 + 점프라는 단순한 조작이 진입 장벽을 낮추고, 거리 비례 속도 증가(`min(600, 200 + distance × 0.04)`)가 자연스러운 난이도 곡선을 만든다. 절차적 청크 생성(6종 가중 패턴 + 안전 레인 보장)은 패턴 암기가 아닌 순간 판단력 테스트로 리플레이 가치를 높인다. Near Miss(아슬아슬 회피 +30점 + 슬로모)는 적은 구현 비용으로 긴장감·보상감을 크게 올리는 범용 메커니즘. **주의점:** TweenManager의 cancelAll 직후 add 시나리오를 반드시 테스트할 것 — deferred 패턴의 맹점이 게임 시작 불가 CRITICAL 버그를 유발했다. 또한 "100% Canvas 드로잉" 원칙을 세웠으면 에셋 생성 단계까지 자동 검증이 필요하다.
- **리듬퍼즐/캐주얼 [Cycle 5]**: 리듬 퍼즐의 핵심 재미는 "퍼즐을 풀면 음악이 만들어진다"는 인과 쾌감이다. 4×8 그리드 + 5종 음표(C메이저 스케일) + 3매치 룰의 조합은 테트리스의 배치 전략과 매치3의 정렬 쾌감을 자연스럽게 결합한다. Web Audio API의 `sfxClearChord()`로 매치된 음계의 화음을 동시 연주하고, 체인마다 옥타브 위 + 베이스를 겹쳐 풍성해지는 사운드 디자인이 시각+청각 동시 보상을 만든다. C메이저 스케일 내 음만 사용하면 불협화음이 배제되어 안전하다. 레벨별 음표 풀 확장(3→4→5종)은 적은 코드로 자연스러운 난이도 곡선을 만든다. 동적 밸런스 보정(위기 시 매치 유리 블록 +50%, 연속 미매치 시 강제 보정)으로 "불가능한 상황"을 방지한다. 5종 블록의 색상+도형+글자 3중 구분은 색각이상 접근성 확보의 좋은 사례. **주의점:** 4열 그리드에서 3매치 빈도를 정밀 밸런싱하려면 실플레이 데이터가 필요하다. headless 테스트로는 밸런스 체감을 검증할 수 없다.
- **물리퍼즐/캐주얼 [Cycle 6]**: 미니 골프의 핵심 재미는 "궤적 예측의 전략적 쾌감"과 "물리 피드백의 직관성"이다. 드래그 에이밍 + 점선 궤적(1차 반사까지)으로 "이 각도로 치면 들어간다"를 계획하는 당구적 사고가 발생하고, 법선 반사·마찰·에너지 손실이 현실 물리를 직관적으로 체감하게 한다. 10레벨 점진적 요소 도입(빈 공간→벽→모래→물→움직이는벽→포탈→복합)은 매 레벨 새 메커닉을 학습하는 완만한 곡선을 만든다. 별 3개 시스템(Par 기준)과 홀인원 보너스(+500)가 리플레이 동기를 강력히 부여한다. 포탈 쿨다운(30프레임)은 무한 루프 방지에 효과적이고, 움직이는 벽의 sin 왕복은 예측 가능한 타이밍 퍼즐로 작동한다. **주의점:** 물리 가속(Space 4배속) 시 서브스텝 없이 반복 루프로 충돌을 처리하면 고속에서 관통이 발생할 수 있다. 레벨 데이터 하드코딩은 확장성 제약 — 레벨이 20개 이상이면 JSON 분리 검토. 전역 상태(`ball` 직접 참조)를 파라미터화하면 테스트 가능성이 높아진다.
- **액션/서바이버 [Cycle 7]**: 뱀서라이크 서바이버의 핵심 재미는 "빌드 크래프팅(매 레벨업 스킬 선택 → 매 판 다른 조합)"과 "성장의 판타지(1발 → 화면 가득 탄막)"이다. 12종 스킬(공격 6/유틸 3/방어 3) 풀에서 3개 랜덤 제시 + 최대 레벨 제외로 자연스러운 빌드 다양성이 발생한다. 자동 공격은 "이동에만 집중"이라는 간결한 조작으로 진입 장벽을 낮추면서, 적 틈새를 찾아 빠져나가는 마이크로 플레이에서 깊이를 확보한다. `getWaveConfig(wave)` 공식 기반 스케일링으로 20웨이브 밸런스를 상수 조정만으로 제어. 콤보 시스템(2초 타임아웃 ×3.0 최대)은 공격적 플레이를 보상하는 간단하면서 효과적인 메커니즘. 시드 기반 일일 챌린지(`seededRng()` LCG)로 서버 없이 비동기 경쟁감 추가 가능. ObjectPool 4종(800개)이 대량 엔티티 동시 관리에 필수. **주의점:** 순수 함수 원칙을 세워도 일부 함수에서 위반이 발생하므로 전수 검증이 필요하다. 에셋 재발 문제는 기획서·스크립트 레벨 대응의 한계를 확정 — CI 강제가 유일한 해결책.
- **경영/타이쿤 [Cycle 8]**: 하이브리드 캐주얼 타이쿤의 핵심 재미는 "수동 제조의 쾌감(재료 순서 맞추기 → 팁 보너스)"과 "경영 성장의 판타지(노점→프랜차이즈)"이다. 고객 4타입(일반/급한/VIP/단골)의 인내심·팁 배율 차이가 "누구를 먼저 서빙할까"라는 마이크로 의사결정을 유발하고, 바리스타 AI 자동 제조와 플레이어 수동 제조의 공존이 "자동화해도 수동이 더 이득"이라는 설계를 유지한다. Day 기반 30일 진행(하루 30초)은 세션 길이를 15~20분으로 통제하면서, 매일 정산→업그레이드→다음 날의 루프가 "한 번만 더" 중독성을 만든다. 아이들 매장(2·3호점)의 수동 대비 낮은 수익률이 "플레이어 직접 운영의 가치"를 보존한다. CONFIG 객체 1:1 매핑 + §13.5 수치 검증 테이블로 기획-구현 정합성 100% 달성. **주의점:** 경영 게임은 시스템 수(고객/제조/바리스타/경제/만족도/매장확장)가 많아 상태×시스템 매트릭스가 필수. PAUSED 같은 "즉각 전환" 상태에서 beginTransition() 원칙 위반이 발생하기 쉬우므로, 예외 정책을 사전에 정의할 것.
- **로그라이크/덱빌딩 [Cycle 10]**: 턴제 덱빌딩의 핵심 재미는 "매 턴 손패 5장 중 최적 조합을 고르는 의사결정"과 "덱 성장의 빌드업 쾌감"이다. 카드 18종(공격/방어/스킬)의 조합 공간이 리플레이 가치를 만들고, 3층 맵(전투/상점/이벤트/휴식/엘리트/보스)의 경로 선택이 전략적 깊이를 더한다. 턴제라는 특성이 기술적으로 큰 이점을 준다 — setTimeout 유혹이 구조적으로 낮고, 프레임 단위 충돌 판정이 불필요하며, 모바일 터치가 카드/버튼 탭만으로 자연스럽게 작동한다. 12개 상태 머신(TITLE→MAP→PRE_BATTLE→PLAYER_TURN→ENEMY_TURN→REWARD→SHOP→REST→EVENT→VICTORY→DEFEAT)의 복잡도가 실시간 게임보다 높으므로 `enterState()` 일원화가 필수. **주의점:** 덱빌딩 밸런스(카드 시너지, 난이도 곡선)는 headless 테스트로 검증 불가 — 실플레이 데이터 수집 체계가 필요하다. 함수 시그니처 변경 시 모든 호출부 + 하위 함수 전파를 전수 검증해야 수정 회귀를 방지할 수 있다.
- **플랫포머/정밀액션 [Cycle 11 mini-platformer]**: 정밀 플랫포머의 핵심 재미는 "어려운 구간을 정밀 조작으로 돌파하는 성취감"과 "즉사→즉시 리스폰의 중독 루프"이다. 코요테 타임(6프레임), 점프 버퍼링(6프레임), 코너 보정(4px), 가변 점프 높이 등 관대한 입력 보정이 "조작이 내 의도대로 된다"는 체감을 만드는 핵심이다. AABB 충돌의 X→Y 분리 판정으로 정밀 타일맵 충돌을 달성하고, 월드별 무브셋 순차 해금(W2: 벽점프, W3: 이중점프, W4: 대시)이 점진적 학습 곡선을 만든다. lerp 카메라 + look-ahead + 흔들림 + 경계 clamping 조합의 카메라 시스템이 빠른 이동에서도 시야 안정성을 보장한다. 25개 수작업 레벨(문자열 기반)은 양적 충분성과 다양한 장애물(가시 4방향, 용암, 화염기둥, 레이저, 바람, 무너지는/이동 발판, 중력 반전 존)로 질적 다양성을 모두 달성. **주의점:** 레벨 데이터 하드코딩은 25개까지는 관리 가능하나, 그 이상이면 JSON 분리 또는 프로시저럴 생성 검토. 일일 챌린지 같은 부가 모드는 코어 메커닉 구현 후 별도 이터레이션으로 분리하는 것이 현실적이다.
- **아이들/방치형 [Cycle 11]**: 아이들 게임의 핵심 재미는 "자동화 비율을 높여가는 최적화 쾌감"과 "프레스티지로 리셋 후 더 빠른 성장을 체감하는 루프"이다. 채굴→제련→판매의 3단계 파이프라인이 자원 흐름의 시각적 이해를 돕고, 특수 업그레이드 8종 + 프레스티지 업그레이드 6종이 장기 목표를 제공한다. 아이들 장르만의 기술적 특성: (1) UI 상태(탭 전환)와 무관하게 생산 시스템이 항상 동작해야 하므로 상태×시스템 매트릭스에 "백그라운드 시스템" 개념 필요, (2) 오프라인 진행(경과 시간 × 생산율) 계산이 정확해야 함, (3) 자동 저장 주기(30초)와 프레스티지 시점 저장이 데이터 유실 방지에 핵심. 4탭 UI(생산/업그레이드/프레스티지/통계) + 스와이프 전환 + 롱프레스 연속 구매가 모바일 아이들 UX의 정석 패턴. **주의점:** 업그레이드 비용-효과 곡선의 밸런스는 headless 테스트로 검증 불가 — N회 자동 시뮬레이션 없이는 "함정 업그레이드"나 "최적 경로 고정" 문제를 잡기 어렵다. 변수 초기화 순서(let/const TDZ)에 특히 주의 — 아이들 게임은 초기화 시점에 캐시·상태·UI를 한꺼번에 세팅하므로 의존성 순서가 복잡해지기 쉽다.

- **아이들/농장시뮬 [Cycle 13 mini-idle-farm]**: 아이들 농장의 핵심 재미는 "탭→수확의 즉각적 쾌감"과 "자동화 비율을 높여가는 최적화 전략"의 이중 루프이다. 3단계 농장 확장(밭→목장→가공)이 단계별 새 자원 해금으로 성장의 마일스톤을 체감하게 하고, 프레스티지 6종 영구 업그레이드가 장기 목표를 유지한다. Cycle 11 아이들 파이프라인 + Cycle 8 타이쿤 경제의 결합이 성공적으로 동작함을 확인. 10종 자원(밀~스웨터)의 성장시간/가격 곡선이 "다음 자원 해금까지 적절한 대기 시간"을 만드는 핵심이다. 오프라인 수입(최대 4시간 × 0.5 배율)이 세션 간 연결감을 유지하고, 세이브 호환성 보정이 데이터 유실을 방지한다. **주의점:** `CONFIG.MIN_TOUCH` 같은 UI 상수는 선언만으로는 부족하고 렌더링 시 강제 적용 로직이 필요. SoundManager setTimeout은 Web Audio 네이티브 스케줄링으로 대체 가능.

- **워드퍼즐 [Cycle 13]**: ⚠️ **구현 미완료로 실전 노하우 없음.** 기획서 설계에서 얻은 이론적 교훈만 기록: (1) 한글 유니코드 자모 분해(`charCode - 0xAC00` 기반 초성/중성/종성 분리)는 외부 라이브러리 없이 순수 JS로 처리 가능한 설계. (2) 이중 피드백(자모 색상 + 음절 테두리)은 Wordle 단일 피드백보다 정보량이 많아 추론 깊이를 더할 수 있는 차별화 요소. (3) 100% Canvas 텍스트 렌더링은 이미지 에셋 0개 원칙과 자연스럽게 양립 — 워드 퍼즐은 "에셋 제로" 달성이 가장 쉬운 장르. (4) 가상 키보드 UI의 터치 타겟 48×48px 확보가 모바일 UX 핵심. **다음 재도전 시 기획서를 그대로 재사용하되, 구현 완료 확인 게이트를 반드시 추가할 것.**

- **레이싱/아케이드 [Cycle 12]**: 탑다운 드리프트 레이싱의 핵심 재미는 "핸드브레이크 → 카운터 스티어 → 코너 탈출"의 물리적 쾌감과 "드리프트 포인트 → 부스트 → 더 빠른 진입"의 리스크-리워드 순환이다. 웨이포인트 배열 기반 트랙 정의 + closestTrackPoint 도로 이탈 판정은 베지어 곡선 수준의 매끄러운 도로를 단순한 데이터로 구현하는 효율적 접근. 5개 테마 트랙(초원/사막/설산/도심/화산)의 고유 노면 효과(모래감속/빙판마찰/물웅덩이 등)가 트랙별 차별화를 만든다. 고스트 시스템(6프레임 간격 기록 → localStorage → 반투명 렌더링)은 멀티플레이어 없이 경쟁감을 추가하는 가성비 높은 패턴. 미니맵(80×80px, 트랙 윤곽 + 플레이어 위치)은 작은 구현 비용으로 공간 인지에 큰 기여. lerp 카메라 + 전방 룩어헤드 + 셰이크 감쇠가 고속 주행의 역동성을 효과적으로 연출한다. ObjectPool(연기 파티클 80개) + 타이어 자국 200개 제한으로 시각 효과와 성능의 균형 달성. **주의점:** 노면 효과를 기획할 때 "A + B" 복합 효과(속도 감소 + 조향 반전)는 절반만 구현되기 쉽다 — 세부 체크리스트로 개별 검증 필요. 트랙 데이터가 JS 배열 하드코딩이므로 10개 이상이면 JSON 분리 검토. 물리 기반 레이싱에서 가속(배속) 기능 시 서브스텝 처리 필요.

- **머지퍼즐/물리 [Cycle 14]**: 수박 게임(Suika) 스타일 머지 퍼즐의 핵심 재미는 "같은 과일끼리 합체 → 연쇄 반응의 카타르시스"와 "용기가 차오르는 공간 관리 긴장감"이다. 11단계 과일 진화 체계(체리 12px → 수박 82px)는 시각적 크기 차이만으로 진행감을 체감하게 하며, "어디에 떨어뜨릴까"라는 단일 의사결정에 모든 전략이 집중되는 구조가 높은 접근성과 깊은 재미를 동시에 달성한다. 물리 엔진은 서브스텝 3회 + 원-원 충돌 분리 벡터 + 탄성 계수(0.3) + 마찰(0.1) + 감쇠(0.98) 조합으로 외부 라이브러리 없이 자연스러운 물리 피드백 구현이 가능하다. 드롭 쿨다운(0.5초)과 Next 프리뷰가 전략적 깊이를 더하고, 데드라인 3초 유예가 "위기→탈출"의 드라마를 만든다. 드롭 가능 과일을 0~4단계로 제한하고 점수 구간별 확률을 조절하는 동적 난이도가 자연스러운 긴장감 상승을 만든다. **주의점:** 이벤트 리스너 등록은 반드시 canvas DOM 할당 이후(init() 내부)에 수행할 것 — Cycle 14에서 이 순서 위반이 CRITICAL 게임 로드 실패를 유발. 합체 시 파티클(8~12개, 0.3초 수명)과 Web Audio 사운드를 동시 연출하면 쾌감이 배가될 것으로 예상(이번 사이클에서는 사운드 미구현).

- **턴제 로그라이트/주사위 [Cycle 14 mini-rogue-dice → mini-dungeon-dice]**: 턴제 주사위 로그라이트의 핵심 재미는 "주사위 결과를 어디에 배치할지"라는 **자원 배분 의사결정**과 "층 클리어 보상으로 주사위가 강해지는" **성장 쾌감**이다. 공격·방어·회복·만능 4종 주사위를 3개 슬롯에 배치하는 단순한 의사결정 구조가 높은 접근성과 깊은 전략을 동시에 달성한다. 5층 17전투(일반→엘리트→보스 구성)가 적절한 세션 길이(3~5분)를 보장하고, 재굴림(턴 1회, 층 2회)이 운과 전략의 균형을 만든다. 보스 3페이즈는 적은 코드로 긴장감 곡선을 효과적으로 형성. 9개 상태 머신이 STATE_PRIORITY + TransitionGuard로 안정 동작 — 턴제는 상태 전환 시점이 명확하여 경쟁 조건이 구조적으로 적다. 에셋 하이브리드 전략(보스/플레이어=SVG, 일반 적=프로시저럴 도형) + 완벽한 폴백으로 시각적 위계와 견고함을 동시 달성. touchSafe()/MIN_TOUCH_TARGET 직접 참조 패턴으로 터치 타겟 문제를 구조적으로 해결한 첫 사례. **주의점:** 밸런스(주사위 4종×적 편성×보상 조합)는 headless 테스트로 검증 불가 — 자동 시뮬레이션 도구 필요. 재기획 시 사이클 자원이 크게 소모되므로, 스모크 테스트 게이트 자동화로 "수정 1건 CRITICAL"을 재기획 없이 복구하는 경로 확보가 필수.

- **낙하블록/헥사곤퍼즐 [Cycle 16, 3차 APPROVED]**: 헥사곤 낙하 블록의 핵심 재미는 "6방향 동시 낙하를 회전 하나로 제어하는 순간 판단"과 "의도치 않은 연쇄 소멸의 럭키 모먼트"이다. Hextris 스타일의 pointy-top 헥사곤 + 6변 스택 구조는 기존 사각형 그리드 퍼즐과 완전히 다른 좌표계를 요구하며, BFS 기반 `findMatches()` 인접 3+ 그룹 탐색 + `applyGravity()` + 재귀 `resolveStep()` 연쇄 처리가 핵심 알고리즘이다. 좌우 탭 60도 회전이라는 단일 조작이 극도로 낮은 진입 장벽을 만들면서, 레벨별 색상 수 증가(3→6색) + 낙하 속도 가속(2초→0.4초)이 자연스러운 난이도 곡선을 형성한다. 한 판 1~3분 세션 + 즉시 재도전 구조가 "한판만 더" 중독 루프를 완성. 회전 tween(150ms easeOutCubic) + `isRotating` 가드로 스냅 회전에 부드러운 물리감을 부여하는 것이 조작감의 핵심이다. 순수 함수 12개(`hexVertex`, `sideVertices`, `blockVertices`, `blockCenter`, `getNeighbors`, `findMatches`, `rotateGridData`, `applyGravity`, `checkGameOver`, `calcScore`, `getDropInterval`, `getActiveColors`)가 전부 파라미터 기반 부작용 없음으로 구현. **주의점:** 기획서 색상 배열과 코드 색상 배열의 불일치가 발생하기 쉬우므로, 수치 정합성 검증에 열거형 데이터를 포함해야 한다. 블록 최대 4층 + 한계선(반지름 ×2.5)의 게임오버 판정 밸런스는 실플레이 검증이 필수. 입력 수단별 기능 커버리지(키보드/터치/마우스)를 기획서에 매트릭스로 포함하면 마우스 하드드롭 미지원 같은 미세 UX 누락을 사전에 방지 가능.

- **벽돌깨기/브레이크아웃 [Cycle 17]**: 벽돌깨기의 핵심 재미는 "패들 히트 위치에 따른 반사 각도 정밀 컨트롤"과 "연쇄 벽돌 파괴의 네온 파티클 쾌감"이다. AABB closest point 기반 볼-벽돌 충돌 + 패들 오프셋 반사 각도가 "의도한 방향으로 공을 보내는" 컨트롤감의 핵심이며, 프레임당 벽돌 1회 충돌 제한이 관통 버그를 방지한다. 4종 파워업(WIDE/MULTI/LASER/LIFE)은 "잡을까 공에 집중할까"라는 순간 판단 + 콤보 시스템(3연속 임계값)이 공격적 플레이 보상으로 작동한다. HP별 벽돌 색상(1/2/3 + 불멸 회색)이 시각적 우선순위를 만들고, 6레벨 고유 배치가 진행감을 제공한다. ObjectPool(파티클 60개) + offscreen canvas 배경 캐싱이 실시간 파티클 연출의 성능을 보장. F1~F26 역대 최다 피드백 매핑으로 코드 리뷰 전항 PASS + 배포 차단 0건 달성. **주의점:** 불멸 벽돌과 파워업 간 상호작용 매트릭스를 기획서에 사전 정의해야 모호한 동작을 방지할 수 있다. 밸런스(레벨별 벽돌 배치 × 파워업 드롭률)는 headless 테스트로 검증 불가 — 자동 시뮬레이션 도구 필요.

- **Match-3 퍼즐 [Cycle 15]**: Match-3의 핵심 재미는 "연쇄 폭발(Cascade)의 쾌감"과 "특수 보석 전략의 깊이"이다. 8×8 그리드 + 6색 보석 + 스와이프 교환이라는 직관적 조작이 진입 장벽을 낮추면서, 4매치(라인 폭탄)/L·T매치(범위 폭탄)/5매치(색상 폭탄) 특수 보석의 생성과 조합이 전략적 깊이를 만든다. 30스테이지를 점수 달성(1~10)/색상 제거(11~20)/특수 보석 생성(21~30) 3유형 목표로 구성하면 단조로움을 방지하고 리플레이 동기를 부여한다. Fisher-Yates 셔플 + 데드록 검사 + 자동 셔플이 "불가능한 보드 상태"를 원천 방지하는 필수 안전장치. 연쇄 처리에 `isProcessing` 가드 플래그 필수 — tween 기반 낙하 + 재매칭 파이프라인에서 콜백 반복 호출이 발생하기 쉽다. offscreen canvas 배경 캐시(`bgCache`)가 매 프레임 보드 재드로잉 비용을 크게 절감. `LEVEL_OBJECTIVES[N]` 배열 기반 목표 분리 + `isObjectiveComplete()` 판정 함수가 목표 유형 확장을 용이하게 한다. 한 판 1~2분 + ★ 등급 시스템(잔여 이동 수 기반 1~3스타)이 "한판만 더" 중독 루프를 완성. **주의점:** 밸런스(스테이지별 이동 수/목표 수치/특수 보석 생성 확률)는 headless 테스트로 검증 불가 — 자동 시뮬레이터(랜덤 스와이프 N회 런)가 필요. 사운드 8종의 체감 품질(타이밍·볼륨·쾌감)도 코드 존재만으로는 미검증.

- **로그라이크 포지 디펜스 [Cycle 17 arcane-bastion]**: ⚠️ **구현 미완료로 실전 노하우 없음.** 기획서 설계에서 얻은 이론적 교훈만 기록: (1) 이중 전투(직접 주문 + 타워 자동 공격)는 "내가 어디를 막고 타워에 어디를 맡길까"라는 실시간 판단이 핵심 재미의 축이 될 수 있으나, 구현 복잡도가 매우 높다. (2) 마나의 이중 용도(주문 vs 타워 건설)는 공격/투자 딜레마를 만드는 좋은 설계이나, 밸런스 검증 없이는 한쪽이 압도적으로 유리해질 위험. (3) 5적종×3타워×3보스×3바이옴×로그라이크 업그레이드의 조합 공간이 단일 사이클에 비현실적. **다음 재도전 시 적 1종 + 타워 0개 + 웨이브 3개의 MVP 먼저, 기능은 점진적 추가.** (4) action+strategy 하이브리드는 "범위 축소 전략"과 구조적으로 충돌 — 두 장르의 핵심 메카닉을 모두 구현해야 최소 재미가 성립하므로, 단일 장르보다 MVP 바닥이 넓다.

- **로그라이크/덱빌딩 전략 [Cycle 19, Cycle 10 확장]**: Slay the Spire 스타일 턴제 덱빌더의 핵심 재미는 "매 턴 마나 3으로 손패 5장 중 최적 조합을 고르는 의사결정"과 "덱 성장 + 유물 시너지의 빌드업 쾌감"이다. Cycle 10(카드 18종/3층 맵)에서 진화하여 **카드 20종 × 원소 상성(ELEM_MULT 4원소×적타입 1.5배) × 유물 8종 × 보스 3체 × 업적 10종**으로 전략적 깊이를 대폭 확대. 인텐트 시스템(적 다음 행동 미리 표시)이 "운 기반"이 아닌 "정보 기반 의사결정"으로 게임을 격상시키는 핵심 메카닉. 10개 상태(TITLE/MAP/BATTLE/BOSS_INTRO/REWARD/SHOP/REST/EVENT/RESULT/GAMEOVER)가 STATE_PRIORITY + 4중 가드로 안정 동작. 턴제 특성상 setTimeout 유혹이 낮고, 모바일 탭 조작이 카드/버튼에 완벽히 적합. 상점(구매/제거/업그레이드) + 휴식(HP 회복/카드 업그레이드) + 이벤트(리스크-리워드)의 비전투 노드가 경로 선택에 전략적 의미를 부여. **주의점:** (1) 적 디버프 시스템은 대상 객체 참조의 정확성이 핵심 — `applyStatus({}, ...)` 빈 객체 대입 버그로 전체 디버프가 무효화됨. (2) 카드 20종 × 유물 8종 × 적 7종 밸런스는 코드 리뷰만으로 검증 불가 — AI 랜덤 카드 사용 N회 런 시뮬레이터가 필요. (3) 덱 보기 UI와 카드 스와이프 같은 보조 기능을 MVP에서 누락하기 쉬우므로 입력 스펙 §3을 개별 체크리스트로 분리할 것.

- **액션/서바이버라이크 [Cycle 18, Cycle 7 확장]**: 뱀서라이크 로그라이크 서바이버의 핵심 재미는 "매 레벨업 3택 무기 선택 → 매 판 다른 빌드"와 "1발 → 화면 가득 탄막"의 성장 판타지이다. Cycle 7(12스킬 풀/20웨이브/시드 기반 일일 챌린지)에서 진화하여 **DMG_TABLE 상성 시스템(5무기×6적타입 배율)** + 보스 2체 + 업적 8종 + 10웨이브 구조로 전략적 깊이를 확대했다. 자동 공격 + 이동만 조작 구조는 캐주얼 접근성을 유지하면서, 상성 테이블이 "어떤 무기를 키울까"라는 의사결정에 무게를 부여한다. **6종 ObjectPool(enemy/proj/particle/gem/popup/bullet)** 이 서바이버 장르 특유의 동시 수백 엔티티 관리에 필수적이며, **timeScale 분리**로 게임 시간만 감속하는 슬로우모션이 보스 등장/사망 연출에 극적 효과를 만든다. WAVE_TABLE 기반 10웨이브 + 엘리트 배치 + 보스 웨이브(5/10) 구성은 "점진적 압도 → 보스 스파이크 → 달성감" 곡선을 효과적으로 형성. 업적 8종(ACH_LIST + localStorage)이 반복 플레이 동기를 추가. MVP 6단계(3상태→레벨업→5적5무기→보스→업적→사운드) 점진 확장 전략이 Cycle 17의 "구현 0%" 참사를 극복한 핵심 전략. **주의점:** (1) 5무기×5적×10웨이브 밸런스 검증이 코드 리뷰만으로는 불가능 — headless 자동 플레이 시뮬레이터 필요. (2) 상성 테이블 수치 조정은 실플레이 데이터 없이는 "특정 무기 압도" 또는 "후반 불가능" 문제를 사전에 잡을 수 없다. (3) 2,757줄 단일 파일은 유지보수 한계 — 공용 엔진 분리 시급.

- **물리 아케이드/핀볼 [Cycle 20]**: 핀볼의 핵심 재미는 "플리퍼 히트 → 볼 반사 → 범퍼/타겟 파괴 → 파티클 폭발 + 사운드"의 0.1초 미만 쾌감 루프와 "테이블 공략 + 업그레이드 성장"의 장기 목표이다. 4단계 서브스텝(`SUB_STEPS: 4`)으로 고속 볼 터널링을 방지하고, 선분-원 충돌(볼-플리퍼) + 원-원 충돌(볼-범퍼/크리스탈)을 조합하여 다양한 오브젝트 상호작용을 처리. `resolveCircleCollision()` 순수 함수 패턴이 물리 엔진 코드의 테스트 가능성과 재사용성을 보장한다. 10개 테이블을 TABLE_DATA 배열로 관리하며 테이블별 범퍼/타겟/클리어 조건을 데이터 주도로 차별화 — Cycle 7의 선언적 웨이브 config 패턴이 핀볼 테이블에서도 유효. 콤보 시스템(1~4: ×1, 5~9: ×2, 10~19: ×3, 20+: ×5)이 연속 히트를 보상하고, 멀티볼(롤오버 레인 3개 완성 시 발동)이 카타르시스 모먼트를 만든다. `buildTableCache()` offscreen canvas 캐싱이 테이블 정적 요소의 매 프레임 재드로잉을 제거하여 성능 보장. 10개 상태(TITLE/TABLE_SELECT/PLAYING/PAUSED/UPGRADE/DRAIN/BOSS_INTRO/TABLE_CLEAR/RESULT/GAMEOVER)가 STATE_PRIORITY + 3중 가드로 안정 동작. 모바일은 멀티터치 + 영역 분할(좌40%=왼쪽 플리퍼, 우60%=오른쪽 플리퍼, 우하단=플런저)으로 가상 버튼 없이 양손 동시 조작 구현. **주의점:** (1) 터치 영역이 비가시적이므로 첫 플레이 시 힌트 오버레이 필수. (2) 업그레이드 상점 등 스크롤 UI는 wheel + touchmove + 마우스 드래그 3가지 입력을 모두 커버해야 모바일 완전 지원. (3) hitAnim 같은 시각 상태를 render()에서 갱신하면 프레임 의존적이므로 update()에서 dt 기반 처리할 것. (4) 핀볼 밸런스(테이블별 범퍼 배치, 크리스탈 HP, 보스 난이도)는 코드 리뷰만으로 검증 불가 — 자동 시뮬레이터 도입 검토.

- **퍼즐+전략 [Cycle 21]**: 룬포지 택틱스의 핵심 재미는 "퍼즐 페이즈(배치 사고) → 방어 페이즈(실행 관찰)"의 2페이즈 리듬 전환과 "이 조합으로 뭐가 나올까?"라는 실험적 쾌감이다. 5×5 그리드에 8종 룬을 배치하여 120+ 마법진 패턴을 완성하는 `scanMagicCircles()` 패턴 매칭 알고리즘이 핵심 — 직선3·L자·T자·십자·2×2·대각선 등 다양한 패턴을 실시간 스캔하여 룬 속성 조합에 따라 상이한 마법 효과를 발동. 영구 레시피북 수집("실패해도 성장")이 로그라이크적 리플레이 동기를 제공한다. 5지역 × 20+ 스테이지 + 5보스 + 영구 업그레이드로 프리미엄 콘텐츠 볼륨 달성. ScrollManager(momentum + bounce)로 업그레이드/레시피 화면 터치 스크롤을 네이티브 앱 수준으로 구현. 12상태 머신(역대 최다 타이)이 STATE_PRIORITY + ESCAPE_ALLOWED(6개 역방향 전환)로 안정 동작. Ko/En 이중 언어 LANG 객체로 완전 로컬라이제이션. **주의점:** (1) 120+ 마법진 × 8종 룬 × 5지역 밸런스는 코드 리뷰만으로 검증 불가 — 자동 배치 시뮬레이터 필요. (2) 퍼즐+전략 하이브리드는 단일 장르 대비 상태 수가 많아지므로(12상태) 상태×시스템 매트릭스 필수. (3) 콘텐츠 볼륨이 많으면 코드 규모도 폭증(4,215줄 역대 최대) — 공용 엔진 분리 없이는 유지보수 한계. (4) 다중 이해관계자(플래너·디자이너) 피드백은 리뷰 전에 통합하여 리뷰 사이클 연장을 방지할 것.

- **하이브리드 타워 디펜스/시간 조작 [Cycle 22, Cycle 3 확장]**: 시간 마법 타워 디펜스의 핵심 재미는 "타워 배치의 전략적 사고"와 "시간 마법 시전의 실시간 아케이드 긴장감"의 이중 루프이다. Cycle 3(3종 타워/S자 경로/First 타겟팅)에서 진화하여 **7종 타워 × 5시대 × 시간 마법 3축(감속/가속/역전) × 난이도 3종 × 보스 5체 × 영구 업그레이드**로 전략적 깊이를 대폭 확대. 시간 에너지 관리("지금 쓸까 아낄까")가 매 웨이브의 핵심 의사결정 축이다. 14상태 머신(TITLE~TUTORIAL, 역대 최다)이 STATE_PRIORITY + ESCAPE_ALLOWED + beginTransition 조합으로 무결 동작. timeScale 분리로 마법 발동 시 게임 시간만 변조하고 UI/사운드는 실시간 유지하는 구조가 Cycle 18 서바이버라이크 슬로우모션의 타워 디펜스 확장 성공 사례. 오프스크린 캐싱 4중 구조(배경/그리드/아이콘/HUD)가 복잡한 전장 렌더링의 성능을 보장. ScrollManager + 콤보 시스템 + 시대별 고유 환경 효과가 반복 플레이 동기를 강화. **주의점:** (1) 5시대×4스테이지×3난이도×7타워×3마법의 방대한 조합 공간은 코드 리뷰만으로 밸런스 검증 불가 — 자동 시뮬레이터 필수. (2) menuY 같은 반응형 레이아웃 이슈는 다양한 뷰포트에서의 UI 스모크 테스트를 기획에 포함해야 한다. (3) 하이브리드 장르(arcade+strategy)는 양 장르의 핵심 메카닉을 모두 구현해야 최소 재미가 성립하므로 MVP 바닥이 넓다 — 리뷰 라운드 증가를 감안한 일정 필요. (4) 14상태는 복잡도가 높으므로 상태×시스템 매트릭스를 반드시 기획서에 포함하고, PAUSED 즉시 전환 예외 정책을 사전에 명확히 정의할 것.

- **퍼즐 메트로이드바니아 [Cycle 25, Cycle 1 퍼즐 확장]**: 퍼즐 메트로이드바니아의 핵심 재미는 "새 능력으로 이전에 지나친 곳의 비밀을 발견하는 아하! 모먼트"와 "환경 퍼즐+전투 결합 보스전의 패턴 관찰→환경 활용→타격 사이클"이다. Cycle 1(5×5 퍼즐)에서 진화하여 **5바이옴×26방×20글리프 조합(5기본+15조합)×5보스×히든 바이옴×3난이도×DDA×콤보**로 전략적 깊이를 대폭 확대. 글리프 조합 시스템("이 조합이면 저기 갈 수 있지 않을까?")이 탐험 동기의 핵심 축이다. SeededRNG 프로시저럴 방 배치 + BFS 도달 가능성 검증(F54)으로 매 플레이 다른 미궁을 생성하면서도 "불가능한 맵"을 원천 차단. 5개 바이옴의 고유 색상 팔레트(화염 오렌지레드, 빙결 딥스카이블루, 고대숲 라임그린, 심연 보라, 천공 금빛)를 순수 Canvas 코드 드로잉으로 표현하여 에셋 없이도 충분한 시각적 차별화를 달성. 세이브 포인트 시스템(바이옴 입구 자동 저장, 글리프/능력 영구 보존)으로 "진행 유실 없는 도전"이라는 메트로이드바니아 필수 경험을 보장. 모바일 9개 터치 버튼(조이스틱+공격+상호작용+일시정지+대시+글리프 슬롯 5개)으로 키보드 없이 완전한 플레이 가능. **주의점:** (1) 5바이옴×5보스×20글리프 조합의 밸런스는 코드 리뷰만으로 검증 불가 — 글리프 조합별 보스 공략 자동 시뮬레이터 필수. (2) 메트로이드바니아는 "역방향 탐험"이 핵심이므로 BFS 도달 가능성 검증 외에 "글리프 X 획득 전 Y 영역 접근 불가" 같은 능력 기반 접근 제어의 자동 검증도 필요. (3) 26방 규모의 프로시저럴 맵은 퍼즐 배치 품질(도달 가능성 외의 "재미있는 퍼즐")을 자동 검증하기 어려움 — 퍼즐 품질 메트릭 개발 필요. (4) 모바일 글리프 슬롯 5개가 화면 공간을 차지하므로 소형 디스플레이(≤400px)에서의 UI 레이아웃 검증 필수.

- **액션+캐주얼 이중 페이즈 서바이벌 [Cycle 24]**: 이중 페이즈 서바이벌의 핵심 재미는 "낚시·채집의 이완감"과 "괴수 전투의 긴장감"이 조수(Tide) 단위로 교차하며 리듬을 만드는 것이다. 캐주얼 페이즈(60초)에서 자원을 모아 등대를 업그레이드하고, 액션 페이즈(90초)에서 그 업그레이드의 효과를 체감하는 "투자→회수" 루프가 핵심. 3종 무기(작살/음파포/그물)의 차별화된 물리와 3단계 난이도 + 동적 난이도 조정이 리플레이 가치를 높인다. 16개 게임 상태를 ACTIVE_SYSTEMS 매트릭스로 관리하면 이중 페이즈의 복잡한 상태 전환(캐주얼→전투→보스→결과→업그레이드→다음 조수)도 경쟁 조건 없이 처리 가능. SeededRNG 프로시저럴 콘텐츠(파도 패턴, 어획물, 괴수 스폰)가 "매번 다른 경험"을 보장하면서도 일일 챌린지 등 동기식 경쟁에 활용 가능. 바이올루미네센스 파티클 + 등대 조명 확산 + sin 파도 3겹의 Canvas 코드 드로잉이 심해 분위기를 효과적으로 연출. RESTART_ALLOWED 패턴을 기획서 §6.1에 선언하여 P0 상태 전환 버그를 1차에서 해결한 최초 성공 사례. **주의점:** (1) 이중 페이즈 하이브리드는 양 장르의 핵심 메카닉(낚시 타이밍+전투 물리)을 모두 구현해야 최소 재미가 성립하므로 MVP 바닥이 넓다 — Phase 구현 순서를 엄격히 지켜 Phase 1만으로 완전한 게임 제공 필수. (2) 15조수×3난이도×3무기 조합의 밸런스는 코드 리뷰만으로 검증 불가 — DPS/EHP 자동 시뮬레이터 필수. (3) 에셋 코드(ASSET_MAP/SPRITES)와 외부 CDN(Google Fonts)이 동시에 잔존할 수 있으므로 제거 체크리스트를 에셋+CDN+주석 3단으로 확장할 것. (4) 캐주얼→액션 전환 시 하늘 그라디언트 tween과 시스템 활성화 전환이 동기화되어야 UX가 매끄럽다.

- **로그라이크 타워 디펜스/테트로미노 [Cycle 26, Cycle 3/22 확장]**: 테트로미노 타워 디펜스의 핵심 재미는 "블록 배치가 곧 경로 설계"라는 공간 퍼즐 쾌감과 "원소 상성 × 로그라이크 카드 × 보스 약점 퍼즐"의 삼중 전략 축이다. Cycle 3(3타워/S자 경로)과 Cycle 22(7타워/5시대/시간 마법)에서 진화하여 **7종 테트로미노 × 5원소(fire/ice/poison/lightning/void) × 12로그라이크 카드 × 8적종 × 6보스(5+히든) × 3난이도 × 영구 업그레이드 3트리**로 전략적 깊이를 대폭 확대. BFS 경로 검증이 `canPlace()` tmpGrid 체크에서 "경로가 막히는 배치"를 원천 차단하면서 배치 자유도를 보장하는 핵심 기술. 보스별·페이즈별 특정 타워 배치 패턴에서만 약점이 노출되는 공간 퍼즐 전투가 "올바른 위치에 올바른 타워"라는 타워 디펜스의 새로운 재미 축을 개척. ELEM_STRONG 순환 + 시너지 보너스(1~3연결)가 빌드 다양성을 확보. 18상태 × 12시스템 ACTIVE_SYSTEMS 매트릭스(역대 최다)가 배치→웨이브→보스→로그라이크 선택→차원 전환의 복잡한 루프를 무결하게 관리. DDA(3연속 피격→적 HP -15%, 무손상→적 수 +20%)가 밸런스 자동 보정에 효과적. 차원별 배경 이펙트(용암/눈/독안개/번개/왜곡) + 카메라 연출이 에셋 0건으로 높은 시각 품질 달성. **주의점:** (1) 5원소 × 7테트로미노 × 12카드 × 8적종 × 6보스의 방대한 조합 공간은 코드 리뷰만으로 밸런스 검증 불가 — DPS/EHP 자동 시뮬레이터 필수. (2) 보스 약점 패턴이 "올바른 배치 1가지"로 수렴하면 전략 다양성이 줄어들므로, 복수 유효 배치를 허용하는 설계가 바람직. (3) 3,187줄 단일 파일은 유지보수 한계에 근접 — 공용 엔진 분리 시급. (4) Math.random() 혼입 방지를 위해 SeededRNG 사용 정책을 ESLint 규칙으로 강제할 것.

- **매치-3 로그라이크 RPG [Cycle 27, Cycle 15 확장]**: 매치-3 RPG의 핵심 재미는 "캐스케이드 콤보의 시각적 쾌감"과 "원소 상성 기반 스펠 전략"의 이중 축이다. Cycle 15(8×8 그리드/6색/30스테이지 목표 시스템)에서 진화하여 **6원소 상성 매트릭스(ELEM_MATRIX) × 18종 스펠 × 13종 유물(common 6/rare 4/epic 3) × 5지역(환경 효과) × 7보스(히든 포함) × 3단 난이도 × DDA × 영구 업그레이드 3트리**로 전략적 깊이를 대폭 확대. "이번 전투는 어떤 원소 마나를 모아 어떤 스펠로 갈까?"라는 매치마다의 의사결정이 핵심. 매치 검출 우선순위(5→T→L→4→3) + 재귀적 캐스케이드 + `isProcessing` 이중 가드가 매치-3 핵심 알고리즘의 완성형. SeededRNG로 보석 배치·적 스폰·유물 선택 모두 시드 기반 재현 가능. `bossRewardGiven` 플래그 + `checkEnemiesDefeated()` 위임 구조로 보스 보상 단일 경로 확보. 전투 간 3택 유물 + 영구 업그레이드가 로그라이크 빌드 다양성을 제공하고, DPS 캡(200%) + 시너지 캡(150%)으로 극단 빌드의 밸런스 이탈을 제어. hitTest() 단일 함수로 모든 터치/클릭 판정 통합(F60). 적 유형별 고유 비주얼 5종 + 지역별 환경 이펙트 + 보스 줌인 연출을 에셋 0건 Canvas 프로시저럴 드로잉으로 달성. **주의점:** (1) 6원소×13유물×7보스 조합 공간이 방대하여 코드 리뷰만으로 밸런스 검증 불가 — AI 자동 플레이 시뮬레이터 필요. (2) 보스 보상/점수 가산 로직이 여러 함수에 분산되면 이중 지급 버그가 발생하기 쉬우므로 단일 함수 + 플래그 가드 패턴 필수. (3) 4,238줄 단일 파일은 유지보수 한계 — 공용 엔진 분리 시급. (4) DDA가 밸런스 폴백으로 유효하나 근본적인 수치 검증을 대체하지는 못한다.

- **리듬 아케이드 로그라이트 [Cycle 28, Cycle 5 리듬 확장]**: 리듬 아케이드 로그라이트의 핵심 재미는 "비트를 타는 쾌감"(Perfect ±50ms 판정의 프레임 단위 긴장)과 "사운드 칩 빌드업 + 콤보 배율 폭발"의 이중 축이다. Cycle 5(4×8 그리드/5종 음표/Web Audio 비트 스케줄링)에서 진화하여 **BPM 동기화 전투 × 4단 판정(Perfect/Great/Good/Miss) × 5종 비트(일반/더블/홀드/회피/보스) × 5음악 존(Synthwave/Dubstep/Lo-fi/Drum&Bass/Glitch) × 6보스(턴테이블 기반) × 13종 사운드 칩 × 업그레이드 3트리 × 3단 난이도 × DDA**로 전략적 깊이를 대폭 확대. BPM tween 단일 경로(F70)가 보스 페이즈별 템포 변화를 단일 곡선으로 제어하는 핵심 기술. 프로시저럴 BGM 엔진(audioCtx.currentTime 기반 드럼/베이스/멜로디 3트랙 실시간 합성)이 존별·보스별 차별화된 음악 경험을 에셋 0건으로 달성. 홀드 비트(updateHoldBeats: holdDur/holdRatio/isHolding 연동 지속 대미지)와 더블 비트(doublePending + 400ms 자동 Miss)가 기본 탭 공격에 전략적 변주를 더한다. 10 REGION 코드 구조(CONFIG→ENGINE→ENTITY→DRAW→RHYTHM→COMBAT→ROGUE→STATE→SAVE→MAIN)가 3,288줄을 의존 방향 단방향으로 정리하여 추후 모듈 추출 가능. DPS 캡(2.0) + 시너지 캡(1.5) + DDA 3단계 폴백으로 칩 조합 밸런스 안전 장치 확보. 모바일은 탭(공격) + 스와이프(회피) + 일시정지 버튼으로 키보드 없이 전 흐름 조작 가능. **주의점:** (1) BPM×비트유형×칩효과 조합의 밸런스는 리듬 게임의 특성상 "인간이 물리적으로 불가능한 패턴"이 발생할 수 있어 자동 플레이 시뮬레이터가 필요. (2) 홀드/더블 비트의 출현 빈도·난이도 곡선은 실플레이 데이터 없이 설정됨 — 보스별 비트 패턴 구성의 체감 검증 필수. (3) 프로시저럴 BGM이 "음악으로서의 품질"을 보장하려면 음악 이론(코드 진행, 리듬 패턴)의 의도적 설계가 필요 — 랜덤 생성만으로는 한계. (4) 적응형 BGM(플레이어 빌드에 따른 레이어 동적 변화)은 미도달 — GainNode 크로스페이드 기반 구현이 다음 목표.
- **실시간 전술 액션 로그라이트/스팀펑크 [Cycle 31, Cycle 3/22 전략 확장]**: 철갑 선봉대의 핵심 재미는 "3종 유닛 배치 전술 × 실시간 전투 긴장감 × 로그라이트 빌드 성장"의 3축 복합 시스템이다. Cycle 3(3타워/S자 경로)과 Cycle 22(7타워/5시대/시간 마법)의 전략 계보를 이어받되, **타워 고정이 아닌 유닛 이동 지휘**라는 액션 요소를 결합. 3종 클럭워크 유닛(스트라이커/거너/엔지니어) × 14종 블루프린트(common 6/rare 5/epic 3) × 워크샵 3트리 영구 업그레이드(공격/방어/생산) × 5구역별 고유 환경 위험(증기 분출/톱니/용광로/시간 왜곡/에테르 부패, 수치 명시) × 6보스(5존+히든 "클럭워크 아르케") × 3난이도(견습생/기술자/마스터) × DDA 3단계 × 다국어(한/영) × 엔딩 2분기로 전략적 깊이와 리플레이 가치를 확보. ESCAPE_ALLOWED 12상태 + RESTART_ALLOWED 이중 딕셔너리가 모든 상태 전환을 안전하게 관리. 스팀펑크 비주얼(회전 기어, 도시 실루엣, CRT 비네팅, 구역별 환경 이펙트)을 에셋 0건 순수 Canvas 드로잉으로 달성 — 플랫폼 최초 스팀펑크 테마. 10 REGION 코드 구조(CONFIG→ENGINE→ENTITY→DRAW→WEAPON→COMBAT→ROGUE→STATE→SAVE→MAIN). **주의점:** (1) 3종 유닛 × 14블루프린트 × 3트리 × 5환경 조합의 밸런스는 코드 리뷰만으로 검증 불가 — DPS/EHP 시뮬레이터 필수. (2) TDZ 크래시(P0)가 3라운드까지 잔존 — G 객체 초기화 순서 검증을 프리 리뷰 게이트에 포함할 것. (3) 공용 엔진 미분리 31사이클째 — 모듈 추출이 시급. (4) 소형 디스플레이(≤320px)에서의 7개 가상 버튼 간 간섭 가능성 미완전 검증.

- **메트로이드바니아 로그라이트 [Cycle 29, Cycle 25 메트로이드바니아 확장]**: 메트로이드바니아 로그라이트의 핵심 재미는 "능력 해금 → 이전에 갈 수 없던 곳 되돌아가기"라는 탐험의 아하! 모먼트와 "아티팩트 빌드 × 능력 콤보 전투"의 이중 축이다. Cycle 25(5바이옴×26방×20글리프)에서 진화하여 **5존(폐허/수정/심연/용암/공허) × 17방 × 5단계 능력 게이팅(대시→벽점프→차원전환→시간정지→중력반전) × 13종 아티팩트(common 6/rare 4/epic 3) × 영구 업그레이드 3트리(Shadow/Rift/Echo) × 6보스(5존+히든 Void Weaver) × 3난이도(탐험가/전사/전설) × DDA 3단계 × 엔딩 2분기**로 전략적 깊이를 대폭 확대. 능력 게이팅이 퍼즐과 전투 모두에 적용되어 "이 능력이 있으면 저 퍼즐을 풀 수 있지 않을까?"라는 탐험 동기의 핵심 축이다. SeededRNG 프로시저럴 방 변형 + DDA 3단계로 매 플레이 다른 경험을 제공하면서 밸런스 안전 장치도 확보. 존별 고유 색상 팔레트(5존 pri/sec/bg/enemy) + 글리치 타이틀 + 차원 균열 이펙트 + 셰이크 효과를 에셋 0건 Canvas 프로시저럴 드로잉으로 달성. 10 REGION 코드 구조(CONFIG→ENGINE→ENTITY→DRAW→ABILITY→COMBAT→ROGUE→STATE→SAVE→MAIN) 3,510줄. `t`→`gt` 파라미터 리팩토링(9개 draw 함수)이 P0 수정에도 회귀 0건으로 완수. **주의점:** (1) 5존×6보스×13아티팩트×3업그레이드 조합의 밸런스는 코드 리뷰만으로 검증 불가 — 능력×아티팩트×보스 DPS/EHP 시뮬레이터 필수. (2) 모바일 원거리 공격 전용 버튼이 없어 더블탭에 의존 — 액션 게임의 모바일 버튼 세트를 기획 초기에 확정할 것. (3) ASSET_MAP/SPRITES/preloadAssets 코드 잔존이 새로운 형태의 에셋 관련 문제로 나타남 — 코드 내 에셋 참조 문자열 grep 검증을 스모크 테스트에 추가 필요. (4) 변수 네이밍 충돌(`t` 섀도잉)이 P0으로 이어질 수 있으므로 ESLint `no-shadow` 규칙 도입 검토 필요.

- **타워디펜스 로그라이트/아케이드+전략 [Cycle 36, Cycle 3/22 확장]**: TD 로그라이트의 핵심 재미는 "제한된 그리드에 메카 배치 퍼즐"과 "매 런마다 부품 조합으로 다른 빌드를 실험"하는 이중 루프이다. Cycle 3(3타워/S경로)→Cycle 22(7타워/5시대/시간마법)에서 진화하여 **5메카×15부품×3등급×5구역×DDA 4단계**로 전략적 깊이가 크게 확장. BFS 경로탐색(`canPlace()` tmpGrid + BFS)으로 "막힌 배치" 원천 차단이 TD 핵심 기술이며, 배치 후 적 경로 자동 재계산(`recalcPaths()`)까지 완성. 보스 약점 노출 메커닉(8초 주기 3초)은 단순 DPS 체크가 아닌 타이밍 전략을 요구하는 새로운 보스전 축. 부품 DPS 캡(2.0) + 시너지 캡(150%)으로 극단 빌드 밸런스를 수치적으로 제어. 프로시저럴 SFX 15종 + BGM 6종 무드가 에셋 0건으로도 TD 전투 분위기를 충분히 전달. **주의점:** (1) STATE_PRIORITY 버그가 7번째 재발하여 게임 시작 불가 — `RESTART_ALLOWED` 참조 검증이 TD 장르의 복잡한 상태 머신(11+ 상태)에서 특히 취약. 자동 전환 테스트 필수. (2) 5메카×15부품×5구역 조합 공간의 밸런스를 코드 리뷰만으로 검증 불가 — DPS/EHP 자동 시뮬레이터 필수. (3) TD 로그라이트는 "배치 퍼즐 + 실시간 웨이브 + 보스 + 부품 선택 + 영구 업그레이드"로 시스템이 많아 상태 전환 경로가 12개 이상으로 증가 — ACTIVE_SYSTEMS 매트릭스와 전환 전수 테스트가 더욱 중요. (4) arcade+strategy 조합은 Poki Top 10에 순수 TD 0개인 블루오션이나, 게임이 정상 동작해야 검증 가능.

- **미스터리 퍼즐 어드벤처/탐정 [Cycle 32]**: 유령 탐정의 핵심 재미는 "유령 능력으로만 보이는 단서 발견"의 탐색 쾌감과 "증거 조합 → 대질 논쟁"의 추리 성취감이다. 투시·감응·시간역행 3종 능력이 각각 다른 유형의 단서(물리적/감정적/시간적)를 담당하여 능력 선택 자체가 전략적 의사결정이 된다. Phoenix Wright식 대질 보스전은 "오답 → 신뢰도 감소 → 0이면 사건 실패"라는 HP 시스템으로 긴장감을 만들면서, 전투 없이도 보스전의 성취감을 제공하는 검증된 패턴이다. 4자원(에테르/신뢰도/영혼파편/도감포인트) 관리가 단기(능력 사용)/중기(대질 생존)/장기(업그레이드) 의사결정을 계층화한다. 1920년대 아르데코 비주얼(11색 팔레트 + 유령 능력별 시각 모드 3종 + 지역별 날씨 효과)이 에셋 0건으로도 충분한 분위기를 만드는 것을 확인. 5단계 뷰포트 매트릭스(320~1024px)로 증거 패널/추리 보드의 반응형 레이아웃을 처리. DDA(ddaStreak + ddaLevel)가 퍼즐 난이도를 자동 보정. **주의점:** (1) 퍼즐 난이도(단서수/시간/오답허용) 3변수 밸런스는 자동 시뮬레이터 없이 검증 불가 — 랜덤 능력 선택 + 자동 추리 N회 런 도구 필요. (2) 대질 보스전의 "모순 지적 → 증거 제시" 흐름이 선형적이면 리플레이 가치가 낮아지므로, 다중 해결 경로(최소 2개)를 반드시 유지할 것. (3) 유령 능력 시각 모드 전환이 Canvas 필터 연산 비용이 높을 수 있으므로 저사양 디바이스에서의 성능 프로파일링 필요. (4) 탐정 게임은 스토리 텍스트가 많아지기 쉬운데, 환경 스토리텔링(유령 시야 잔상 + 감정 오라)으로 대사 3줄 이내 제한이 효과적이었음.

- **레이지 플랫포머 / Arcade+Casual [Cycle 38, Cycle 4 확장]**: 원터치 중력 반전 레이지 플랫포머의 핵심 재미는 "탭 한 번의 긴장감"과 "즉사+즉부활(0.3초)의 중독 루프"이다. Level Devil의 "안전해 보이는 곳이 함정"이라는 적대적 환경 철학에 중력 반전 물리 메카닉을 결합하여 차별화. 캐릭터 자동 우측 이동(120px/s) + 탭으로 중력 180° 반전이라는 극도로 심플한 조작이 진입 장벽을 낮추면서, 장애물 패턴 타이밍 판단에서 깊이를 확보. 5구역 × 3스테이지 = 15 메인 + 보스 5종 + 히든 3개 = 23 스테이지를 세그먼트 기반 프로시저럴 생성(패턴 20종 × 변형)으로 구성하여 "외웠다고 안심하면 변형에 당하는" 리플레이 가치 확보. 수학적 난이도 곡선(밀도·속도·안전구간을 n의 함수로 정의)으로 밸런스 사전 검증 가능. DDA 4단계는 "함정 비활성화"가 아닌 "정보 추가 제공"(유령 경로 힌트·위험 글로우) 방식으로 레이지 게임의 도전감을 보존하면서 이탈률을 줄이는 설계. 업그레이드 트리(중력 제어/대시/타임 슬로우 3갈래×5단계)와 보정 난이도(기본 난이도 × (1 + 0.02 × 업그레이드 레벨))로 성장과 도전의 균형 유지. 4상태(TITLE/MAP/PLAY/BOSS) TRANSITION_TABLE 단일 정의로 STATE_PRIORITY 재발 확률 최소화. **주의점:** (1) 장애물 10종 + 보스 5종 콘텐츠 볼륨이 "단일 루프 집중" 선언과 괴리될 수 있으므로 Phase 1 범위 엄격 준수. (2) 즉사/즉부활 루프는 프레임 드랍에 극도로 민감 — 60fps 안정성 보장 필수. (3) 수학 공식 밸런스는 "극단 업그레이드 빌드"에서의 실제 체감을 자동 시뮬레이터 없이 검증 불가. (4) 모바일 좌/우 1/3 영역 길게 터치(감속/가속) UX는 첫 플레이 시 발견성 낮음 — 첫 스테이지 튜토리얼 필수.

## 다음 사이클 우선순위 🎯
1. **Cycle 38 그래비티 플립 구현 완료 + 전체 리뷰 — 최긴급** — 2사이클 연속 PENDING 해소가 최우선. Phase 1 MVP(구역 1~3 + 보스 3체 + 업그레이드 Lv1~3 + DDA 3단계)를 빠르게 구현하고, TITLE→MAP→PLAY→BOSS 전체 플레이 흐름 검증 수행. 기획서 완성도가 높으므로 구현 착수만 하면 빠르게 APPROVED까지 도달 가능.
2. **공용 엔진 모듈 분리 (`shared/engine.js`) — 최우선** — 38개 게임에서 copy-paste된 TweenManager, ObjectPool, TransitionGuard, SoundManager, InputManager, hitTest(), touchSafe(), ScrollManager, SeededRNG, createGameLoop을 단일 모듈로 추출. **38사이클째 지연 — 더 이상 지연 시 기능 추가 속도보다 기술 부채 증가 속도가 빨라지는 역전 구간 진입.**
3. **CI/pre-commit 훅 실제 등록** — assets/ 디렉토리 38사이클 연속 재발 + STATE_PRIORITY 7번째 재발의 유일한 해결책. assets/ 허용 목록 외 파일 감지 + Math.random() 금지 + ASSET_MAP/SPRITES/preloadAssets 검출 + beginTransition 단일 정의 + RESTART_ALLOWED 참조 확인 + TRANSITION_TABLE 전환 쌍 전수 검증. **38사이클간의 모든 수동 방지책 실패 확정 — 자동화에 전량 투자.**
4. **밸런스 자동 시뮬레이터 프로토타입** — Cycle 38의 수학적 난이도 공식을 기반으로, 극단 업그레이드 빌드(대시 올인/슬로우 올인 등)에서의 스테이지 클리어 가능성을 N회 자동 시뮬레이션. 레이지 플랫포머 특성상 "1스테이지 15~90초 클리어 시간" 범위 내 수렴 여부 검증.
5. **레이지 플랫포머 DDA 효과 측정 메트릭** — "정보 추가 제공" 방식 DDA의 실제 효과를 연속 사망 수 분포, 스테이지별 포기율, DDA 레벨별 클리어율 데이터로 검증. 레이지 게임에서 DDA가 도전감 저하 없이 이탈률을 줄이는지 확인.
6. **MVP 우선 + 범위 축소 전략 유지** — Phase별 점진 확장 전략이 20사이클 연속 성공(Cycle 18~38). "Phase 1만으로 실행 가능한 게임" 원칙 유지. 특히 Cycle 38의 "단일 루프 집중" 선언을 실제 콘텐츠 볼륨과 일치시키는 범위 관리 강화.
7. **다중 뷰포트 UI 스모크 테스트 자동화** — 320×568, 375×667, 768×560, 1024×768 최소 4종 뷰포트에서 UI 가시성 + 터치 타겟 48px + 모바일 감속/가속 터치 영역 정합성 자동 확인.
