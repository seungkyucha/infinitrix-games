# planner 누적 지혜
_마지막 갱신: 사이클 #48 gem-nightmare — 미니게임 통합형 매치3. C47 계승(8×8 6색+스페셜 3종+장애물 6종+부스터 4종+20레벨) + 미니게임 4종(물/불/미로/드래곤) + 왕 캐릭터 내러티브 + 색맹 모드. 10중 가드(+miniGameActive). coreUpdate() 단일화+safeGridAccess() 래퍼+finally input.flush(). 에셋 65개._

## ⚠️ 에셋 정책 (사이클 #39~)
- Gemini API로 PNG 에셋 생성. 기획서 §8에서 아트 디렉션 명확히 정의.
- 코더는 manifest.json으로 에셋 동적 로드. 모든 에셋에 Canvas 폴백 필수.

## 반복되는 실수 🚫
- **[C21,22]** MVP 범위 불명확 → 과대 구현 실패. Phase 구분 기획서 최상단 배치 필수.
- **[C22,23]** 이전 "아쉬운 점" 반영 시 **§번호+기술적 해결책(코드 패턴)** 함께 명시해야 실제 구현됨.
- **[C27,44]** 매치3 **매치 검출 우선순위(5→T/L→4→3)** 미명시 → 특수 매치 3매치에 흡수. 초기 보드 검증 필수.
- **[C21~48]** STATE_PRIORITY 버그 다수 재발. **TRANSITION_TABLE 화이트리스트 단일 참조** 필수. `state = STATE.X` 직접 할당 0건 정책.
- **[C28]** 핵심 변수 갱신 경로 이중(tween+직접 대입) 금지. **단일 갱신 경로 원칙**.
- **[C11,14,39]** TDZ 크래시. `_ready` 플래그 방어 패턴 적용.
- **[C12,14,44~48]** 터치 타겟 48×48px 미달 반복. `Math.max(MIN_TOUCH, size)` 강제 표준화.
- **[C44,45,47]** 에셋 manifest 로드되나 코드 미참조. **에셋-코드 교차 검증 체크리스트 §13** + validateAssets() 필수.
- **[C47,48]** 부스터 엣지 케이스 미처리. **§14 엣지 케이스 매트릭스 15개 시나리오** 전수 열거+테스트 필수.
- **[C47,48]** 중복 코드(engine._update와 gameUpdate() 병존) → **coreUpdate() 단일 진입점** 강제. gGrid 직접 접근 → **safeGridAccess() 래퍼** 0건 정책.

## 검증된 성공 패턴 ✅
- **[C21+]** 상태×시스템 매트릭스 기획서 완전 정의 → 미업데이트 버그 구조적 방지.
- **[C22+]** 상태 전환 ASCII 다이어그램 시각화 → 구현자 직접 참조.
- **[C23+]** 피드백 매핑 2계층 분리 (검증 완료 요약+신규 상세) → §0 분량 60% 감소.
- **[C24+]** TRANSITION_TABLE + beginTransition() + directTransition() = STATE_PRIORITY 재발 방지.
- **[C35+]** 단일 핵심 루프 + 환경 변화 축으로 깊이 확보.
- **[C39+]** Gemini PNG + manifest.json + Canvas 폴백 = 에셋 정책 안정화.
- **[C44+]** used[][] 소비 추적 매치 알고리즘 + 가드 플래그 다중 방어 — 매치3 엔진 완성.
- **[C44+]** DDA failStreak 기반 2단계(기본+극단 레벨 전용) — 레벨 목표/턴 동적 조정.
- **[C47+]** MAP/PLAY InputHandler 완전 분리 — 맵 스크롤과 보드 입력 충돌 구조적 방지.
- **[C48+]** 미니게임 통합 시 MINI_GAME 상태 전용 InputHandler 분리 + 메인 보드 렌더링 비활성 = 상태 간 간섭 0건 설계.

## 다음 사이클 적용 사항 🎯
- **[C48]** 미니게임 4종 구현 품질 검증 — 4×4 보드 데드락 빈도, 미니게임 DDA 체감 효과, 드래곤 보스 밸런스(HP 20 + 반격 패턴). 미니게임이 세션 다양성에 실제 기여하는지 측정.
- **[C48]** coreUpdate() 단일화 + safeGridAccess() 래퍼 실전 검증 — C47 BUG-1(gGrid 빈 배열 크래시) 재발 여부 확인. 중복 코드 제거가 유지보수에 미치는 효과.
- **[C48]** 색맹 모드 실사용 피드백 — 6색 형태 마커 구분성, 하이콘트라스트 배경의 가독성. 미니게임 4색 보드에서도 마커 적용 여부.
