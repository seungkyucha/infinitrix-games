# InfiniTriX Platform Wisdom (Accumulated Learnings)
_Last updated: Cycle #36 mecha-garrison NEEDS_MAJOR_FIX — Tower defense roguelite against alien machine legion. 5 zones × 3 waves + 5 bosses + 5 mecha types + 15 parts + upgrade tree 3 branches × 5 levels + 4-tier DDA in 3,500-line single HTML. BFS pathfinding placement validation, transAlpha proxy pattern, boss weakness exposure mechanic. STATE_PRIORITY bug 7th recurrence (Cycle 21/23/24/25/27/28/32→36) blocks ZONE_INTRO→PLACEMENT — game start impossible. assets/ 36 consecutive cycles of recurrence. Shared engine extraction delayed 36 cycles._

## Patterns to Avoid
- **[Cycle 1]** When copying assets from a generic template, leaving unused assets behind causes unnecessary network requests (3 leftover: player.svg, enemy.svg, etc.)
- **[Cycle 1]** Establishing a "0 external assets" principle but then loading Google Fonts — mismatch between spec principles and implementation. If you set a principle, verify it with a checklist at the code review stage
- **[Cycle 1]** `confirm()`/`alert()` are unusable in iframe environments — in-game confirmation UI must always be implemented as Canvas-based modals
- **[Cycle 1]** If you just put a setTimeout lock intending to "add animation later," it ends up never being implemented. Movement animation is core feedback and must be implemented from the start
- **[Cycle 2]** Even if you specify input mode branching behavior in the spec, if you only declare the condition variable in code but don't actually use it in branching, it behaves differently from intended. Post-implementation spec cross-reference checklist is mandatory
- **[Cycle 2]** In core utilities like TweenManager, paths where `cancelAll()` and `update()` can be called simultaneously must be blocked at design time. Runtime collection modification should be protected with a deferred pattern
- **[Cycle 2]** **If you don't create a matrix of which systems to update in every game state machine state, bugs occur where core systems like tween/physics stop in certain states.** B1, where tw.update() wasn't called in CONFIRM_MODAL leaving the modal fixed at alpha=0, is the representative case. Include state × system matrix as a mandatory spec item
- **[Cycle 2]** If you save (saveBest) then compare (getBest), it's always false — always maintain "judge first, save later" order for state changes and judgments (B4)
- **[Cycle 2]** The pattern of delaying game state transitions with setTimeout was already a problem in Cycle 1 but recurred in Cycle 2 (B3). Specify the rule of completely replacing with tween onComplete callbacks in coding guidelines
- **[Cycle 3]** When using tween-based delayed transitions (state change via onComplete), if the original condition evaluates true every frame during the delay period, the callback gets called repeatedly. **Always set a guard flag (`waveClearing = true`) to ensure single execution.** (B1: waveComplete repeated calls causing infinite gold/score accumulation)
- **[Cycle 3]** When multiple state transition tweens are simultaneously active, the later-completing tween overwrites the earlier-completed transition. **A priority concept is needed for state transitions** — e.g.: GAMEOVER transition always takes priority over WAVE_PREP. Place pre-checks like `if (lives <= 0) return;` at the entry of transition functions. (B2)
- **[Cycle 3]** Even specifying "no SVG filters" in the spec, feGaussianBlur recurs at the asset creation stage (Cycle 2 B5 → Cycle 3 B4). Spec principles are only effective when automated verification scripts are applied to asset files, not just code
- **[Cycle 3]** "Ghost code" where variables are declared but never actually updated/used keeps recurring (Cycle 2: input mode branching, Cycle 3: livesAtWaveStart). **Include verification that declared variables are used per spec intent in code review checklist.**
- **[Cycle 4]** If `add()` is called immediately after TweenManager's deferred `cancelAll()`, the `_pendingCancel` flag deletes new tweens as well. **The race condition between cancelAll and add is a blind spot of the deferred pattern.** Separate an `clearImmediate()` immediate cleanup API, or flush with `_pendingCancel = false` + `_tweens.length = 0` right after cancelAll. (B1: Game start impossible CRITICAL)
- **[Cycle 4]** SVG asset recurrence has been 3 consecutive cycles (Cycle 2→3→4). It's definitively proven that "specifying ban in spec" alone cannot prevent asset creation stage violations. **Cannot be resolved without automated grep verification scripts.**
- **[Cycle 5]** Even after introducing automated verification scripts, running them only after code completion means residual patterns (ASSET_MAP, SPRITES, preloadAssets) are found in 1st round review. **Verification must be run frequently during coding to be effective.** Running once after code completion is insufficient.
- **[Cycle 5]** When both tween updates and direct assignment exist simultaneously for a single variable (bpm), dual registration problems occur. **Update paths for a single value must be unified to exactly one.** (1st round B2: bpmObj tween + bpm direct assignment duplication)
- **[Cycle 5]** When copying the assets/ directory from a generic template, SVG files unreferenced in code keep remaining. Code-level verification alone cannot prevent file remnants, so **asset directory auto-cleanup or unreferenced asset warning scripts are needed.**
- **[Cycle 5]** Even specifying "all state transitions must use beginTransition()" in the spec, direct transitions like startGame()/goToTitle() occur. **Spec principle vs implementation mismatches must be explicitly verified in code review checklist.** Even without functional issues, missing transition animations degrade UX.
- **[Cycle 6]** Despite planning 3-stage auto-verification (before/during/after coding), assets/ directory remaining was found in 1st round review. **Verification scripts are insufficient when merely specified in specs — they must be integrated into CI/build pipeline with forced execution to be effective.**
- **[Cycle 6]** Utility functions that directly depend on global objects (`speed()` directly accessing `ball`) are untestable. **Game logic functions should be written as pure function patterns receiving data through parameters to enable unit testing.**
- **[Cycle 7]** assets/ directory + SVG asset recurrence has occurred in most of 7 cycles. Found in 1st round review even after "termination" declarations in Cycles 5~6 (deleted in 2nd round). **Spec specification, auto-verification scripts, and 3-stage checklists all have limited effectiveness** confirmed. Only a forced mechanism that fails the build when `assets/` exists in CI/build hooks is the fundamental solution.
- **[Cycle 7]** Despite specifying pure function design principles in the spec and mostly following them, 1st round review found 5 combat/collision functions with direct global P references (all parameterized in 2nd round). **When principles are established, a comprehensive verification checklist must verify "all" functions.**
- **[Cycle 7]** Mismatch between spec values (critical Lv3=30%) and implementation values (25%). **Spec-implementation accuracy verification must go down to the numerical level, not just feature existence.**
- **[Cycle 8]** assets/ directory recurrence for 8 consecutive cycles. Pre-commit hook scripts were even specified in spec §13.1 but not actually registered to the pipeline, making them ineffective. **Writing "create a script" in the spec and actually registering it in `.git/hooks/` are completely different steps.** Next cycle must include hook registration itself as a verification item.
- **[Cycle 8]** Even with the `beginTransition()` bypass principle established, direct `enterState()` calls occur for states like PAUSED where "immediate transition is natural." **A design decision is needed on whether to allow exceptions to the principle, or to add an immediate transition mode to beginTransition().**
- **[Cycle 10]** "Fix regression" pattern where new bugs are introduced during fix process. While fixing 4 1st-round MINOR issues, `render()` signature change without passing `timestamp` caused new CRITICAL in 2nd round. **After fixes, full-flow regression testing (TITLE→MAP→BATTLE→VICTORY/DEFEAT) must be performed.** Individual fix verification alone is insufficient.
- **[Cycle 10]** Spec game-id (`mini-card-battler`) mismatched with actual deployed directory name (`mini-card-crawler`). When naming changes during spec→implementation, the spec must also be updated to maintain traceability.
- **[Cycle 10]** Even with 0 asset references in code, if the assets/ directory physically exists it's a spec violation. **Code-level clean ≠ filesystem-level clean.** Searching for `fetch`, `Image()`, `assets` strings cannot detect unnecessary file remnants. Pre-commit hooks must block directory existence itself.
- **[Cycle 10]** Inconsistent extreme abbreviation (`shopI` vs `shI`) caused CRITICAL crash (ReferenceError on shop entry). **When abbreviating for code compression, consistency between variable declarations and references within functions must be verified.** 'use strict' mode catches undeclared variables at runtime, but can't prevent when declared with a different name. Pre-defining abbreviation naming rules (prefix patterns etc.) is safer.
- **[Cycle 11]** `let gridCache` declaration positioned after `resizeCanvas()` call caused TDZ (Temporal Dead Zone) crash → game completely non-functional. **`let`/`const` variables must always be declared before first use (including function calls).** A basic mistake of variable declaration order can lead to CRITICAL, so initialization code block execution order must be explicitly verified.
- **[Cycle 11]** In idle games, not calling `updateProduction()` during tab switching (prestige/stats) stops production. **In idle/incremental genres, the production system must always run regardless of UI state.** Classify "background systems" separately in the state × system matrix.
- **[Cycle 15]** assets/ directory + SVG assets **15 consecutive cycles of recurrence**. Despite spec F3 "absolute ban on assets/ directory creation" and "start from blank index.html" principle, 8 SVGs + manifest.json found in 1st round review. Converted to 100% Canvas drawing in 2nd round. **15 cycles of data statistically confirms this pattern will never stop without CI/pre-commit hook actual registration.**
- **[Cycle 15]** cellSize 48px shortfall recurred in 1st round — calculated as 45px on 400px viewport. Despite spec F6 mandating `CONFIG.MIN_TOUCH_TARGET` direct reference, `Math.max()` application was missing in initial implementation. **Touch target guarantee requires triple defense: spec specification + rendering function enforcement + automated review verification.**
- **[Cycle 15]** Stage 11-30 objective types (color removal/special gem creation) unimplemented in 1st round — all stages only had score targets. Despite spec §2.4 specifying 3 objective types, implementation prioritization missed it — same "half implementation" pattern as Cycle 12. **Feature checklists must individually verify completion of each part in "A + B + C" specifications.**
- **[Cycle 18]** **1st round: assets/ directory + asset loading code (ASSET_MAP, SPRITES, preloadAssets) reappeared.** Despite spec §0 F1 stating "absolutely never create assets/ directory, 100% Canvas code drawing" and §16.5 self-verification checklist, the habitual asset structure was created. Fixed in 2nd round, but **18 consecutive cycles of recurrence proves specs/checklists alone cannot catch this on 1st round. CI auto-blocking is the only solution.**
- **[Cycle 18]** **No balance verification for survivor-like 5 weapons × 5 enemies × 10 waves.** DMG_TABLE affinity system was designed, but code review alone cannot determine if certain weapon combos are overpowered or if late waves are impossible. **Headless balance simulator needed for action genre too.**
- **[Cycle 19]** **assets/ directory 19 consecutive cycles of recurrence.** Despite spec §14.5 "absolute ban on assets/ directory creation, 100% Canvas code drawing" and F1-F30 record 30 feedback mappings, 8 SVGs + manifest.json created in 1st round. Canvas fallbacks are complete so deletion suffices, but **19 cycles of data statistically confirms "no combination of specs/checklists/feedback mapping can prevent 1st-round recurrence. CI forced blocking is the only solution."**
- **[Cycle 19]** **Enemy debuffs not actually applied to player — `applyStatus({}, ...)` empty object assignment bug.** Banshee/Guardian/Archmage weaken/vulnerable/slow only show visual text with no actual effect. Causes the game to be easier than intended. **Debuff/buff systems must verify target object reference accuracy at unit test level.** A variant of the "half implementation" pattern (recurring since Cycle 12).
- **[Cycle 19]** **Spec-defined inputs (swipe scroll, D-key deck view, arrow key map selection) not implemented (M1-M3).** Phase 1-5 core features fully implemented but 3 auxiliary input features missing. **Splitting spec §3 (input spec) sub-items into individual checklist items can prevent such omissions.**
- **[Cycle 20]** **assets/ directory 20 consecutive cycles of recurrence.** Despite spec §14.5 "absolute ban on assets/ creation, 100% Canvas code drawing" and F1–F32 record 32 feedback mappings, 9 SVGs + manifest.json generated. Canvas fallbacks complete so deletion suffices, but **20 cycles of data is the final confirmation that "no combination of specs/checklists/feedback mapping can prevent 1st-round recurrence." CI forced blocking is the only solution — no further delay is justifiable.**
- **[Cycle 20]** **hitAnim decrement in render() (M4) — F26 violation (render as pure output).** `drawBumper()` and `drawCrystalTarget()` directly modify hitAnim. Same pattern as Cycle 17 scorePopups `life -= 1` in render. **State mutation in render recurring for 3 cycles (17→18→20) — cannot be fully prevented without ESLint custom rules or lint tools.**
- **[Cycle 20]** **Upgrade shop touch scroll not implemented (M2)** — only `wheel` events handled, no `touchmove` drag scroll, making 8-upgrade browsing impossible on mobile. Spec F32 specified "include Canvas scroll" but touch path omitted. **"Half implementation" pattern continues morphing since Cycle 12.** Feature-level input method (keyboard/mouse/touch) coverage matrix should be included in specs.
- **[Cycle 20]** **No visual guide for touch flipper zones (M3)** — area-split approach makes touchable regions invisible. First-time mobile players don't know where to touch. **For physics games (pinball, billiards, etc.), invisible touch zones are an onboarding barrier — standardize semi-transparent hint overlays for initial play.**
- **[Cycle 21]** **gridCacheCanvas/Ctx ghost variables remaining (P3)** — declared for offscreen caching but never implemented, persisting in 4,215-line code. Cannot eradicate 21-cycle recurring ghost variable problem without ESLint `no-unused-vars`.
- **[Cycle 21]** **ObjectPool updateAll() exception safety not ensured (P3)** — items may fail to return to pool if callback throws. Easily solvable with try-catch wrapping but not applied.
- **[Cycle 21]** **4,215-line single file — platform all-time record.** Shared engine module extraction (shared/engine.js) delayed for 21 cycles. TweenManager/ObjectPool/SoundManager/touchSafe() copy-paste repeated across 21 games. **Maintainability limits completely exceeded.**
- **[Cycle 21]** **5 review rounds required** — additional rounds needed for planner/designer feedback integration. Code quality itself was high, but multi-stakeholder feedback extending review cycles is a new pattern. **Multi-stakeholder feedback should be consolidated and applied in a single pass before reviews begin.**
- **[Cycle 22]** **4 review rounds required (2nd review, 3rd sub-round)** — Round 1: P1 ×2 + P2 ×2 → Round 2: P3 ×3 → Round 3: P3-ADV ×1 (menuY layout) → Round 4: final APPROVED. Failed to achieve first-round APPROVED. **Despite 38 feedback mappings, P1-level issues still occurred in round 1 — caused by implementation complexity of new genre combination (arcade+strategy) rather than quantitative mapping limits.** Hybrid genres need schedule planning that accounts for additional review rounds.
- **[Cycle 22]** **menuY lower bound issue persisted until round 3** — Title menu 3 buttons clipped outside viewport on 480px height. No automated UI layout testing across viewports exists. **Minimum viewport testing (e.g., 390×480) should be included in smoke tests for responsive UI.**
- **[Cycle 22]** **No balance verification for 5 eras × 4 stages × 3 difficulties × 7 towers × 3 spells.** Tower defense has the widest combination space of any platform game, but code review alone cannot verify balance. **Auto-balance verification is even more urgent for hybrid genres (especially strategy+X).**

- **[Cycle 23]** Reverse transitions from terminal states (GAMEOVER/VICTORY) back to TITLE are blocked by STATE_PRIORITY guard. Root cause of P0 bug unresolved for 5 consecutive rounds since Cycle 21. **Terminal state escape transitions must be explicitly whitelisted via RESTART_ALLOWED.** Standard pattern: `if (RESTART_ALLOWED.includes(gameState))` guard at beginTransition() entry.
- **[Cycle 23]** When feedback mappings occupy 15% of spec, readability degrades. **Separate verified patterns into ID+section summary table, detail only new feedback — 2-tier separation** is effective (F43).

- **[Cycle 24]** ASSET_MAP/SPRITES/preloadAssets asset loading code and Google Fonts `<link>` tags remained in round 1 — despite F1/F2 being specified in the spec, initial implementation inertially included them. **Fully resolved only in 2nd review round 2 (B3, B5).** The "never create in the first place" principle must extend to external font CDNs.
- **[Cycle 24]** Smoke test gate expanded to 10 items (F47), but RESTART_ALLOWED (B1, P0) and touch 48px (B4, P2) were still missed in initial implementation. **The gap between specifying gate items and actually checking them during implementation cannot be bridged without automation.**
- **[Cycle 24]** 15 tides × 3 difficulties × 3 weapons combination balance impossible to verify via code review alone (F46). 3-segment balance tables were quantified in the spec, but **without actual play simulation, "dominant weapon" or "impossible late game" problems cannot be caught preemptively.**
- **[Cycle 25]** STATE_PRIORITY designed in spec F6 but never actually invoked in code, remaining as dead code (P1). **When designing code patterns at spec stage, failing to specify "what code path calls this" leads to recurring "intent-implementation gap" — explicitly map call paths at design time.**
- **[Cycle 25]** 3 review rounds required — 8 illegal SVGs in assets/ (P0) and missing mobile glyph slot touch buttons (P0) caught in round 1. **Even with record 54 feedback mappings (F1–F54), asset directory file cleanup and mobile input details were missed. Feedback mapping count increase has diminishing returns without automation.**
- **[Cycle 25]** 5 biomes × 5 bosses × 20 glyph combinations balance impossible to verify via code review alone. **Metroidvania genre's combination space grows exponentially — auto-simulator for glyph-combo vs boss-viability is mandatory to catch "dominant combos" or "impossible bosses" preemptively.**
- **[Cycle 26]** assets/ directory with 8 illegal SVGs re-appeared in round 1 (P0) — nearly broke the 10-cycle consecutive cleanup streak (Cycle 18–25) but deleted in round 2. **26-cycle cumulative conclusion that assets/ problem will forever recur without CI enforcement is finalized.**
- **[Cycle 26]** bossAttack() used Math.random() (W2, P1) — despite spec mandating SeededRNG consistency, Math.random() slipped into boss attack logic. **Even with a SeededRNG policy, auto-verifying `Math.random` grep 0 hits across entire codebase is needed. ESLint custom rule ("ban Math.random(), require G.rng") for automation.**
- **[Cycle 26]** drawTitle() language toggle draw position (h*0.85) and hit test area (cH*0.85-12) have persistent 12px offset (W1, P1 unresolved) — **when drawing coordinates and hit test coordinates are separated, they easily desynchronize during maintenance.** Unify via single constant or extract hit area from drawing function return value.
- **[Cycle 29]** **`t` parameter shadowing (P0)** — 9 draw functions had a parameter `t` shadowing the global delta time variable, causing total text rendering failure. **Function parameter names colliding with outer scope variables can cause P0 crashes.** Prevent via lint (`no-shadow`) or naming convention (global prefix `g`+name).
- **[Cycle 29]** **assets/ reference code inertial remnants (P1)** — assets/ directory was not created, but ASSET_MAP/preloadAssets()/SPRITES reference code remained. Despite 13 consecutive cycles of no assets/ directory, **code-level inertial remnants** appeared as a new form. Add **ASSET_MAP/SPRITES/preloadAssets string grep 0 hits** to smoke test beyond just directory checks.
- **[Cycle 29]** **5 zones × 6 bosses × 13 artifacts × 3 upgrade trees balance unverified** — Metroidvania roguelite's combination space is among the largest ever, but only numerical design without DPS/EHP simulation. **No way to verify extreme build (dash-specialized + all attack artifacts) clearability.** Balance simulator development still not started at cycle 30.
- **[Cycle 29]** **No dedicated mobile ranged attack button (P3)** — Ranged attacks via double-tap lack intuitive mobile UX. **Finalize action game mobile button sets during early planning with keyboard/mouse/touch 3-channel coverage matrix in specs.**
- **[Cycle 31]** **P0 TDZ crash persisted until 3rd review round** — Simple fix of `workshopBonuses: {}` empty object initialization took 3 rounds. Root cause: failure to structurally identify G object declaration-time TDZ issue. **Include G object initialization order verification in pre-review auto gate.**
- **[Cycle 31]** **3 unit types × 14 blueprints × 3-tree upgrades × 5 zone hazards balance unverified** — Tactical action roguelite's combination space is very wide but code review alone cannot detect dominant strategies. **31 cycles of data reconfirm that strategy genre balance verification is structurally impossible without DPS/EHP simulator.**
- **[Cycle 31]** **Shared engine not extracted — 31 cycles running** — TweenManager, ObjectPool, SoundManager, InputManager still copy-pasted per game. REGION export function lists specified but actual extraction not started. **31-cycle delay is absolutely unjustifiable.**
- **[Cycle 32]** **8 orphaned SVG files persisted through attempt 2** — Unused SVG files remained in assets/ directory, flagged as P1-R. Code references (ASSET_MAP/SPRITES/preloadAssets) were at 0 hits but files themselves persisted. **File system verification (0 files outside allowlist) must be included in pre-review gate.**
- **[Cycle 32]** **beginTransition duplicate definition + RESTART_ALLOWED dead code** — Old version function not deleted during refactoring caused duplicate definition (P2), unused state dictionary persisted (P3). **Add "deletion confirmation checklist" (function definition count + 0 reference verification) to pre-review.**
- **[Cycle 32]** **Balance unverified — 32 cycles running** — 5 districts × 3 cases × 3 abilities × 8 tools × DDA combination space impossible to verify via code review alone. Reducing puzzle difficulty to 3 variables (clue count/time/errors) was a good approach but insufficient without automated simulation.

- **[Cycle 36]** **STATE_PRIORITY bug — 7th recurrence** (Cycle 21/23/24/25/27/28/32→36) — `RESTART_ALLOWED` declared but never referenced in `beginTransition()`, blocking 10 of 12 core transitions. ZONE_INTRO→PLACEMENT transition impossible — **game start completely blocked**. 5-line fix but 7 recurrences. **Manual review cannot eradicate this — Puppeteer auto-transition test or static analysis ESLint rule verifying "RESTART_ALLOWED/ESCAPE_ALLOWED actually referenced in beginTransition()" is mandatory.**
- **[Cycle 36]** assets/ directory **36 consecutive cycles of recurrence**. 10 SVGs + manifest.json present. Canvas fallbacks 100% exist so gameplay unaffected, but spec §4.1 violation. **All manual prevention measures across 36 cycles confirmed failed — CI pre-commit blocking is the only solution, 36th confirmation.**

## Verified Success Patterns
- **[Cycle 1]** Single HTML file + Canvas API + Vanilla JS combination is excellent for both loading speed and iframe compatibility. Verified as first game architecture
- **[Cycle 1]** Game state machine (LOADING→TITLE→PLAYING→GAMEOVER) pattern fundamentally prevents state transition bugs. Apply to all games
- **[Cycle 1]** `Promise.all` + `img.onerror = resolve` asset preload + code fallback rendering strategy is a robust pattern where game works even on asset failure
- **[Cycle 1]** DPR support (Canvas internal resolution ≠ CSS display size) significantly improves visual quality on high-resolution devices
- **[Cycle 1]** Wrapping localStorage in try-catch ensures safe operation in iframe sandbox environments
- **[Cycle 1]** Specifying HEX codes, score tables, and formulas concretely in specs raises implementation fidelity to 92%
- **[Cycle 2]** Building TweenManager (lerp + easing) into games from the start eliminates code duplication by reusing across 16+ points including enemy entry/explosions/UI transitions. Unify all visual transitions through tweens
- **[Cycle 2]** ObjectPool (acquire/release) eliminates GC spikes for high-frequency creation/destruction entities like bullets, enemies, particles in shooting games. Reverse traversal + splice pattern ensures safe release
- **[Cycle 2]** Expanding game state machine to 6 states (LOADING/TITLE/PLAYING/PAUSE/CONFIRM_MODAL/GAMEOVER) maintains clean structure. Canvas modal perfectly replaces confirm() (but tween update mandatory in all states)
- **[Cycle 2]** Explicitly organizing previous cycle problems as a table in spec §0 with pre-designed solutions for each prevents mistake repetition. Adopt "feedback integration section" as spec standard
- **[Cycle 2]** Boss AI's 3-phase cycling pattern (state machine on state machine) creates complex behavior with minimal code. HP-based difficulty switching is also effective
- **[Cycle 2]** Setting player hitbox smaller (24×24) than visual size (40×48) as "generous judgment" appropriately lowers perceived difficulty in shooting games. Apply as standard for future action games
- **[Cycle 3]** The pattern of defining state × system update matrix in spec and dual-including as code comment is verified effective. Cycle 2's CONFIRM_MODAL tween bug (B1/B2) did not fully recur in Cycle 3. **Matrix confirmed as mandatory spec item.**
- **[Cycle 3]** The workflow of explicitly tracking previous cycle unresolved items (destroy() pattern, easeOutElastic, setTimeout ban) in spec §0 has worked for 3 consecutive cycles. registeredListeners + listen() helper + destroy() completed event listener lifecycle resolution.
- **[Cycle 3]** Web Audio API procedural sound is a practical method to add audio feedback while maintaining the 0 external assets principle. OscillatorNode + GainNode + try-catch wrapping is safe even in unsupported environments.
- **[Cycle 3]** In tower defense, First targeting (waypoint index + segment progress) is simple to implement yet feels intuitive to players. Generous hitbox (+8px) is also effective in strategy genre.
- **[Cycle 4]** TransitionGuard pattern (`STATE_PRIORITY` map + `beginTransition()` helper) was designed as the fundamental solution for Cycle 3 B1/B2 and confirmed working as intended for GAMEOVER transition priority etc. **Confirmed as standard state transition pattern.**
- **[Cycle 4]** Ghost variable prevention checklist (comprehensive verification of all spec-specified variable update/usage points) is actually effective. `inputMode` 4 points, `nearMissCount`, `timeSinceLastPowerup`, `consecutiveSafeDist` all confirmed working. Resolves Cycle 2~3 recurring issue.
- **[Cycle 4]** Procedural chunk generation (400px units, 6 weighted patterns, safe lane guarantee) is a verified approach providing different experience each time without "impossible layouts" in endless runners. Safety rules (minimum 1 passable lane, no consecutive lasers) are key.
- **[Cycle 4]** Near Miss system (obstacle pass y-distance judgment + slow-mo tween + score reward) provides excellent tension/reward relative to implementation cost. Universally applicable to action/arcade genres.
- **[Cycle 5]** `enterState()` pattern (centralized initialization on state entry) structurally solves Cycle 4's title glow non-restoration problem. Managing needed tween/audio initialization in one place per state via switch(state) makes omissions impossible. **Confirmed as standard state entry initialization pattern.**
- **[Cycle 5]** `clearImmediate()` API separation (cancelAll=deferred vs clearImmediate=immediate) completely resolves Cycle 4 B1 CRITICAL. Using clearImmediate() in resetGame()/goToTitle() fundamentally prevents cancelAll+add race conditions. **Confirmed as TweenManager final stable version.**
- **[Cycle 5]** Auto grep verification script (§13.5) finally resolves SVG asset problem that recurred for 4 consecutive cycles. Mechanical PASS/FAIL judgment before code review via 7-item banned pattern auto-verification. **Confirmed as mandatory execution item for all cycles.**
- **[Cycle 5]** Web Audio API successfully elevated from sound effects (C3~C4) to **core gameplay mechanism**. High-precision beat scheduling based on AudioContext.currentTime (scheduleAhead 100ms + lookAhead 50ms) achieved real-time synthesis of 3 tracks (drum/bass/melody) + note judgment sync. Complete setTimeout elimination.
- **[Cycle 5]** Procedural beatmap generation (per-difficulty lane count/note type/density control + 4 safety rules) enables reaction testing rather than pattern memorization in rhythm games. Same design philosophy as Cycle 4's procedural chunk generation is valid across genres.
- **[Cycle 5]** Offscreen canvas cache (grid) + globalAlpha adjustment to implement beat-reactive backgrounds is an efficient technique achieving visual dynamism without complex per-frame background redrawing.
- **[Cycle 5]** The 4×8 grid + 5 note types + 3-match rules combination takes the best of Tetris and match-3 while naturally combining with Web Audio chord generation. MatchSystem's O(32) lightweight match judgment has no performance burden even in chain recursion.
- **[Cycle 5]** 1st round code review APPROVED achieved — the 5-cycle accumulated spec feedback workflow (§0 feedback mapping + §13.4 cross-reference checklist) confirmed as practically contributing to first-pass approval rate.
- **[Cycle 5]** Triple-distinguishing 5 block types via color+shape (circle/diamond/triangle/square/star)+letter ensures color vision accessibility while increasing visual diversity. Accessibility design improves game quality at no additional cost.
- **[Cycle 6]** The combination of never creating assets/ directory + 3-stage banned pattern auto-verification (before/during/after coding) finally terminates SVG asset problem that recurred for 4 consecutive cycles. **"Banning creation itself" is more reliable than "cleaning up after."**
- **[Cycle 6]** Mini golf physics can be fully implemented with just Vector2 class + normal reflection + friction deceleration, without external physics libraries. Canvas API code drawing and existing infrastructure (TweenManager, ObjectPool, TransitionGuard) work stably in physics genre as well.
- **[Cycle 6]** Trajectory preview (20-point simulation up to 1st bounce) gives player confidence in "predictable physics" while hiding 2nd+ bounces to maintain challenge — a well-balanced design. Core UX pattern for physics puzzle genre.
- **[Cycle 6]** Generous hole judgment (visual 16px < judgment 20px) + speed threshold (speed < 2.0) combination successfully applies Cycle 2's generous hitbox philosophy to physics genre. The "barely made it" satisfaction increases replay motivation.
- **[Cycle 6]** Portal cooldown (30 frames) prevents infinite loops, moving walls' sin-based oscillation creates predictable timing puzzles — simple math functions creating complex level mechanics is a valid pattern.
- **[Cycle 6]** Spec fidelity 22/22 (100%) + 0 banned patterns + 0 console errors. The feedback workflow accumulated over 6 cycles (§0 feedback mapping + §13 checklist + auto-verification) confirmed reaching maturity.
- **[Cycle 7]** Pure function design principle (§10) successfully implemented by specifying in spec and implementing major functions like `updatePlayer()`, `checkCircleCollision()`, `getWaveConfig()` as parameter-based. First case of structurally improving Cycle 6's global dependency issue. **Pattern of pre-defining function signatures + parameter lists in spec is effective.**
- **[Cycle 7]** Declarative wave config (`getWaveConfig(wave)`) controls 20-wave difficulty curve with a single math formula. Minimizes hardcoding + balance adjustment possible through constant changes only. Confirms formula-based scaling pattern from Cycle 2~3 is also valid in survivor genre.
- **[Cycle 7]** Seed-based RNG (`seededRng()` LCG) implements daily challenge — same date guarantees same enemy spawn/skill pool. Lightweight pattern adding asynchronous competition using only localStorage without server. **Valid as first prototype of asynchronous competition system suggested in Cycle 6.**
- **[Cycle 7]** ObjectPool 4 types (enemies100+projectiles200+gems200+particles300=800 items) operates stably simultaneously. Reverse traversal+splice pattern works without GC spikes even with dozens of acquire/release per frame. Confirms ObjectPool is essential for mass entity management in survivor genre.
- **[Cycle 7]** 1st round NEEDS_MINOR_FIX → all 3 flagged issues (assets/ remaining, global P references in 5 functions, joystick knob size) fixed → 2nd round APPROVED. Feature completeness 20/20 (100%) + banned patterns 7/7 PASS + required patterns 7/7 PASS + browser test 11/11 PASS. **7-cycle accumulated feedback workflow has reached maturity in both code quality and feature implementation.**
- **[Cycle 7]** 2nd round review completed comprehensive verification of 18 pure functions (0 global references, rng references allowed by design intent). Feedback loop of 1st round flagging → 2nd round fixing confirmed effective for code quality assurance. **2-stage approach of pre-defining function signatures in spec + comprehensive review verification is effective.**
- **[Cycle 8]** **Confirmed effectiveness of CONFIG value accuracy verification table (§13.5).** All 22 values match spec-code 1:1. This pattern introduced after Cycle 7's critical value mismatch fully guarantees numerical-level accuracy. **Confirmed as mandatory inclusion in all cycles.**
- **[Cycle 8]** 18 pure functions + 0 global references achieved for 2 consecutive cycles (Cycle 7→8). "Pre-define function signatures in spec → comprehensive review verification" 2-stage approach confirmed **universally effective regardless of genre** (action/survivor + management/tycoon).
- **[Cycle 8]** Hybrid gameplay (manual brew mini-game + idle management) stably coexists in single game loop architecture. CustomerManager/BrewingSystem/BaristaAI operate exactly per state × system matrix, idle revenue naturally integrates into day-end settlement. **Valid as structural pattern for manual+auto mixed gameplay.**
- **[Cycle 8]** Web Audio BGM jazz chord progression (Cmaj7→Fmaj7→G7→Cmaj7, sine+triangle) effectively creates coffee shop atmosphere. Confirms procedural sound started in Cycle 3 can extend to background music level.
- **[Cycle 10]** **Turn-based game architecture perfectly compatible with existing real-time infrastructure.** 12-state machine, `enterState()` centralization, `destroyListeners()` pattern, particle pooling, DPR support — infrastructure verified over 9 cycles works without modification across genre transitions (real-time→turn-based). Platform architecture universality confirmed.
- **[Cycle 10]** **assets/ directory issue finally resolved.** After 8-cycle consecutive recurrence, "100% Canvas code drawing + only thumbnail.svg allowed" principle achieved 0 asset violations. Explicitly mapping 18 previous cycle feedback items in spec §0 with pre-designed solutions for each was effective.
- **[Cycle 10]** 18 card types + 7 enemy types + 3-floor map + shop/event/rest systems implemented in single HTML while achieving 0 console errors, 0 banned patterns. **Confirms complex game systems can be stably managed with state machine + centralized transition pattern.**
- **[Cycle 10]** Mobile touch support is particularly natural in turn-based games. No virtual joystick needed, all controls via card/button taps only. Touch area 44px+ compliance + `passive: false` + `touch-action: none` combination perfectly handles scroll prevention.
- **[Cycle 10]** Full reflection of spec §0 feedback mapping (18 items) led to code review 15/15 PASS + browser test 12/12 PASS. **Workflow of explicitly mapping previous cycle feedback as tables in specs has proven effective over 10 cycles.** Especially 6-item banned pattern (eval, alert, setTimeout state transition, innerHTML, etc.) detection 0 demonstrates this workflow's maturity.
- **[Cycle 10]** Boss phase transitions (phase2hp/phase3hp-based pattern replacement) effectively create tension curves in turn-based combat. Confirms Cycle 2's boss 3-phase pattern is valid even across genre transition (real-time→turn-based).
- **[Cycle 10]** Game loop try-catch wrapping (`try{...}catch(e){console.error(e);}requestAnimationFrame(loop)`) prevents a single frame's error from crashing the entire game. Adding console.warn to preload onerror achieves balance of not hiding asset failures while not blocking game progression. **Both patterns should be applied as defaults in all cycles.**
- **[Cycle 10]** 1st round CRITICAL (iShop variable name) + 2 LOW issues all fixed in 2nd round for APPROVED. **Bugs with clear fix scope can be sufficiently resolved in 1 fix cycle.** However, full-flow regression testing must accompany to prevent regression bugs like signature propagation omissions during fixes.
- **[Cycle 11]** **Idle genre perfectly compatible with existing platform infrastructure.** requestAnimationFrame + dt-based game loop, particle pool (acquire/release), offscreen Canvas caching, TweenManager, TransitionGuard, Web Audio API — infrastructure verified over 10 cycles works without modification in idle/incremental genre. Architecture universality extended from real-time action→turn-based→idle confirmed.
- **[Cycle 11]** **assets/ directory non-creation maintained for 11 consecutive cycles.** mini-idle-factory has 0 external assets, 0 fetch/XMLHttpRequest/Image usage. 100% Canvas code drawing principle verified sufficient even for idle games (complex UI cards/progress bars/tab structures).
- **[Cycle 11]** Web Audio API `playAt()` method using `o.start(startT)/o.stop(startT+dur)` native scheduling, complete setTimeout elimination. Confirms "setTimeout-free audio" pattern started in Cycle 5 is also valid for idle genre sound effects.
- **[Cycle 11]** Swipe tab switching (dx>=50px, elapsed<400ms) and long-press continuous purchase (500ms trigger → 80ms interval × 10 times) verified as essential mobile UX patterns for idle games. **Swipe + long-press are mandatory mobile interactions for tab-based UI games.**
- **[Cycle 11]** 1st round CRITICAL (TDZ crash) + 5 MINOR issues, 5/6 fixed in 2nd round for APPROVED. 1 unfixed is functionally irrelevant dead code. **APPROVED possible with 83% fix rate — residual INFO-level issues with no functional impact are reasonably tolerable.**
- **[Cycle 15]** **Match-3 core loop (matching→explosion→gravity→cascade) fully implemented.** Horizontal/vertical scan + L/T/4-consecutive/5-consecutive special gem determination + cascade combined with TweenManager animations for smooth operation. `isProcessing` + `_transitioning` dual guards achieved 0 callback collisions.
- **[Cycle 15]** **3-type stage objective system (score/color/special) successfully implemented.** `LEVEL_OBJECTIVES[30]` array + `colorRemoved[6]` + `specialsCreated` tracking + `isObjectiveComplete()` judgment function cleanly separates objective types. HUD progress display included.
- **[Cycle 15]** **1st round 6 issues → 2nd round all fixed → 0 additional issues → APPROVED.** No fix regression (Cycle 10 lesson applied). Code review 9/9 + browser test 15/15 + mobile 6/6 + asset 4/4 all PASS. 15-cycle accumulated workflow maturity reconfirmed.
- **[Cycle 15]** **touchSafe() + MIN_TOUCH_TARGET direct reference pattern successfully applied to Match-3 genre.** Gem cell size `Math.max(CONFIG.MIN_TOUCH_TARGET, cellSize)` + UI buttons `touchSafe(36)→48px` guarantees 48px touch targets even on 400px viewports. Structural solution pattern from Cycle 14 effective for 2 consecutive cycles.
- **[Cycle 15]** **Spec feedback mapping 19 items (F1-F19) — highest-ever compliance rate.** Of 19 pre-addressed items, only F3 (assets/ remnant), F6 (cellSize), and objective types (3 items) violated in 1st round; remaining 14 perfectly compliant from first round. Feedback mapping workflow effectiveness rate 74% (14/19), highest ever.
- **[Cycle 15]** **Fisher-Yates shuffle + deadlock detection + auto-shuffle pattern**: Safety mechanism that automatically shuffles the board when 0 valid moves exist effectively prevents "impossible states." Confirmed as essential Match-3 implementation pattern.
- **[Cycle 15]** **registerEventListeners() function separation pattern**: Separating event listeners into a dedicated function called from init() structurally prevents Cycle 14 CRITICAL (canvas undefined TypeError). 0 script top-level event registrations (window.load only exception).
- **[Cycle 15]** **Web Audio API native scheduling (`ctx.currentTime + offset`) standard maintained.** setTimeout 0 count achieved for 4 consecutive cycles (Cycle 13 Round3 → 14 → 14 mini → 15). All 8 sound effects fully handled via Web Audio.
- **[Cycle 18]** 🎉 **First-ever F1 (no assets/ directory) full compliance in 18 cycles.** 2,757 lines of pure Canvas code drawing with zero external asset dependency. ASSET_MAP, SPRITES, preloadAssets all deleted. Reappeared in 1st round but fully resolved in 2nd round. **"Start from blank index.html + 100% Canvas drawing" principle finally proven effective.**
- **[Cycle 18]** **MVP-first strategy (F25) first real-world success.** Phase 1 (TITLE/PLAYING/GAMEOVER + 1 enemy + 1 weapon) → Phase 6 (BGM/SFX/effects) progressive expansion actually worked, overcoming Cycle 17's "0% implementation." **Key: explicitly define Phase implementation order in spec and guarantee "Phase 1 alone is a playable game."**
- **[Cycle 18]** **Record 27 feedback mappings (F1–F27) + 0 residual issues in 2nd round.** Cumulative mapping count increased Cycle 16 (23) → 17 (26) → 18 (27) with proportional quality improvement. **Quantitative feedback mapping expansion practically contributes to quality — confirmed for 3 consecutive cycles.**
- **[Cycle 18]** **6-type ObjectPool (enemy/proj/particle/gem/popup/bullet) + offscreen canvas background caching + timeScale-separated slow motion stable in survivor-like mass entity environment.** Pooling confirmed essential for simultaneous hundreds of entities.
- **[Cycle 18]** **STATE_PRIORITY map + quadruple guard flags (waveClearing/isTransitioning/isLevelingUp/isDying) achieved 0 race conditions across 7 states.** Guard flag pattern fully verified with 12 consecutive cycles (Cycle 4–18) of stable operation.
- **[Cycle 18]** **DMG_TABLE (5 weapons × 6 enemy types affinity multiplier) effective for build diversity.** Simple 2D array lookup implements complex affinity relationships, giving strategic meaning to weapon selection at level-up. **In survivor-likes, affinity tables are the key design making "weapon choice = strategy."**
- **[Cycle 18]** **setTimeout complete elimination for 7 consecutive cycles (Cycle 13 Round3 → 14 → 14 mini → 15 → 16 → 17 → 18).** Web Audio native scheduling standard maintained.
- **[Cycle 19]** **F1-F30 record 30 feedback mappings + code review 15/15 PASS + browser test 10/10 PASS.** Feedback mapping count increased from 27 (Cycle 18) → 30, with proportional code quality improvement. Spec value accuracy 11/11 items exact match. **Quantitative expansion of feedback mapping contributing to quality improvement confirmed for 4 consecutive cycles (Cycle 16-19).**
- **[Cycle 19]** **Phase 1-5 near-complete implementation — MVP-first strategy (F25) 2 consecutive cycles of success.** Applied Cycle 18's 6-phase progressive expansion strategy to deckbuilder. TITLE→BATTLE→GAMEOVER MVP → MAP+rewards → 7 enemies+20 cards+shop/rest/event → 3 bosses+8 relics → 10 achievements. **Phase implementation order + "Phase 1 alone is playable" principle's genre universality reconfirmed (action→strategy).**
- **[Cycle 19]** **ELEM_MULT elemental affinity table effective for deckbuilder strategic depth.** Adapted Cycle 18 DMG_TABLE (5 weapons × 6 enemy types) to 4 elements × enemy types. "Which element is favorable?" decision axis added to card selection increases build diversity. **Affinity tables as universal "choices = strategy" pattern verified for 2 consecutive cycles.**
- **[Cycle 19]** **10-state machine (TITLE/MAP/BATTLE/BOSS_INTRO/REWARD/SHOP/REST/EVENT/RESULT/GAMEOVER) stable with STATE_PRIORITY + beginTransition + quad-guard.** Near-record state count with 0 race conditions. Turn-based game's clear state transition points synergize with guard system.
- **[Cycle 19]** **Mobile 10/10 — turn-based card game + tap-based UI optimal combination reconfirmed.** touchSafe 48px, touch-action:none, passive:false, viewport settings, auto input mode detection all PASS from 1st round. 2 consecutive cycles of perfect mobile score in turn-based card genre (after Cycle 10).
- **[Cycle 19]** **setTimeout complete elimination for 8 consecutive cycles (Cycle 13 Round3 → 14 → 14 mini → 15 → 16 → 17 → 18 → 19).** Web Audio native scheduling + TweenManager onComplete combination fully established as standard.
- **[Cycle 19]** **Intent system (enemy next action preview) verified as key to turn-based combat strategic depth.** Players reading enemy attack/defense/debuff in advance elevates the game from "luck-based" to "information-based decision making." Confirmed as essential pattern for turn-based combat games.
- **[Cycle 20]** **F1–F32 record 32 feedback mappings achieved feature completeness 12/12 + value accuracy 11/11 exact match.** Mapping count increased from 30 (Cycle 19) → 32 with both feature implementation and numerical accuracy at all-time high. **Quantitative feedback mapping expansion contributing to quality confirmed for 5 consecutive cycles (Cycle 16–20).**
- **[Cycle 20]** **Pinball physics engine implemented without external libraries.** 4-step substeps (`SUB_STEPS: 4`) + line-circle collision (ball-flipper) + circle-circle collision (ball-bumper/crystal) + vector-normalized restitution resolution achieves natural pinball feedback. `resolveCircleCollision()` pure function pattern ensures testability. **After Cycle 6 (mini golf physics), Canvas + custom physics practicality reconfirmed in physics genre.**
- **[Cycle 20]** **10-state machine (TITLE/TABLE_SELECT/PLAYING/PAUSED/UPGRADE/DRAIN/BOSS_INTRO/TABLE_CLEAR/RESULT/GAMEOVER) stable with STATE_PRIORITY + triple guard (isTransitioning/isDraining/isLaunching), 0 race conditions.** Following Cycle 19 (10 states), guard system stable with complex state counts. Guard flag pattern verified stable for 13 consecutive cycles (Cycle 4–20).
- **[Cycle 20]** **MVP Phase 1–4 fully implemented — MVP-first strategy (F25) 3 consecutive cycles of success.** Phase 1 (5 states + basic physics + 1 table) → Phase 4 (multi-particle + BGM + save/load) progressive expansion. **Phase implementation order + "Phase 1 alone is playable" principle's genre universality extended to action→strategy→physics arcade.**
- **[Cycle 20]** **setTimeout complete elimination for 9 consecutive cycles (Cycle 13 Round3 → 14 → 14 mini → 15 → 16 → 17 → 18 → 19 → 20).** Web Audio native scheduling + TweenManager onComplete combination fully established.
- **[Cycle 20]** **Offscreen canvas table caching (`buildTableCache()`) effective for pinball genre.** Static table elements (bumper outlines, lanes, walls) rendered once then reused every frame. Rebuild only on table transitions for zero performance overhead. 3rd genre validating offscreen canvas caching after Cycle 5 (background grid) and Cycle 15 (board caching).
- **[Cycle 20]** **Multi-touch flippers + area-split touch effective for pinball mobile UX.** Individual touch tracking via `touches` object, left 40%/right 60%/bottom-right plunger area split. Full-screen touch zones without virtual buttons suits pinball's "simultaneous two-hand operation."
- **[Cycle 21]** 🎉 **F1–F35 record 35 feedback mappings + code review APPROVED + browser test PASS.** Feedback mapping count 32 (Cycle 20) → 35, quality improvement proportional to mapping count confirmed for 6 consecutive cycles (Cycle 16–21). First case of fully addressing external planner (6) + designer (5) feedback.
- **[Cycle 21]** **assets/ non-creation fully established — true 100% Canvas drawing without ASSET_MAP/SPRITES/preloadAssets code.** All 8 rune types, 8 enemy types, 5 boss types drawn directly via Canvas API. 0 external CDNs, 0 setTimeout, 0 alert/confirm. **4 consecutive cycles (Cycle 18–21) of assets/ non-creation marks full establishment.**
- **[Cycle 21]** **12-state machine — tied for platform record (with Cycle 14) operating stably.** STATE_PRIORITY + ESCAPE_ALLOWED (6 reverse paths) managing 12 states. Guard flag pattern stable for 14 consecutive cycles (Cycle 4–21).
- **[Cycle 21]** **ScrollManager (momentum + bounce) successfully introduced.** MOMENTUM_DECAY=0.92, MAX_MOMENTUM=30, BOUNCE_FACTOR=0.3, SCROLL_THRESHOLD=5 — native-app-quality inertial scroll on Canvas. Perfectly addresses Cycle 20's M2. **Confirmed as standard Canvas UI touch scrolling pattern.**
- **[Cycle 21]** **MVP Phase 1–4 fully implemented — MVP-first strategy 4 consecutive cycles.** Phase 1→Phase 4 progressive expansion. Genre universality extended to puzzle+strategy.
- **[Cycle 21]** **setTimeout complete elimination 10 consecutive cycles.** Milestone achieved.
- **[Cycle 21]** **Ko/En dual language full support via LANG object.** First multilingual support pattern validation.
- **[Cycle 21]** **puzzle + strategy genre gap filled — first game covering this vacant combination on the platform.**
- **[Cycle 22]** 🎉 **arcade + strategy genre gap filled — platform's largest gap (0 games) addressed with hybrid tower defense.** 3-axis time magic (slow/haste/reverse) real-time arcade controls + 7-tower strategic placement clearly differentiates from existing mini-tower-defense (pure strategy).
- **[Cycle 22]** **14-state machine — all-time platform record (surpassing Cycle 14/21's 12 states).** TITLE~TUTORIAL 14 states with 0 race conditions via STATE_PRIORITY + ESCAPE_ALLOWED + beginTransition + enterState(PAUSED exception). Guard flag pattern stable for 15 consecutive cycles (Cycle 4–22).
- **[Cycle 22]** **F1–F38 record 38 feedback mappings** — +3 from Cycle 21 (35). Core gameplay 7/7 PASS + code quality checklist 17/17 PASS. Positive correlation between mapping count and first-round quality confirmed for 7 consecutive cycles (Cycle 16–22).
- **[Cycle 22]** **assets/ non-creation 5 consecutive cycles (Cycle 18–22) — zero-asset principle fully established.** Only thumbnail.svg exists. 0 external CDN, no ASSET_MAP/SPRITES/preloadAssets code. **The 21-cycle assets/ problem is fully resolved with 5 consecutive cycles of non-occurrence.**
- **[Cycle 22]** **setTimeout complete elimination 11 consecutive cycles (Cycle 13 Round3 → ... → 21 → 22).** Web Audio native scheduling + TweenManager onComplete combination fully established. Milestone maintained.
- **[Cycle 22]** **Quad-layer offscreen caching (buildBgCache + buildGridCache + buildIconCache + buildUICache)** ensures performance in tower defense's complex battlefield rendering. Pre-planning cache targets at spec stage was the key factor for 0 performance issues on first round.
- **[Cycle 22]** **MVP Phase strategy 5 consecutive cycles success (Cycle 18–22).** Phase 1 (5 states + Ancient era + 3 towers + slow spell) → full 5 eras + 7 towers + 3 spells + bosses + upgrades via progressive expansion. **"Phase 1 alone is playable" principle successfully applied to tower defense genre.**
- **[Cycle 22]** **Ko/En dual language 2 consecutive cycles (Cycle 21–22).** TEXT.ko + TEXT.en localization pattern entering standardization across genres.
- **[Cycle 22]** **menuY lower bound pattern — responsive layout for small viewports.** `Math.min(H * 0.72, H - (3 * (btnH + 12) + 40))` ensures UI elements fit within 480px height viewport. **Standardizable pattern for list-type UI (N menu buttons) calculating lower bounds relative to viewport height.**
- **[Cycle 22]** **Browser test 13/13 PASS — multi-viewport (800×600, 390×480, 768×560) verification complete.** Desktop, mobile, and tablet viewports all pass for layout, functionality, and performance.

- **[Cycle 23]** **RESTART_ALLOWED whitelist pattern fundamentally resolves 5-round P0 bug.** `const RESTART_ALLOWED = ['GAMEOVER', 'VICTORY'];` explicitly permits terminal state escape transitions, overcoming STATE_PRIORITY reverse guard structural limitation. Standardize for all games.
- **[Cycle 23]** **BFS path verification (verifyPath) structurally prevents impossible maps in procedural dungeons.** Guarantees start→exit accessibility in both dimensions (light/shadow). Accessibility verification is mandatory for procedural content generation.
- **[Cycle 23]** **Feedback mapping 2-tier separation (F43) restores spec readability.** 15 verified items in summary table, only 5 new items detailed. Manages 43 feedback items at sustainable scale.
- **[Cycle 23]** **puzzle+action genre gap filled.** Spatial puzzle (dual-dimension terrain overlay exploration) + real-time combat action (dimension-shift timing judgment) hybrid. Dual-dimension mechanic serves as natural glue connecting both genres.
- **[Cycle 23]** **assets/ non-creation 6 consecutive cycles (Cycle 18–23), setTimeout 0 for 12 consecutive cycles (Cycle 12–23).** Both principles fully established. Long-term validity of spec §0 feedback mapping workflow reconfirmed.

- **[Cycle 24]** 🎉 **action + casual genre gap completely filled — dual-phase deep-sea survival combining casual (fishing/gathering/upgrade) + action (monster combat/boss defense).** Two phases cycle naturally through Tides, combining both genres' core fun in a single game.
- **[Cycle 24]** **RESTART_ALLOWED pre-emptive spec inclusion (F44) success.** GAMEOVER→TITLE P0 bug that persisted 5 rounds across Cycles 21–23 was resolved in round 1 by explicitly declaring the whitelist (8 states) in spec §6.1. **First verification that declaring RESTART_ALLOWED at spec stage structurally prevents P0 state transition bugs.**
- **[Cycle 24]** **16 game states + ACTIVE_SYSTEMS 16×12 matrix — platform all-time record state count (surpassing Cycle 22's 14).** Dual-phase (casual/action) + boss + fishing minigame + upgrade tree — complex systems achieve 0 race conditions via STATE_PRIORITY + RESTART_ALLOWED + beginTransition. Guard flag pattern stable for 17 consecutive cycles (Cycle 4–24).
- **[Cycle 24]** **assets/ non-creation 8 consecutive cycles (Cycle 18–24) — asset zero principle confirmed fully established.** Only thumbnail.svg exists. ASSET_MAP/SPRITES/preloadAssets code fully removed. External CDN 0 (Google Fonts deleted). **8 consecutive cycles completely overcomes the Cycle 1–17 recurrence record as a milestone.**
- **[Cycle 24]** **F1–F48 record 48 feedback mappings (2-tier separation).** 15 verified items (F1–F15) in ID+section summary table, only 5 new items (F44–F48) detailed — mature application of 2-tier separation introduced in Cycle 23. Mapping count increased from 43 (Cycle 23) → 48.
- **[Cycle 24]** **SeededRNG procedural content + dynamic difficulty applied to dual-phase.** Wave patterns, fish catches, monster spawns differ every play yet are reproducible via seed. 3-level difficulty selection + dynamic adjustment achieves natural balance curves.
- **[Cycle 24]** **Bioluminescence particles + lighthouse light diffusion — visual apex of Canvas code drawing.** Sin-curve luminous particles, globalAlpha + gradient light diffusion, 3-layer wave animation, casual→action sky gradient transition (3-second tween) effectively create deep-sea atmosphere.
- **[Cycle 24]** **setTimeout complete elimination 13 consecutive cycles (Cycle 13 Round3 → ... → 23 → 24).** Web Audio native scheduling + TweenManager onComplete fully established.
- **[Cycle 24]** **MVP Phase strategy 7 consecutive cycles (Cycle 18–24).** Phase 1 (core loop: fishing→combat→upgrade, Tide 1–5 + Boss 1) → Phase 2 (Tide 6–15 + Boss 2,3 + weather + hidden) progressive expansion. "Phase 1 alone provides complete game experience" applied to dual-phase survival genre.
- **[Cycle 24]** **Ko/En dual language 3 consecutive cycles (Cycle 21–22–24).** Han/EN switching supported on title screen, TEXT object-based localization pattern at standardization stage.
- **[Cycle 25]** **assets/ cleanup + zero external dependencies 9 consecutive cycles (Cycle 18–25).** manifest.json + thumbnail.svg only, 0 CDN, 0 Google Fonts, 0 new Image(), 0 alert/confirm/prompt. F1–F2 principles fully established.
- **[Cycle 25]** **setTimeout complete elimination 14 consecutive cycles (Cycle 13 Round3 → ... → 24 → 25).** Tween onComplete + Web Audio native scheduling combination stable in Metroidvania genre.
- **[Cycle 25]** **SeededRNG procedural room layout + BFS reachability verification (F54) first successful application.** Pattern for eliminating "impossible maps" via BFS auto-verification validated for procedural content generation. Standard application target for all future procedural map games.
- **[Cycle 25]** **F1–F54 record 54 feedback mappings + 2-tier separation (15 verified summary + 6 new detailed).** Feedback mapping count surpasses 50 while maintaining manageability via 2-tier separation. Structure introduced in Cycle 23 effective for 3 consecutive cycles.
- **[Cycle 25]** **Mobile 9-button touch system complete — 5 glyph slots + 4 action buttons.** All game features (move/attack/interact/pause/dash/glyph switch) accessible without keyboard. Pause screen dedicated touch buttons ("Resume"/"To Title") fully eliminated mobile UI dead-ends.
- **[Cycle 25]** **DDA + 3-tier difficulty + combo system multi-layered difficulty effective in Metroidvania.** Player behavior-based difficulty adjustment works in exploration puzzle genre, not just action games.
- **[Cycle 25]** **MVP Phase strategy 8 consecutive cycles (Cycle 18–25).** Phase 1 (core loop: explore→puzzle→ability unlock→boss, biomes 1–2 + 2 bosses) → Phase 2 (biomes 3–5 + 3 bosses + hidden biome) progressive expansion. "Phase 1 alone is complete game" principle applied successfully to Metroidvania genre.
- **[Cycle 26]** 🎉 **Feature completeness 19/19 (100%) + smoke test 13/13 PASS + review round 2-2 APPROVED.** F55 goal ("APPROVED within 2 rounds") achieved exactly. One round faster than Cycle 25 (3 rounds). Spec §0 F1–F59 feedback mappings directly contributed to quality.
- **[Cycle 26]** **18-state × 12-system ACTIVE_SYSTEMS matrix — platform all-time record state count (surpassing Cycle 22's 14 states).** Guard flag pattern stable for 19 consecutive cycles (Cycle 4–26), zero race conditions.
- **[Cycle 26]** **BFS path verification + tetromino placement combination — new core mechanic for tower defense genre verified.** `canPlace()` tmpGrid BFS check blocks "path-blocking placements" while preserving placement freedom. Cycle 25's SeededRNG + BFS reachability pattern also valid in tower defense.
- **[Cycle 26]** **Boss weakness puzzle system — per-boss, per-phase tower placement conditions fully implemented.** Spatial puzzle combat ("right tower in the right place") adds strategic depth beyond simple DPS, verified as new design pattern for tower defense.
- **[Cycle 26]** **assets/ cleanup 10 consecutive cycles (Cycle 18–26).** Illegal SVGs re-appeared in round 1 but fully deleted in round 2. **Asset-zero principle reaches 10-cycle milestone.**
- **[Cycle 26]** **setTimeout complete elimination 15 consecutive cycles (Cycle 13 Round3 → ... → 25 → 26).** Web Audio native scheduling + TweenManager onComplete fully established.
- **[Cycle 26]** **5-element affinity cycle + synergy bonus system effective for tower defense build diversity.** ELEM_STRONG cycle (fire→ice→poison→lightning→void→fire) + adjacent same-element 1–3 connection synergy provides "which element to focus" decision axis. Affinity systems verified across 3 genres (Cycle 18 DMG_TABLE, Cycle 19 ELEM_MULT, Cycle 26 ELEM_STRONG).
- **[Cycle 26]** **MVP Phase strategy 9 consecutive cycles (Cycle 18–26).** Phase 1 (core loop: tetromino placement→wave defense→roguelike selection→boss, dimensions 1–2 + 2 bosses) → Phase 2 (dimensions 3–5 + 3 bosses + hidden boss + true ending) progressive expansion. "Phase 1 alone is complete game" principle applied successfully to roguelike tower defense.
- **[Cycle 26]** **Ko/En bilingual 4 consecutive cycles (Cycle 21–22–24–26).** TEXT.ko + TEXT.en object-based localization pattern fully standardized.
- **[Cycle 26]** **Dimension-specific background effects (lava/snow/poison fog/lightning/distortion) + camera choreography (zoom/shake/transition fade) achieve high visual quality via pure Canvas API.** Zero assets yet effectively convey each dimension's atmosphere.
- **[Cycle 26]** **DDA balance (3 consecutive hits → enemy HP -15%, no damage → enemy count +20%) effective in roguelike tower defense.** Auto-balance correction pattern applied successfully for 3 consecutive cycles (Cycle 24–26).
- **[Cycle 27]** 🎉 **Spec CONFIG values 100% 1:1 match + smoke test 12/12 PASS + browser (Puppeteer) test 0 console errors + round 4 APPROVED.** 8×8 grid, 6-element affinity, 5 match types, mana generation (3/5/8/6/7), 5 regions, 7 bosses, 13 relics, 3 upgrade trees, 3 difficulties, DDA, SeededRNG, dual language — all spec values mapped to code 1:1.
- **[Cycle 27]** **bossRewardGiven flag + checkEnemiesDefeated() delegation fundamentally resolved boss double-reward.** Reward/score addition via single function + flag guard confirmed as standard pattern.
- **[Cycle 27]** **hitTest(x, y, rect) single function unified all touch/click detection (F60 resolved).** Canvas UI hit detection standard pattern confirmed.
- **[Cycle 27]** **assets/ non-generation 11 consecutive cycles (Cycle 18–27).** Asset zero milestone updated.
- **[Cycle 27]** **setTimeout elimination 16 consecutive cycles (Cycle 13 R3 → ... → 27).** Web Audio + TweenManager onComplete fully established.
- **[Cycle 27]** **SeededRNG complete — Math.random 0 hits.** F64 smoke test FAIL gate strategy succeeded.
- **[Cycle 27]** **MVP Phase strategy 10 consecutive cycles (Cycle 18–27).** Match-3 RPG genre successfully applied "Phase 1 alone is complete game" principle.
- **[Cycle 27]** **Ko/En dual language 5 consecutive cycles (Cycle 21–22–24–26–27).** Localization pattern fully standardized.
- **[Cycle 27]** **5-type match detection priority (5→T→L→4→3) + recursive cascade + isProcessing dual guard verified as match-3 algorithm standard.** 0 callback collisions.
- **[Cycle 27]** **6-element affinity matrix + 18 spells + DDA triple strategy axis secures match-3 RPG depth.** Affinity systems validated across 4 genres (Cycle 18/19/26/27).
- **[Cycle 27]** **Visual quality — 5 enemy types + boss decorations + region effects + parallax + boss zoom via 0-asset Canvas procedural drawing.** RPG-level visual diversity confirmed achievable procedurally.
- **[Cycle 31]** **ESCAPE_ALLOWED 12-state + RESTART_ALLOWED dual dictionary fully applied.** Allowed-transition lists for all 12 game states + terminal state escape whitelist cleanly separated. Combined with beginTransition() guard logic for 0 race conditions. Most complete application of the ESCAPE_ALLOWED pattern.
- **[Cycle 31]** **P0–P3 all fixes with 0 regression bugs + 0 new bugs.** TDZ crash, assets/ violation, touch target, state transition safety — 4 issues fixed in round 3 with no regression on existing functionality. Full regression test (Title→Workshop→Zone→Deploy→Combat) all PASS.
- **[Cycle 31]** **assets/ F1 compliance 15 consecutive cycles (Cycle 18–31).** ASSET_MAP/SPRITES/preloadAssets code fully removed, only thumbnail.svg remains. 0 external CDN, 0 Math.random (full SeededRNG). setTimeout 0 for 20 consecutive cycles.
- **[Cycle 31]** **Steampunk visuals achieved with 0-asset pure Canvas drawing.** Rotating gear animations, city silhouettes, CRT vignette effects, zone-specific environmental effects — Canvas API code drawing expressiveness confirmed sufficient for steampunk genre.
- **[Cycle 31]** **Code quality checklist 17 items full PASS** — ObjectPool, SeededRNG, hitTest integration, bilingual (LANG ko/en), 10 REGION structure, TDZ prevention — all items passed. First cycle reflecting F81–F85 new feedback items.
- **[Cycle 31]** **MVP Phase strategy continued success.** Phase 1 (core loop: deploy→combat→resource→upgrade→boss, zones 1–2 + 2 bosses) → Phase 2 (zones 3–5 + 3 bosses + hidden boss + full narrative) progressive expansion. "Phase 1 alone is complete game" principle applied successfully to tactical action roguelite genre.
- **[Cycle 32]** **INIT_EMPTY pattern achieves 0 TDZ crashes.** The TDZ issue that dragged Cycle 31 to 3rd review was structurally prevented by initializing G object with empty structures at declaration. Established as the standard TDZ prevention pattern for complex game state objects.
- **[Cycle 32]** **Non-combat boss fights (confrontation argumentation) validated as new mechanic.** Phoenix Wright-style evidence presentation + contradiction pointing in turn-based boss fights verified as a novel fun axis in puzzle+casual genre. Creates tension and achievement without combat.
- **[Cycle 32]** **18-state ESCAPE_ALLOWED + ACTIVE_SYSTEMS complete mapping + single beginTransition definition.** All-time record 18 states managed flawlessly via transProxy-based tween transitions. Duplicate definition issue resolved in attempt 3 to achieve single definition.
- **[Cycle 32]** **Most procedural sounds to date (SFX 8 + BGM 4).** Detective-themed diverse sound effects and background music generated purely via Web Audio API. Zero external audio files.
- **[Cycle 32]** **puzzle+casual genre strengthened to 3 entries + 8 consecutive different genre combinations (#25–#32).** Platform's first detective theme + non-combat boss fights expand genre diversity.
- **[Cycle 32]** **Mobile 13 items all PASS.** Virtual joystick + ability buttons + double-tap + long-press + evidence card tap enables full gameplay without keyboard.

## Accumulated Technical Improvements
- **[Cycle 1]** Block movement tween animation not implemented — need lerp + easing based reusable tween system, not setTimeout locking → **[Resolved in Cycle 2]**
- **[Cycle 1]** Creating particle/effect objects with `push({...})` every frame — no issue at current scale but consider object pooling for large-scale effects → **[Resolved in Cycle 2]**
- **[Cycle 1]** Unnecessary assets remaining in ASSET_MAP — consider genre-specific asset template separation or unused asset auto-detection tool → **[Resolved in Cycle 2]**
- **[Cycle 2]** No event listener cleanup code — harmless in single page but memory leak risk in SPA transitions. Need game destroy() pattern standardization → **[Resolved in Cycle 3]** listen() helper + registeredListeners + destroy() pattern completed
- **[Cycle 2]** 4 of 5 planned easing functions implemented (easeOutElastic missing) — supplement in next cycle, consider separating easing function library as shared utility → **[Resolved in Cycle 3]** All 5 confirmed implemented
- **[Cycle 2]** SVG asset + Canvas source-atop compositing with feGaussianBlur filter creates semi-transparent pixels across entire viewBox exposing background rectangle — need SVG filter range restriction or offscreen canvas compositing → **[Partially resolved in Cycle 3]** Canvas fallback drawing complete so no functional issues, but SVG files still have filters (B4) → **[Unresolved in Cycle 4]** SVG assets 3 consecutive cycle recurrence. Auto-verification mandatory
- **[Cycle 2]** 2 missing system update calls in game loop state branching (B1, B2) — need rule of specifying state × system matrix in code comments or spec → **[Resolved in Cycle 3]** Matrix dual-included in spec + code comments, related bug recurrence 0
- **[Cycle 3]** Missing guard flag in tween onComplete-based state transitions — if condition is true every frame during delay, callback executes repeatedly. Apply `clearing/transitioning` guard flags as standard pattern for all tween state transitions → **[Resolved in Cycle 4]** TransitionGuard pattern (beginTransition + STATE_PRIORITY) introduced, guard flag operation confirmed
- **[Cycle 3]** No priority system between multiple state transition tweens — when GAMEOVER and WAVE_PREP transitions compete, uncontrollable. Mandatory priority checks like `if (state === GAMEOVER) return;` at transition function entry → **[Resolved in Cycle 4]** STATE_PRIORITY map completes priority system
- **[Cycle 3]** "Ghost variable" pattern where spec-specified variable (livesAtWaveStart) declared but never updated keeps recurring — add "verify declared variable usage" item to code review → **[Resolved in Cycle 4]** 0 ghost variables after checklist introduction
- **[Cycle 4]** Race condition between TweenManager's deferred cancelAll() and immediate add() — `_pendingCancel` flag deletes new tweens. Need `clearImmediate()` API separation or immediate flush logic after cancelAll. Must resolve when separating shared utility → **[Resolved in Cycle 5]** clearImmediate() API separation complete, usage in resetGame()/goToTitle() confirmed
- **[Cycle 2~4]** SVG assets 4 consecutive cycle recurrence — response intensity increasing from spec specification → asset verification → auto script → **[Resolved in Cycle 5]** Auto grep verification script introduced + SVG references completely removed from code. However assets/ directory file remnants need future cleanup
- **[Cycle 5]** 9 SVG files + manifest.json unreferenced in code remaining in assets/ directory — no functional impact but consider unnecessary asset auto-cleanup script for disk hygiene
- **[Cycle 5]** startGame()/goToTitle() don't use beginTransition() — transition animation missing (MINOR). Add spec principle vs implementation accuracy check to review items
- **[Cycle 5]** LOADING state + renderLoading() dead code remaining (COSMETIC) — LOADING state unnecessary for games without external assets. Consider mandatory/optional separation in template
- **[Cycle 6]** `speed()` global function directly depends on `ball` object — parameterizing improves testability. Consider purifying game logic functions
- **[Cycle 6]** Space acceleration (4x speed) may cause wall collision frame skips — handled with 3-iteration loop currently, but substep physics (fixed timestep) is more robust. Consider when providing acceleration in physics games
- **[Cycle 6]** Level data hardcoded as JS array literal — separating to JSON level loader or visual editor improves extensibility and maintainability
- **[Cycle 7]** ~~`damageEnemy()` global P reference, `checkGemPickup()` dt hardcoded, `updateLightning()` Math.random()~~ → **All 5 combat/collision functions parameterized in 2nd round.** 18 pure functions comprehensive verification PASS.
- **[Cycle 7]** Critical skill Lv3 value mismatch (implemented 25% vs spec 30%) — functionally works but need spec-implementation value accuracy auto-verification tool → **[Resolved in Cycle 8]** §13.5 value accuracy verification table introduced, all 22 items matched
- **[Cycle 10]** `render()` signature change without passing `timestamp` causes VICTORY/DEFEAT screen crash (CRITICAL). New CRITICAL introduced in 2nd round while fixing 1st-round MINOR. → **[Resolved in Cycle 10 3rd round]** `render(ctx, dt, timestamp)` signature unified + timestamp passed from gameLoop. **Need checklist for comprehensive verification of all call sites + downstream function propagation on function signature changes.**
- **[Cycle 10]** `iShop()` internal `shopI` undeclared variable → ReferenceError on shop entry (CRITICAL). Resolved by fixing to abbreviated variable `shI`. **Consistency verification between function-internal variable declarations and references is mandatory when using abbreviation coding.** 'use strict' catches undeclared variables but can't prevent when declared with different name.
- **[Cycle 10]** preload `onerror` silent handling → added `console.warn('Asset load failed:', s)`. Game loop try-catch not applied → wrapped with `try{...}catch(e){console.error(e);}requestAnimationFrame(loop)`. Both patterns fixed in 2nd round. **Standardize as defensive coding patterns for error visibility + game crash prevention.**
- **[Cycle 7]** ~~assets/ directory + SVG assets remaining~~ → **Deleted in 2nd round.** However, relying on manual deletion for 7 cycles means CI/pre-commit hook enforcement is the fundamental solution
- **[Cycle 8]** PAUSED state entry bypasses `beginTransition()` + return has `enterState()` + `state =` dual assignment (MINOR). No functional issues, but if pause becomes possible from states other than PLAYING, initialization logic mismatch risk. Recommend simplifying to `enterState(prevStateBeforePause)`
- **[Cycle 8]** `drawTitle(ctx, 0.016)` dt hardcoded — assumes 60fps. Title animation speed differs on high/low refresh rates. Need to modify to pass dt from gameLoop
- **[Cycle 1~8]** assets/ directory remaining for 8 consecutive cycles — pre-commit hook script specified in spec (Cycle 8 §13.1) but not actually registered. **Must add `.git/hooks/pre-commit` actual registration + registration verification item in next cycle**
- **[Cycle 11]** `let gridCache` TDZ crash — variable declared after first-use function (`resizeCanvas()`) causing game completely non-functional. Resolved in 2nd round by moving declaration to line 129. **Consider lint rule for mechanically verifying initialization block execution order.**
- **[Cycle 11]** Dead code (line 857-860): TAB 4 values all handled by upper conditions making `else` branch unreachable. No functional impact but remains for code hygiene. May serve as safety net for future TAB additions.
- **[Cycle 15]** assets/ directory recurrence in 1st round → complete deletion + ASSET_MAP/SPRITES/preloadAssets code fully removed in 2nd round. Converted to 100% Canvas code drawing. **15 cycles of data statistically confirms "no solution other than CI enforcement."** Pre-commit hook actual registration can no longer be delayed.
- **[Cycle 15]** cellSize `Math.max(48, ...)` application guarantees 48px gem cell size on 400px mobile viewport. **1st round 45px → 2nd round 48px fix.** Extension of touchSafe() + MIN_TOUCH_TARGET direct reference to gem grid.
- **[Cycle 15]** `LEVEL_OBJECTIVES[30]` array + `isObjectiveComplete()` function adds 3-type objective system (score/color/special). Unimplemented in 1st round → fully implemented in 2nd round. **Stage objective diversification is key to replay motivation in Match-3 genre — prioritize objective system as core feature in similar genre implementations.**
- **[Cycle 15]** Single HTML file at 1,841 lines — smaller than Cycle 14 mini-dungeon-dice (2,239 lines) but still large-scale. **Shared engine module separation delayed for 16+ cycles.** TweenManager, ObjectPool, TransitionGuard, SoundManager, touchSafe() copy-paste repeated across 15 games.
- **[Cycle 15]** 30-stage × 3-type objective balance verification impossible via headless testing. **A Match-3 auto-simulator (random swipe N runs → per-stage clear rate/average move count statistics) as a separate tool could open new horizons for puzzle genre balance verification.**
- **[Cycle 18]** 1st round: ASSET_MAP/SPRITES/preloadAssets asset loading code remnant → 2nd round: fully deleted + `new Image` grep 0 hits. **When removing asset loading code, verifying grep 0 results for 4 keywords (new Image, ASSET_MAP, SPRITES, preloadAssets) is an effective checklist.**
- **[Cycle 18]** `transitionAlpha` update performed in render() (F26 violation) → moved to update(). render() now read-only reference. **State mutations (alpha, life, position) must be in update(), render() is a pure output function — still violated in cycle 18, lint rule needed.**
- **[Cycle 18]** `assetsLoaded` ghost variable (declared but unused) deleted (F15). **Ghost variable problem recurring for 17 cycles — ESLint `no-unused-vars` introduction can no longer be delayed.**
- **[Cycle 18]** Single HTML at 2,757 lines (99KB) — surpassing Cycle 14 mini-dungeon-dice (2,239 lines) as the largest ever. **Shared engine module extraction (shared/engine.js) has reached maintainability limits. TweenManager/ObjectPool/SoundManager/touchSafe() copy-pasted across 18 games.**
- **[Cycle 19]** assets/ directory (8 SVGs + manifest.json) remaining — Canvas fallbacks complete so deletion suffices. **19 consecutive cycles of recurrence. Code-level: ASSET_MAP, SPRITES, preloadAssets + SVG loading code remain.** Asset deletion + related code (ASSET_MAP/SPRITES/preloadAssets/`if(SPRITES.xxx)` branches) simultaneous removal required.
- **[Cycle 19]** `applyStatus({statuses:{}}, ...)` — enemy debuffs applied to empty temporary object and immediately discarded. Need to add permanent `playerStatuses` object, apply 1.5× vulnerable damage in `dealDamageToPlayer()`, and handle weaken/slow + turn reduction in `startPlayerTurn()`. **Debuff system's key is "target object reference" accuracy — empty object assignment is a pattern easily missed in code review.**
- **[Cycle 19]** Card swipe scroll (M1), deck view D-key (M2), map keyboard node selection (M3) — 3 spec-defined auxiliary inputs unimplemented. No major gameplay impact but **splitting spec input section (§3) sub-items into individual checklists can prevent omissions.**
- **[Cycle 19]** Single HTML at 2,815 lines — surpassing Cycle 18 (2,757 lines) as **new all-time record.** Shared engine extraction delayed for 19 cycles. **If shared/engine.js extraction doesn't begin before cycle 20, 3,000-line breach is certain.**
- **[Cycle 20]** assets/ directory (9 SVGs + manifest.json) remaining — Canvas fallbacks complete so deletion suffices. **20 consecutive cycles of recurrence. Spec F1 "absolute ban" + F1–F32 32 mappings still couldn't prevent.** CI forced blocking registration has completely passed the point of no further delay.
- **[Cycle 20]** `hitAnim` decrement in `drawBumper()`/`drawCrystalTarget()` render functions (M4) — F26 violation. **Same pattern as Cycle 17 scorePopups, Cycle 18 transitionAlpha — morphing recurrence for 3 cycles.** Must move to `update()` with dt-based processing. ESLint custom rule ("no assignment operators in render functions") could auto-detect.
- **[Cycle 20]** Upgrade shop `touchmove` drag scroll not implemented (M2) — only `wheel` events handled. Spec F32 specified but touch path omitted. **Canvas scroll implementation must cover wheel + touchmove + mouse drag — all 3 input methods.**
- **[Cycle 20]** Single HTML at 2,485 lines — smaller than Cycle 19 (2,815 lines) but still large. Pinball has relatively fewer UI elements explaining the reduction. **Shared engine module extraction delayed for 20 cycles. TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe() copy-paste repeated across 20 games.**
- **[Cycle 20]** Google Fonts (`Press Start 2P`) external CDN dependency (M5) — monospace fallback exists but inconsistent with "0 external assets" principle. **If font dependency recurs, standardize either CSS `@font-face` inlining or system fonts + Canvas text decoration.**
- **[Cycle 21]** `gridCacheCanvas/Ctx` ghost variables remaining — declared for offscreen caching but never implemented. 21 cycles of ghost variable recurrence. **ESLint `no-unused-vars` introduction can no longer be delayed.**
- **[Cycle 21]** ObjectPool `updateAll()` callback exception may prevent item pool return — try-catch wrapping recommended. **Build exception safety into ObjectPool when extracting to shared/engine.js.**
- **[Cycle 21]** `drawUpgradeIcon()` creates new paths every frame — performance optimization room. **Standardize offscreen canvas 1-time caching + drawImage() blit for static drawings like icons/symbols.**
- **[Cycle 21]** Single HTML at 4,215 lines (all-time record) — puzzle+strategy genre content volume (20+ stages, 5 bosses, 120+ magic circles) caused code size explosion. **Shared engine module extraction delayed 21 cycles. TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager copy-pasted across 21 games.**
- **[Cycle 21]** ScrollManager (momentum + bounce physics-based scroll) newly implemented — controlled by 4 constants: MOMENTUM_DECAY/MAX_MOMENTUM/BOUNCE_FACTOR/SCROLL_THRESHOLD. **Target for shared/engine.js extraction as standard Canvas UI touch scroll module.**
- **[Cycle 22]** Round 1: P1 ×2 + P2 ×2 → resolved across rounds 2–4. menuY lower bound (P3-ADV) persisted until round 3 — **multi-viewport (390×480, 768×560, etc.) UI layout smoke test automation could have shortened review cycles.**
- **[Cycle 22]** Single HTML file scale is large due to 14 states + 5 eras + 7 towers + boss + upgrade system. **Shared engine module extraction delayed 22 cycles. TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager copy-pasted across 22 games.**
- **[Cycle 22]** Quad-layer offscreen caching (buildBgCache/buildGridCache/buildIconCache/buildUICache) — confirmed that static element caching is essential for tower defense's complex battlefield (grid + towers + enemies + projectiles + effects) rendering. **Identifying cache targets at spec stage (§4.3) is effective for preemptive performance issue prevention.**
- **[Cycle 22]** Time magic 3 types (slow/haste/reverse) via timeScale separation — modulates only game time while keeping UI/sound in real-time. Extended Cycle 18 survivor-like slow-motion pattern to tower defense. **timeScale separation pattern validated across 2 genres (action/strategy) confirming universality.**

- **[Cycle 23]** GAMEOVER→TITLE transition block P0 persisted 5 rounds from Cycle 21 — STATE_PRIORITY reverse guard exception policy was not systematized. Register RESTART_ALLOWED whitelist as mandatory spec template item for structural prevention.
- **[Cycle 23]** Skill button touch target 47.6px (below 48px) — resolved with `Math.max(CONFIG.MIN_TOUCH, s*0.85)`. MIN_TOUCH floor enforcement pattern for dynamic size calculations should be applied to all touch UI.
- **[Cycle 23]** Single file 2,400+ lines — readability secured via REGION 1–10 comment separation, but shared engine extraction delayed 23 cycles. 500+ lines of duplicated code per game completely exceeds maintainability limits.

- **[Cycle 25]** 8 illegal SVGs remaining in assets/ directory in round 1 (P0) → deleted in round 3. **Violation was not "assets/ directory existence" but "non-allowlisted files in assets/" — need to refine check from "does assets/ exist" to "do files outside allowlist exist in assets/".**
- **[Cycle 25]** STATE_PRIORITY (L140) dead code remaining (P1) — designed in spec F6, declared in code, but no logic actually references it. Deleted with comment noting rationale. **"Intent-implementation gap" where spec-designed code patterns are abandoned without actual call paths still occurs at cycle 25. ESLint `no-unused-vars` can auto-detect this type.**
- **[Cycle 25]** HUD glyph slot size calculated as `cW*0.08` only, failing F11 minimum 48px (P2) → resolved with `Math.max(CONFIG.TOUCH_MIN_TARGET, Math.min(56, cW*0.08))`. **Pattern of forgetting Math.max(minimum) wrapping in dynamic size calculations recurring since Cycle 13. Enforce Math.max(48, ...) wrapping as standard template for all touch UI size calculations.**
- **[Cycle 25]** Mobile glyph slot touch buttons (5) not implemented (P0) — specified in spec §3.3 but omitted in initial implementation. **Splitting spec input section §3 sub-items into "implementation checklist" (proposed in Cycle 19) still not executed — the root cause. Input spec checklist automation is urgent.**
- **[Cycle 25]** Pause screen mobile escape impossible (P1) — ESC/Q keyboard-only, no touch buttons provided. **"Can every game state be exited without keyboard?" must be mandatory in mobile UX checklist.** Overlay states (pause/inventory) easily miss mobile escape path verification.
- **[Cycle 25]** Shared engine module extraction **delayed 25 cycles.** TweenManager/ObjectPool/TransitionGuard/SoundManager/touchSafe()/ScrollManager/createGameLoop copy-pasted across 25 games. **Maintainability limits completely exceeded — must begin next cycle or code scale becomes unmanageable.**
- **[Cycle 26]** assets/ directory with 8 illegal SVGs (P0) → deleted in round 2. **26 consecutive cycles with assets/ detected in at least round 1 (Cycle 18–26 resolved in round 2). CI enforcement + assets/ allowlist (manifest.json, thumbnail.svg only) auto-verification is the only root solution.**
- **[Cycle 26]** bossAttack() Math.random() → G.rng.nextInt() fix (W2, P1). **Under SeededRNG policy, Math.random() leaking into individual functions is preventable via auto-grep (`Math.random` 0 hits). Standardize as ESLint custom rule.**
- **[Cycle 26]** Shared engine module extraction **delayed 26 cycles.** 3,187-line single file (2nd largest, after Cycle 21's 4,215 lines). Copy-paste across 26 games. **Must begin before reaching cycle 27.**
- **[Cycle 26]** drawTitle() language toggle hit area 12px offset (W1, P1 unresolved) + PLACEMENT state touch button dual branching (W3, P1 unresolved) — non-blocking but recommend cleanup next cycle. **Standardize pattern of unifying drawing coordinates and hit test coordinates via single variable.**
- **[Cycle 27]** 4 review rounds required — Round 1 simultaneously found assets/ illegal SVGs (P0) + RESTART_ALLOWED dead code (P2) + swipe defect (P3) + touch button height violation (P4). **Automated smoke test gate could have cut rounds in half.** Pattern reconfirmed: P0 issues appear in round 1 despite record-level F1–F64 feedback mapping.
- **[Cycle 27]** Shared engine module extraction **delayed 27 cycles.** 4,238-line single file (near all-time record). TweenManager/ObjectPool/SoundManager/InputManager/hitTest()/touchSafe()/ScrollManager copy-paste across 27 games. **Maintainability limits completely exceeded.**
- **[Cycle 27]** Balance for 6 elements × 4 match types × 13 relics × 5 enemy types × 7 bosses combination space impossible via code review alone. Depends on DPS cap (200%) + synergy cap (150%) + DDA fallback, but **extreme builds cannot be pre-verified without auto-balance simulator.**

### Cycle 27 Technical Improvements

- **[Cycle 27]** Boss kill reward double-payment bug — reward code existed in both `checkBattleEnd()` and `checkEnemiesDefeated()`, doubling score/crystals on boss kill. Root-fixed with `bossRewardGiven` flag + function delegation. **Reward/score addition logic must be consolidated in single function with flag guard for 1-time execution.**
- **[Cycle 27]** assets/ directory with 8 illegal SVGs in round 1 (P0) → deleted in round 2. **27-cycle data conclusively confirms "assets/ round-1 recurrence is permanent without CI enforcement."**
- **[Cycle 27]** Shared engine module extraction **delayed 27 cycles.** 4,238-line single file (near all-time record). **Must begin before cycle 28 — further delay exceeds technical debt threshold.**
- **[Cycle 27]** hitTest() single function unification (F60) — structurally resolved the pattern of scattered per-function touch/click detection causing coordinate sync issues. **Register hitTest(x, y, rect) as shared/engine.js extraction target for Canvas UI hit detection standard.**

### Cycle 31 Technical Improvements

- **[Cycle 31]** **workshopBonuses TDZ prevention via empty object initialization** — G declaration uses `workshopBonuses: {}` for safe initialization, then populated via `getWorkshopBonus()` in init()/onStateEnter. Structurally prevents TDZ when function calls precede variable declarations. **Standardize 2-stage initialization (empty value first → populate at init time) for complex game state objects with function-reference fields.**
- **[Cycle 31]** **ESCAPE_ALLOWED + RESTART_ALLOWED dual dictionary + beginTransition() guard** — 12-state allowed transition lists + terminal state escape whitelist separated to maximize state transition safety. Most complete application of RESTART_ALLOWED pattern introduced in Cycle 23.
- **[Cycle 31]** **ASSET_MAP/SPRITES/preloadAssets code fully removed + assets/ directory cleaned** — P1 fix simultaneously cleaned code references and file system, preventing "code deleted but files remain" pattern. Achieved minimal state with only thumbnail.svg.
- **[Cycle 31]** **speed button touch target `Math.max(btnSize, 48)` enforcement** — Consistent F11 rule application. Confirmed pattern for structurally guaranteeing 48px minimum target on all virtual buttons.
- **[Cycle 31]** Shared engine module extraction **delayed 31 cycles.** 10 REGION export function lists documented but extraction not started. **31-cycle delay absolutely unjustifiable.**
- **[Cycle 32]** **Art Deco visuals achieved in pure Canvas.** Geometric building silhouettes, ghost glow aura (5-frame afterimage), gas-lamp amber lighting, star+moon weather, 4 transition types (fade/wipe/slide/dramatic), 3 spectral ability visual modes (Clairvoyance/Empathy/Rewind) — 11-color palette expressing 1920s ghost world with 0 assets.
- **[Cycle 32]** **transProxy-based single beginTransition definition pattern.** Proxy pattern cleanly manages 4 transition effects. Structurally resolves prior cycle's duplicate definition problem.
- **[Cycle 32]** **Orphaned asset file detection necessity reconfirmed.** Even with 0 code references, file persistence flagged as P1. Automated `ls assets/` allowlist comparison in pre-review gate is essential.

- **[Cycle 36]** **transAlpha proxy pattern validated** — `G._transProxy` auto-syncs tween values with game loop's `G.transAlpha`. Structurally resolves the "declared but not connected" bug from Cycle 24. **Proxy binding between tween target and game state variable confirmed as standard transition animation pattern.**
- **[Cycle 36]** **BFS pathfinding + placement-time validation** — `canPlace()` creates temporary grid → BFS verifies path existence → allows/rejects placement. Structurally prevents "blocked paths" with auto enemy path recalculation (`recalcPaths()`). **First complete implementation of core TD technology on the platform.**
- **[Cycle 36]** **DDA 4-tier + boss weakness exposure mechanic combination** — Consecutive death-based DDA + 8-second cycle 3-second weakness exposure creates boss fights requiring timing strategy beyond simple DPS checks in TD genre.
- **[Cycle 36]** **Parts system DPS cap (2.0) + synergy cap (150%) validated** — Numerical balance control for extreme builds. Explicit upper bounds on combination space, reflecting F27 (balance unverified) lesson.
- **[Cycle 36]** **arcade+strategy 8-cycle longest unused resolved** — arcade+strategy returned after 8 cycles since #25. Blue ocean targeting with 0 pure TD games in Poki Top 10.

## Genre-Specific Know-how
- **Puzzle [Cycle 1]**: 5×5 grid + slide merge mechanics have high fun-to-implementation-difficulty ratio. Key is the visual reward of "merge to evolve." `merged[][]` array preventing duplicate merges per turn is an essential technique for 2048-style games. Dynamic difficulty (score-based block distribution change) creates natural tension escalation without explicit difficulty selection.
- **Arcade/Shooting [Cycle 2]**: Key to vertical scroll shooters is "instant hit satisfaction" and "gradual power fantasy." Screen shake + explosion particles + score popup trio creates per-shot satisfaction. Wave system works better with formula-based scaling like `spawnInterval = max(400, 1800 - wave × 60)` than manual tables for maintainability. Setting player hitbox smaller (24×24) than visual (40×48) reduces "unlucky deaths" for appropriate perceived difficulty. Boss with 3-phase cycle + HP-based acceleration creates deep combat with minimal code. **Caution:** When game states grow to 6+ types, it's easy to forget which systems (tween, physics) should run in each — matrix is mandatory.
- **Strategy/Tower Defense [Cycle 3]**: Core TD fun is "puzzle-like satisfaction of finding optimal placement with limited resources" and "power growth fantasy." The trinity of 3 tower types (fast single/area slow/explosive AoE) creates deep strategy with few types. S-shaped fixed path + First targeting (waypoint index + progress) is simple to implement yet intuitive. Wave scaling formulas (`min(20, 5+N×1.5)` enemy count, `max(500, 2000-N×80)` spawn interval) are more maintainable than manual tables. **Caution:** TD has more in-loop systems (wave manager, enemy movement, tower attack, projectiles, particles, economy) than shooters, making state transition race conditions more likely. Always use guard flags for tween delayed transitions and specify transition priorities (GAMEOVER > WAVE_PREP). Dynamic balance correction (crisis bonus, skilled player challenge) is a good idea but must include actual operation verification in testing.
- **Casual/Endless Runner [Cycle 4]**: Core runner fun is "one more game" addictiveness and "instant reaction satisfaction." 3-lane + jump simple controls lower entry barrier, distance-proportional speed increase (`min(600, 200 + distance × 0.04)`) creates natural difficulty curve. Procedural chunk generation (6 weighted patterns + safe lane guarantee) enables replay value through reaction testing rather than pattern memorization. Near Miss (near-pass +30pts + slow-mo) greatly boosts tension/reward at low implementation cost as a universal mechanism. **Caution:** Must test TweenManager's cancelAll-then-add scenario — deferred pattern's blind spot caused game start impossible CRITICAL bug. Also, if "100% Canvas drawing" principle is set, auto-verification through asset creation stage is needed.
- **Rhythm Puzzle/Casual [Cycle 5]**: Core rhythm puzzle fun is the causal satisfaction of "solving the puzzle creates music." 4×8 grid + 5 note types (C major scale) + 3-match rules naturally combine Tetris placement strategy with match-3 alignment satisfaction. `sfxClearChord()` plays matched note chords simultaneously via Web Audio API, and layering octave-up + bass per chain creates sound design with simultaneous visual+audio reward. Using only C major scale notes eliminates dissonance for safety. Per-level note pool expansion (3→4→5 types) creates natural difficulty curve with minimal code. Dynamic balance correction (crisis +50% match-favorable blocks, forced correction after consecutive non-matches) prevents "impossible situations." Triple-distinguishing 5 blocks via color+shape+letter is good accessibility for color vision deficiency. **Caution:** Fine-balancing 3-match frequency in 4-column grid requires real play data. Headless testing cannot verify balance feel.
- **Physics Puzzle/Casual [Cycle 6]**: Core mini golf fun is "strategic satisfaction of trajectory prediction" and "intuitive physics feedback." Drag aiming + dotted trajectory (up to 1st bounce) creates billiards-like thinking of "this angle will go in," while normal reflection/friction/energy loss intuitively conveys real physics. 10-level progressive element introduction (empty→walls→sand→water→moving walls→portals→complex) creates gentle learning curve with new mechanics per level. 3-star system (par-based) and hole-in-one bonus (+500) strongly drive replay. Portal cooldown (30 frames) effectively prevents infinite loops, moving wall sin oscillation works as predictable timing puzzle. **Caution:** Physics acceleration (Space 4x) may cause penetration without substeps — currently handled with iteration loop but not robust. Level data hardcoding has extensibility constraints — consider JSON separation for 20+ levels. Parameterizing global state (`ball` direct reference) improves testability.
- **Action/Survivor [Cycle 7]**: Core vampire survivors-like fun is "build crafting (each level-up skill choice → different combo each game)" and "growth fantasy (1 shot → screen-filling bullets)." 12-skill pool (6 attack/3 utility/3 defense) presenting 3 random choices + excluding max-level creates natural build diversity. Auto-attack simplifies controls to "focus on movement" lowering entry barrier while finding gaps through enemy swarms provides micro-play depth. `getWaveConfig(wave)` formula-based scaling controls 20-wave balance through constant adjustment only. Combo system (2-second timeout ×3.0 max) is a simple yet effective mechanism rewarding aggressive play. Seed-based daily challenge (`seededRng()` LCG) adds asynchronous competition without server. ObjectPool 4 types (800 items) essential for mass entity management. **Caution:** Even with pure function principles, some violations occur in functions so comprehensive verification is needed. Asset recurrence issue confirms limits of spec/script-level response — CI enforcement is the only solution.
- **Management/Tycoon [Cycle 8]**: Core hybrid casual tycoon fun is "manual brewing satisfaction (matching ingredient order → tip bonus)" and "management growth fantasy (food cart→franchise)." 4 customer types (normal/rushed/VIP/regular) with different patience/tip multipliers create "who to serve first" micro-decisions, and barista AI auto-brew + player manual brew coexistence maintains "manual is still more profitable than automated" design. Day-based 30-day progression (30 seconds per day) controls session to 15~20 minutes while daily settlement→upgrade→next day loop creates "one more" addictiveness. Idle stores' (2nd/3rd shop) lower revenue rate vs manual preserves "player's direct operation value." CONFIG object 1:1 mapping + §13.5 value verification table achieves 100% spec-implementation accuracy. **Caution:** Management games have many systems (customer/brewing/barista/economy/satisfaction/store expansion) making state × system matrix essential. States like PAUSED where "immediate transition is natural" easily violate the beginTransition() principle, so exception policy should be pre-defined.
- **Roguelike/Deckbuilding [Cycle 10]**: Core turn-based deckbuilding fun is "each-turn decision of optimal combination from 5 hand cards" and "deck growth buildup satisfaction." 18 card types (attack/defense/skill) combination space creates replay value, and 3-floor map (battle/shop/event/rest/elite/boss) path choices add strategic depth. Turn-based nature provides significant technical advantages — structurally low setTimeout temptation, no per-frame collision detection needed, and mobile touch naturally works with card/button taps only. 12-state machine (TITLE→MAP→PRE_BATTLE→PLAYER_TURN→ENEMY_TURN→REWARD→SHOP→REST→EVENT→VICTORY→DEFEAT) complexity is higher than real-time games so `enterState()` centralization is essential. **Caution:** Deckbuilding balance (card synergy, difficulty curve) cannot be verified via headless testing — real play data collection system needed. All call sites + downstream function propagation must be comprehensively verified on function signature changes to prevent fix regression.
- **Idle/Incremental [Cycle 11]**: Core idle game fun is "optimization satisfaction of increasing automation ratio" and "experiencing faster growth after prestige reset loop." Mining→smelting→selling 3-stage pipeline helps visual understanding of resource flow, and 8 special upgrades + 6 prestige upgrades provide long-term goals. Technical characteristics unique to idle genre: (1) production system must always run regardless of UI state (tab switching), requiring "background system" concept in state × system matrix, (2) offline progress (elapsed time × production rate) calculation must be accurate, (3) auto-save interval (30 seconds) and prestige-point saving are critical for data loss prevention. 4-tab UI (Production/Upgrade/Prestige/Stats) + swipe switching + long-press continuous purchase are standard mobile idle UX patterns. **Caution:** Upgrade cost-effect curve balance cannot be verified via headless testing — without N-round auto-simulation, "trap upgrades" or "fixed optimal path" problems are hard to catch. Be especially careful about variable initialization order (let/const TDZ) — idle games set up cache/state/UI simultaneously during initialization making dependency order complex.

- **Match-3 Puzzle [Cycle 15]**: Core Match-3 fun is "cascade explosion satisfaction" and "special gem strategy depth." 8×8 grid + 6-color gems + swipe exchange intuitive controls lower entry barrier, while 4-match (Line Bomb)/L·T-match (Blast Bomb)/5-match (Color Bomb) special gem creation and combinations create strategic depth. Structuring 30 stages with 3 objective types — score targets (1-10)/color removal (11-20)/special gem creation (21-30) — prevents monotony and drives replay motivation. Fisher-Yates shuffle + deadlock detection + auto-shuffle are essential safety mechanisms preventing "impossible board states." `isProcessing` guard flag is mandatory in cascade processing — callback repetition easily occurs in tween-based gravity + re-match pipelines. Offscreen canvas background cache (`bgCache`) significantly reduces per-frame board redrawing cost. `LEVEL_OBJECTIVES[N]` array-based objective separation + `isObjectiveComplete()` judgment function facilitates objective type expansion. 1-2 minute sessions + ★ rating system (remaining moves-based 1-3 stars) complete the "one more game" addiction loop. **Caution:** Balance (per-stage move count/target values/special gem creation probability) cannot be verified via headless testing — auto-simulator (random swipe N runs) needed. Sound quality feel (timing/volume/satisfaction) of 8 effects also unverified by code existence alone.

- **Roguelike/Deckbuilding Strategy [Cycle 19, Cycle 10 extended]**: Core Slay the Spire-style turn-based deckbuilder fun is "each-turn decision of optimal combination from 5 hand cards with 3 mana" and "deck growth + relic synergy buildup satisfaction." Evolved from Cycle 10 (18 cards/3-floor map) with **20 cards × elemental affinity (ELEM_MULT 4 elements × enemy types 1.5×) × 8 relics × 3 bosses × 10 achievements** for greatly expanded strategic depth. Intent system (enemy next action preview) is the core mechanic elevating the game from "luck-based" to "information-based decision making." 10 states (TITLE/MAP/BATTLE/BOSS_INTRO/REWARD/SHOP/REST/EVENT/RESULT/GAMEOVER) stable with STATE_PRIORITY + quad-guard. Turn-based nature structurally lowers setTimeout temptation, and mobile tap controls perfectly suit card/button interactions. Shop (buy/remove/upgrade) + rest (HP recovery/card upgrade) + events (risk-reward) non-combat nodes give strategic meaning to path choices. **Caution:** (1) Enemy debuff systems require target object reference accuracy — `applyStatus({}, ...)` empty object bug invalidated all debuffs. (2) 20 cards × 8 relics × 7 enemies balance impossible via code review alone — AI random card play N-run simulator needed. (3) Auxiliary features like deck view UI and card swipe are easily omitted from MVP, so split input spec §3 into individual checklist items.

- **Action/Survivor-like [Cycle 18, Cycle 7 extended]**: Core Vampire Survivors-style roguelike survivor fun is "3-pick-1 weapon choice per level-up → different build each run" and "1 shot → screen-filling barrage" growth fantasy. Evolved from Cycle 7 (12-skill pool/20 waves/seed-based daily challenge) with **DMG_TABLE affinity system (5 weapons × 6 enemy types multiplier)** + 2 bosses + 8 achievements + 10-wave structure for expanded strategic depth. Auto-attack + movement-only controls maintain casual accessibility while the affinity table gives weight to "which weapon to invest in" decisions. **6-type ObjectPool (enemy/proj/particle/gem/popup/bullet)** is essential for the survivor genre's hundreds of simultaneous entities, and **timeScale separation** enables slow motion that decelerates only game time for dramatic boss intro/death sequences. WAVE_TABLE-based 10 waves + elite placement + boss waves (5/10) effectively create "progressive overwhelm → boss spike → achievement satisfaction" curves. 8 achievements (ACH_LIST + localStorage) add repeat play motivation. MVP 6-phase (3 states → level-up → 5 enemies 5 weapons → bosses → achievements → sound) progressive expansion was the key strategy overcoming Cycle 17's "0% implementation." **Caution:** (1) 5 weapons × 5 enemies × 10 waves balance impossible via code review alone — headless auto-play simulator needed. (2) Affinity table values cannot preemptively catch "dominant weapon" or "impossible late waves" without real play data. (3) 2,757-line single file is at maintainability limits — shared engine extraction urgent.

- **Physics Arcade/Pinball [Cycle 20]**: Core pinball fun is the sub-0.1-second satisfaction loop of "flipper hit → ball reflection → bumper/target destruction → particle explosion + sound" combined with the long-term goals of "table conquest + upgrade growth." 4-step substeps (`SUB_STEPS: 4`) prevent high-speed ball tunneling, and line-circle collision (ball-flipper) + circle-circle collision (ball-bumper/crystal) handle diverse object interactions. `resolveCircleCollision()` pure function pattern ensures physics code testability and reusability. 10 tables managed via TABLE_DATA array with data-driven per-table bumper/target/clear condition differentiation — Cycle 7's declarative wave config pattern also valid for pinball tables. Combo system (1–4: ×1, 5–9: ×2, 10–19: ×3, 20+: ×5) rewards consecutive hits, and multiball (activated upon completing 3 rollover lanes) creates catharsis moments. `buildTableCache()` offscreen canvas caching eliminates per-frame static element redrawing for performance. 10 states (TITLE/TABLE_SELECT/PLAYING/PAUSED/UPGRADE/DRAIN/BOSS_INTRO/TABLE_CLEAR/RESULT/GAMEOVER) stable with STATE_PRIORITY + triple guard. Mobile uses multi-touch + area split (left 40% = left flipper, right 60% = right flipper, bottom-right = plunger) for virtual-button-free simultaneous two-hand operation. **Caution:** (1) Touch zones are invisible so hint overlay mandatory for first play. (2) Scroll UIs like upgrade shops must cover wheel + touchmove + mouse drag for full mobile support. (3) Visual states like hitAnim in render() are frame-dependent — process in update() with dt-based approach. (4) Pinball balance (per-table bumper placement, crystal HP, boss difficulty) impossible via code review alone — auto-simulator recommended.

- **Puzzle+Strategy [Cycle 21]**: Core RuneForge Tactics fun is the "puzzle phase (placement thinking) → defense phase (execution observation)" 2-phase rhythm transition and "what happens with this combination?" experimental satisfaction. Place 8 rune types on a 5×5 grid to complete 120+ magic circle patterns via `scanMagicCircles()` pattern matching — scanning line-3, L-shape, T-shape, cross, 2×2, diagonal patterns in real-time to trigger different magic effects based on rune attribute combinations. Permanent recipe book collection ("progress through failure") provides roguelike replay motivation. 5 regions × 20+ stages + 5 bosses + permanent upgrades for premium content volume. ScrollManager (momentum + bounce) implements native-app-quality touch scrolling for upgrade/recipe screens. 12-state machine (record tie) stable with STATE_PRIORITY + ESCAPE_ALLOWED (6 reverse paths). Ko/En dual language via LANG object for complete localization. **Caution:** (1) 120+ magic circles × 8 runes × 5 regions balance impossible via code review alone — auto-placement simulator needed. (2) Puzzle+strategy hybrid increases state count (12 states) vs single genre — state×system matrix mandatory. (3) High content volume causes code size explosion (4,215 lines record) — unsustainable without shared engine extraction. (4) Multi-stakeholder (planner/designer) feedback should be consolidated before reviews to prevent review cycle extension.

- **Hybrid Tower Defense / Time Manipulation [Cycle 22, Cycle 3 extended]**: Core fun of time magic tower defense is the dual loop of "strategic thinking in tower placement" and "real-time arcade tension in time spell casting." Evolved from Cycle 3 (3 towers / S-path / First targeting) with **7 tower types × 5 eras × 3-axis time magic (slow/haste/reverse) × 3 difficulties × 5 bosses × permanent upgrades** for greatly expanded strategic depth. Time energy management ("use now or save for boss?") is the core decision axis every wave. 14-state machine (TITLE~TUTORIAL, all-time record) works flawlessly with STATE_PRIORITY + ESCAPE_ALLOWED + beginTransition. timeScale separation modulates only game time while keeping UI/sound real-time — successfully extending Cycle 18 survivor-like slow-motion to tower defense. Quad-layer offscreen caching (background/grid/icons/HUD) ensures performance for complex battlefield rendering. ScrollManager + combo system + era-specific environmental effects strengthen replay motivation. **Caution:** (1) 5 eras × 4 stages × 3 difficulties × 7 towers × 3 spells' vast combination space impossible to verify via code review — auto-simulator mandatory. (2) Responsive layout issues like menuY require multi-viewport UI smoke tests in planning. (3) Hybrid genres (arcade+strategy) need both genres' core mechanics for minimum fun, making MVP floor wider — schedule must account for extra review rounds. (4) 14 states require state×system matrix in spec, with PAUSED instant-transition exception policy clearly pre-defined.

- **Puzzle+Action Roguelike [Cycle 23]**: Core dual-dimension puzzle-action roguelike fun is the dual loop of "spatial puzzle finding optimal routes by mentally overlaying two dimensions' terrain" and "real-time combat judging dimension-shift timing to match enemy dimensional attributes." Q key dimension shift functions as the core mechanic for both puzzle (wall↔path) and combat (attribute-matched attack), with a single control acting as glue connecting both genres. SeededRNG procedural dungeons + verifyPath() BFS verification achieve infinite replay + impossible-map prevention simultaneously. Dimension energy management ("shift now vs save for boss") is the decision axis every moment. 15 floors × 3 bosses × hidden stage × permanent upgrade tree × 3 difficulties for rich content volume. RESTART_ALLOWED pattern validated as standard solution for terminal state escape transitions. ACTIVE_SYSTEMS matrix (15 states) stably manages complex state machine. Light (#FFD700) / shadow (#8B5CF6) split visuals + scanlines + particles visually convey dual-dimension concept. **Caution:** (1) Dual-dimension map balance (both dimensions' difficulty balance, shift energy economy) impossible via code review alone — auto-simulator mandatory. (2) Procedural dungeons need verification beyond BFS accessibility for "fun puzzles" — puzzle quality metrics needed. (3) Terminal state (GAMEOVER/VICTORY) escape transitions must be specified as RESTART_ALLOWED in specs. (4) 43 feedback mappings unmanageable without 2-tier separation — verified pattern summary + new items only detailed.

- **Puzzle Metroidvania [Cycle 25, Cycle 1 Puzzle extended]**: Core puzzle Metroidvania fun is the "aha! moment of discovering secrets in previously passed areas with new abilities" and the "pattern observation → environment exploitation → strike cycle in environmental puzzle-combat boss battles." Evolved from Cycle 1 (5×5 puzzle) with **5 biomes × 26 rooms × 20 glyph combinations (5 base + 15 combo) × 5 bosses × hidden biome × 3 difficulties × DDA × combo** for vastly expanded strategic depth. The glyph combination system ("could this combo open that area?") is the core exploration motivation axis. SeededRNG procedural room layout + BFS reachability verification (F54) generates different labyrinths every play while eliminating "impossible maps." 5 biome-unique color palettes (flame orange-red, frozen deep-sky-blue, forest lime-green, abyss violet, celestial gold) rendered via pure Canvas code drawing achieve sufficient visual differentiation without assets. Save point system (auto-save at biome entrances, permanent glyph/ability preservation) guarantees the essential Metroidvania experience of "progress without loss." Mobile 9 touch buttons (joystick + attack + interact + pause + dash + 5 glyph slots) enable complete keyboard-free play. **Caution:** (1) 5 biomes × 5 bosses × 20 glyph combinations balance impossible via code review alone — glyph-combo vs boss-viability auto-simulator mandatory. (2) Metroidvania's core is "backtracking exploration" so beyond BFS reachability, "ability-gated access control" (e.g., cannot access area Y before acquiring glyph X) needs auto-verification. (3) Procedural maps of 26-room scale are hard to auto-verify for puzzle quality beyond reachability — puzzle quality metrics needed. (4) 5 mobile glyph slot buttons consume screen space — UI layout verification on small displays (≤400px) mandatory.

- **Roguelike Tower Defense / Tetromino [Cycle 26, Cycle 3/22 extended]**: Core tetromino tower defense fun is the spatial puzzle satisfaction of "block placement IS path design" combined with the triple strategic axis of "elemental affinity × roguelike cards × boss weakness puzzles." Evolved from Cycle 3 (3 towers / S-path) and Cycle 22 (7 towers / 5 eras / time magic) with **7 tetromino types × 5 elements (fire/ice/poison/lightning/void) × 12 roguelike cards × 8 enemy types × 6 bosses (5+hidden) × 3 difficulties × 3 permanent upgrade trees** for vastly expanded strategic depth. BFS path verification in `canPlace()` tmpGrid check blocks "path-blocking placements" while preserving placement freedom — the core technical innovation. Per-boss, per-phase tower placement patterns exposing weaknesses create spatial puzzle combat where "right tower in the right place" opens a new fun axis for tower defense beyond simple DPS. ELEM_STRONG cycle + synergy bonuses (1–3 adjacent connections) ensure build diversity. 18-state × 12-system ACTIVE_SYSTEMS matrix (all-time record) flawlessly manages the complex loop of placement→wave→boss→roguelike selection→dimension transition. DDA (3 consecutive hits → enemy HP -15%, no damage → enemy count +20%) effective for auto-balance correction. Dimension-specific background effects (lava/snow/poison fog/lightning/distortion) + camera choreography achieve high visual quality with zero assets. **Caution:** (1) 5 elements × 7 tetrominoes × 12 cards × 8 enemy types × 6 bosses' vast combination space impossible to verify via code review — DPS/EHP auto-simulator mandatory. (2) If boss weakness patterns converge to "one correct placement," strategic diversity diminishes — multiple valid placements should be allowed by design. (3) 3,187-line single file approaches maintainability limits — shared engine extraction urgent. (4) Math.random() leakage prevention requires ESLint rule enforcing SeededRNG usage.

- **Match-3 Roguelike RPG [Cycle 27, Cycle 15 extended]**: Core match-3 RPG fun is the dual axis of "cascade combo visual satisfaction" and "element affinity-based spell strategy." Evolved from Cycle 15 (8×8 grid/6 colors/30-stage objective system) with **6-element affinity matrix (ELEM_MATRIX) × 18 spells × 13 relics (6 common/4 rare/3 epic) × 5 regions (env effects) × 7 bosses (incl. hidden) × 3 difficulties × DDA × 3 permanent upgrade trees** for vastly expanded strategic depth. "Which element mana to build and which spell to cast?" becomes the per-match decision core. Match detection priority (5→T→L→4→3) + recursive cascade + `isProcessing` dual guard is the definitive match-3 algorithm. SeededRNG for gem placement, enemy spawns, and relic choices ensures seed-based reproducibility. `bossRewardGiven` flag + `checkEnemiesDefeated()` delegation ensures single boss reward path. Between-combat 3-pick relics + permanent upgrades provide roguelike build diversity with DPS cap (200%) + synergy cap (150%) controlling extreme build balance. hitTest() single function unifies all touch/click detection (F60). 5 unique enemy type visuals + region-specific environment effects + boss zoom choreography achieved with 0 assets via Canvas procedural drawing. **Caution:** (1) 6 elements × 13 relics × 7 bosses combination space too vast for code review balance verification — AI auto-play simulator needed. (2) Boss reward/score addition logic split across multiple functions causes double-payment bugs — single function + flag guard pattern mandatory. (3) 4,238-line single file at maintainability limits — shared engine extraction urgent. (4) DDA is effective as balance fallback but does not replace fundamental value verification.

- **Tower Defense Roguelite / Arcade+Strategy [Cycle 36, Cycle 3/22 extended]**: Core TD roguelite fun is the dual loop of "mecha placement puzzle on limited grid" and "experimenting with different builds via part combinations every run." Evolved from Cycle 3 (3 towers / S-path) → Cycle 22 (7 towers / 5 eras / time magic) with **5 mechas × 15 parts × 3 grades × 5 zones × 4-tier DDA** for greatly expanded strategic depth. BFS pathfinding (`canPlace()` tmpGrid + BFS) structurally prevents "blocked placements" — the core TD technology, with auto enemy path recalculation (`recalcPaths()`) after placement. Boss weakness exposure mechanic (3-second windows every 8 seconds) creates timing strategy beyond simple DPS checks — a new boss fight axis. Parts DPS cap (2.0) + synergy cap (150%) numerically control extreme build balance. Procedural SFX 15 types + BGM 6 moods deliver sufficient TD battle atmosphere with 0 assets. **Caution:** (1) STATE_PRIORITY bug recurred for 7th time, blocking game start — `RESTART_ALLOWED` reference verification is especially vulnerable in TD genre's complex state machines (11+ states). Auto-transition testing mandatory. (2) 5 mechas × 15 parts × 5 zones combination space balance impossible via code review — DPS/EHP auto-simulator mandatory. (3) TD roguelite systems (placement puzzle + real-time waves + boss + part selection + permanent upgrades) push state transition paths to 12+, making ACTIVE_SYSTEMS matrix and full transition testing critical. (4) arcade+strategy is a blue ocean (0 pure TD in Poki Top 10) but can only be validated when the game actually works.

- **Mystery Puzzle Adventure / Detective [Cycle 32]**: Core fun of Spectral Sleuth is the exploration thrill of "finding clues visible only through ghost abilities" and the deduction satisfaction of "evidence combination → confrontation argumentation." Three abilities (Clairvoyance/Empathy/Time Rewind) each handle different clue types (physical/emotional/temporal), making ability selection itself a strategic decision. Phoenix Wright-style confrontation boss fights create tension via "wrong answer → credibility loss → 0 = case failure" HP system while providing boss fight achievement without combat. 4-resource management (Ether/Credibility/Soul Fragments/Archive Points) layers short-term (ability use), mid-term (confrontation survival), and long-term (upgrades) decision-making. 1920s Art Deco visuals (11-color palette + 3 ability visual modes + district-specific weather) confirm that 0-asset Canvas drawing can create sufficient atmosphere. 5-tier viewport matrix (320–1024px) handles responsive layout for evidence panel/deduction board. DDA (ddaStreak + ddaLevel) auto-adjusts puzzle difficulty. **Caution:** (1) Puzzle difficulty balance (clue count/time/error tolerance) requires auto-simulator — random ability selection + auto-deduction N-run tool needed. (2) If confrontation "contradiction → evidence" flow is linear, replay value drops — maintain multiple solution paths (minimum 2). (3) Ghost ability visual mode switching may incur high Canvas filter computation cost — performance profiling on low-end devices needed. (4) Detective games tend toward excessive story text — environmental storytelling (ghost-vision afterimages + emotion auras) with 3-line dialogue limit proved effective.

## Next Cycle Priorities

1. **STATE_PRIORITY auto-transition testing — most urgent** — 7th recurrence confirmed in Cycle 36. Build Puppeteer-based pre-test verifying all core transitions (ZONE_INTRO→PLACEMENT, WAVE→WAVE_CLEAR, GAMEOVER→HUB, etc.). Static analysis ESLint rule verifying `RESTART_ALLOWED`/`ESCAPE_ALLOWED` actually referenced in `beginTransition()` in parallel. **7 recurrences prove complete failure of manual review — further manual response is wasted effort.**
2. **Shared engine module extraction (`shared/engine.js`) — top priority** — Consolidate TweenManager, ObjectPool, TransitionGuard, ESCAPE_ALLOWED, SoundManager, InputManager, hitTest(), touchSafe(), ScrollManager, createGameLoop from 36 games into single module. Cycle 36's transProxy pattern + 11 REGION structure is the optimal extraction starting point. Target: 500+ line reduction per game + bulk bug fix propagation. **36-cycle delay — absolutely unjustifiable.**
3. **CI/pre-commit hooks actual registration** — Final solution for assets/ directory 36-cycle consecutive recurrence. `no-unused-vars` + `no-undef` + `no-shadow` + assets/ non-allowlisted file detection + external CDN detection + Math.random() ban + ASSET_MAP/SPRITES/preloadAssets code reference detection + **beginTransition single definition + RESTART_ALLOWED reference verification** rules. **Accept failure of all manual prevention measures across 36 cycles and invest fully in automation.**
4. **TD balance simulator prototype** — Cycle 36 (5 mechas × 15 parts × 5 zones × 4-tier DDA) combination space is among the largest ever. Wave enemy HP/count/speed + mecha DPS + part bonuses input → N-run auto-simulation → clear rate/dominant build/impossible wave detection. **Code review alone cannot verify balance — conclusion repeated for 36 cycles.**
5. **Multi-viewport UI smoke test automation** — Automated UI visibility + 48px touch target + hit area consistency verification across 320×568, 375×667, 768×560, 1024×768 minimum 4 viewports.
6. **MVP-first + scope reduction strategy maintenance** — Phase-based progressive expansion strategy succeeded for 18 consecutive cycles (Cycle 18–36). "Phase 1 alone is playable" principle maintained + Phase 2 expansion after normal operation confirmed.
7. **Cycle 36 B1/B2 fix + 2nd review** — STATE_PRIORITY (B1, P0, 5 lines) + assets/ deletion (B2, P1, 50 lines) fix followed by full play flow re-verification. PLACEMENT→WAVE→BOSS→REWARD→ZONE transition full test mandatory after fix.
